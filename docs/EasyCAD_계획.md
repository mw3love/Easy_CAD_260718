# Easy CAD — 계획서

> 빠르고 쉬운 순서도/간단도면 작성기. 최종 목표는 **PDF 인쇄**, 기존 CAD 자산과의 **DXF 상호운용**.
> 작성일 2026-07-18. 이 문서는 살아있는 계획서 — 진행하며 갱신한다.

---

## 1. 확정된 결정 (심층 인터뷰 결과)

| 축 | 결정 | 근거 |
|---|---|---|
| 플랫폼 | **PyQt6 데스크톱** | pasteflow의 2,949줄 벡터편집기 재사용 · QGraphicsScene 네이티브 성능 · CAD 클립보드/PDF가 데스크톱에서 쉬움 · 신규 의존성 0 |
| CAD 상호운용 | **DXF 파일 왜복** (`ezdxf`) | 개별 객체 정확 인식 · 베지어→SPLINE 변환 · 상용 CAD 엔진 불필요 |
| v1 필수 | **기본 작도 + PDF 출력** | "속도·쉬움" 최우선. 나머지는 검증된 코어 위에 단계적으로 |
| 빌드 vs 바이 | **직접 만든다** | draw.io 인터페이스 불편(경험) · QCAD는 과함 · 둘 다 사랑하는 스냅+베지어 UX 못 줌 |

## 2. 토대 (이미 있는 것 — 밑바닥부터가 아니다)

`C:\Users\7make\Dev\Paste_flow\pasteflow\ui\image_annotator.py` (2,949줄, PyQt6 `QGraphicsScene`):

```
image_annotator.py
├── _RectItem / _EllipseItem / _LineItem / _PathItem   (도형)
├── _ArrowItem                                          (큐빅 베지어 화살표)
├── _HandleResizeMixin                                  (핸들 리사이즈)
├── _border_snap_at / _endpoint_border_snap             (테두리 스냅)
└── _recompute_snap_curve                               (스냅 시 곡선 재계산)
```

→ Easy CAD는 이 편집기를 **독립 무한캔버스 앱으로 승격**하는 프로젝트다.

**pasteflow와의 관계 (추천):** 새 독립 저장소로 코어를 **복사해서 분기**한다(공유 라이브러리 아님).
- 근거: CAD 기능으로 크게 갈라질 예정이라, 1인 개발에서 공유 패키지의 결합 비용이 이득보다 큼.
- 트레이드오프: pasteflow 쪽 버그픽스가 자동 전파되지 않음(수동 반영). 감수.

## 3. 아키텍처 핵심 결정

**A. 좌표계 / 단위**
CAD 왕복과 A-size 인쇄를 위해 **mm 단위 월드좌표**를 채택(화면 픽셀과 분리). 무한 캔버스는 월드좌표 위에서 팬/줌. Y축 방향(CAD는 Y-up, 화면은 Y-down)은 DXF 입출력 시 변환.

**B. 문서 모델 (가장 중요한 리팩터 지점)**
pasteflow 편집기는 QGraphics 아이템을 사실상 모델로 쓰는 구조로 보임(`확인 필요` — grep만 했고 완독 안 함). Easy CAD는 결국 **모델↔뷰 분리**가 필요하다:
- 이유: DXF 매핑 · Undo/Redo · 저장/열기 · "객체 개별 인식" 전부 깨끗한 엔티티 모델을 전제로 함.
- 방침: v1은 속도를 위해 기존 아이템 구조를 최대한 재사용하되, **각 아이템이 직렬화 가능한 의미정보(타입·기하 파라미터)를 반드시 지니게** 설계한다. 정식 문서모델은 Phase 3(DXF 직전)에 도입 — 그때 리팩터 규모는 pasteflow 코어 완독 후 확정.

**C. Undo/Redo**
`QUndoStack` 기반 커맨드 패턴으로 Phase 1부터 도입(뒤에 붙이면 고통). 모든 편집을 커맨드로.

## 4. 작업 순서 (단계별 · 각 단계 검증 기준 포함)

### Phase 0 — 셋업 & 코어 추출
- 새 저장소 `Easy CAD` 생성, `git init`, PyInstaller 스펙(pasteflow 참고)
- `image_annotator.py` 편집기 코어 이식(도형·베지어·스냅 아이템)
- 무한 캔버스: `QGraphicsView` 팬(스페이스/휠)·줌(Ctrl+휠)
- **검증:** 빈 캔버스에서 사각형·베지어 화살표 그리고 테두리 스냅 동작 확인

### Phase 1 — v1: 그리기 + PDF ⭐(사용자 v1 필수)
- 툴바: 선택 / 사각형 / 타원 / 선 / 베지어 화살표 / 텍스트
- 테두리 스냅(재사용)
- **Undo/Redo** (`QUndoStack`)
- 네이티브 저장/열기 — JSON 문서 포맷(문서모델 씨앗)
- **PDF 출력**(`QPrinter`): 전체 fit-to A4~A1 · 선택영역만 출력
- **검증(실조건):** 순서도 1장 그려 A4 PDF 출력 → 인쇄물 확인 → 저장 후 다시 열기

### Phase 2 — 속도 UX (AutoCAD식 생산성)
- **연속 복사붙여넣기**(한 번 copy → 반복 paste)
- **좌/우 드래그 선택**(왼→오 완전포함 window / 오→왼 걸침 crossing)
- **선 더블클릭 라벨**(lucid 편의)
- 회전 / 미러 / 스케일 / stretch (다중 선택)
- **o-snap 확장 + 토글**(endpoint·mid·center·grid, 켜고 끄기)
- **검증:** 복잡 순서도를 복사 위주로 빠르게 재현, 시간 측정

### Phase 3 — CAD 상호운용 (DXF) — **완료**
- **문서모델 씨앗**(`.ecad` 직렬화, 각 아이템이 타입·기하 파라미터 보유) — 정식 리팩터 대신 재사용 방침대로.
- `ezdxf` export **완료**(`3f4afde`): 우리 객체 → DXF, **베지어→SPLINE** 변환. 타입별 레이어(EC_*)·true_color·Y축 뒤집기.
- `ezdxf` import **완료**(`dd31967`): LINE·LWPOLYLINE·CIRCLE·ELLIPSE·ARC·SPLINE·TEXT·MTEXT → 개별 객체.
  레이어 힌트로 타입 판정·회전 흡수. 후속: 구식 POLYLINE 흡수.
- ~~INSERT(BLOCK) 흡수~~ — **완료**(2026-07-29, 프록시검증 — 합성 DXF만, 실조건 대기). 외부 무료
  DXF 심볼/블록 라이브러리 활용 목적. 상세: CLAUDE.md 신규기능 항목.
- 펜 두께 왕복 **완료**(`262a904`): XDATA(EASYCAD/1040). 후속: AutoCAD 두께 렌더용 lineweight 병행.
- **레이어** — 타입별 EC_* 레이어로 분리 export(패널 UI는 후속).
- **검증(실조건) 완료 ✓** — 우리 DXF(`789.dxf`)를 **AutoCAD 2022**에서 열어 네모·타원·마름모·화살표·텍스트가
  **개별 엔티티로 인식**됨을 확인(2026-07-20). Phase 3 완료 게이트 충족. 왕복(Easy CAD 재가져오기)도 정상.

### Phase 4 — 문서 완성도 (진행)
- **이미지 삽입(PNG·JPG) — 완료**: `_ImageItem`(rect 기반 → 8핸들 리사이즈·회전·stretch·undo·그룹변형
  재사용, 꼭짓점=종횡비 고정·변=자유). 파일 메뉴 `Ctrl+Shift+M` + 캔버스 드래그앤드롭. `.ecad`에
  base64 embed(무손실 PNG, 원본 해상도 보관·표시만 축소). PDF 자동 렌더, DXF는 제외(조용히 skip).
  실조건검증 완료(2026-07-20). 후속: 클립보드 붙여넣기.
- **표 삽입(table) — 완료(실조건검증 2026-07-20 ✓)**: `_TableItem`(rect 기반 → `_ImageItem`
  처럼 리사이즈·회전·undo·그룹변형·PDF 재사용). NxM 균등 비례 격자 + 첫 행 헤더. 셀 더블클릭→인라인
  편집(`_CellEditor` — Enter=아래·Tab=오른쪽 줄넘김·Esc=취소). 삽입 `Ctrl+Shift+B`. `.ecad` 직렬화,
  DXF 제외. 스코프(deep-interview 2026-07-20): 균등만·인라인편집. 밖: 개별 열폭·셀 병합·붙여넣기 파싱.
- **표제란 / paper space — 완료(실조건검증 2026-07-20)**: `_TitleBlockItem`(모델공간 위 A-size
  용지 프레임 객체 — 진짜 paper space·뷰포트 도입 안 함, deep-interview 2026-07-20). 우하단 3행 표제란
  표(발주처/프로젝트·도면번호·도면명·축척·작성/검토/날짜) + 도면 테두리. 삽입 `Ctrl+Shift+T`(용지 크기·
  방향), 더블클릭→필드 편집 폼. 프레임 있으면 PDF가 그 용지경계·크기·방향으로 자동 전환. `.ecad` 직렬화.
  스코프 밖(합의): 다중 페이지·축척 자동계산(텍스트 필드로)·DXF에 표제란 export·필드 undo. §7 다중 페이지
  질문은 '단일 프레임'으로 매듭. 실조건검증 ✓ — 한글 필드·더블클릭 폼·A2 PDF 사용자 확인.
- ~~**Mermaid import**(순서도 코드 → 편집가능 도형 자동배치)~~ — **완료**(실조건검증 2026-07-21). 판단 근거는 §6
- **검증:** 표+이미지 포함 도면을 A2 PDF로 출력

### Phase 5 — AI
- **이미지→도면**: 사진 업로드 → 도형/텍스트 인식 → 편집가능 객체
- 재료: jbnu-gateway 비전 모델 + pasteflow의 winocr(OCR) 재사용
- **검증:** 인쇄된 도면 사진 1장을 불러와 주요 블록·선이 편집가능 객체로 복원되는지

### Phase 6 — 편집 경험 현대화 (UI/UX) ⭐ **현재 우선**

> 초기 계획(Phase 0~4) 기능은 거의 구현 완료. 이 단계는 "가벼운 CAD + 나에게 맞춤"의
> **사용성**을 끌어올린다. 세부 요청 종합 + deep-interview식 확정(2026-07-22).

**확정 결정 (2026-07-22):**

| 축 | 결정 |
|---|---|
| 다크모드 | **다크 기본 + 라이트 토글**(보기 메뉴/버튼) — CAD 관습·사용자 선호 |
| 빠른 편집 UI | **선택 시 객체 위에 뜨는 플로팅 컨텍스트 툴바**(FigJam식) |
| 화살표 통합 | 곡선+직선 화살표 → **1개 도구 + 하위옵션**(곡선/엘보/직결). 일반 '선'은 **별도 유지** |
| 진행 순서 | 시각·UX 토대(M1) → 편집 깊이(M2) → 빠른편집 UX(M3) |

#### M1 — 시각·UX 토대 (먼저) — **완료 ✓ (2026-07-22)**
목표: 처음 봐도 빠르게 쓰는 화면. 값이 보이고, 어둡고, 군더더기 없다.

> **완료 (커밋 `80f22fa`~`e8f3d45`, 실조건검증 ✓):** 아래 7항목 전부 구현. 추가로 사용자 GUI
> 피드백 반영 — 가로 dock 시 버튼 한 줄로 눕힘, 패널 콤팩트 기본폭(도형 144·속성 170px) 진짜 최소
> 클램프(슬랙 0), 버튼 고정크기 좌측뭉침(안 벌어짐). 상단바는 QToolBar 승격(오버플로우 ≫). 아이콘/UI
> 방향은 icon_proposal 아티팩트. **드래그 중 드롭 미리보기 폭이 넓은 건 Qt 오버레이 한계(최종은 콤팩트).**

1. **도구 아이콘화** — 상단바 텍스트 버튼 → 아이콘(+툴팁·단축키). 코어에 `_tool_icon()` 렌더러가
   **이미 있음** → 상단바에도 연결. 단색(currentColor) 라인 아이콘 세트로 다크/라이트 자동 적응.
   - 검증: 도구가 아이콘으로 뜨고 hover 시 이름·단축키 툴팁, 활성 시 강조.
2. **다크모드** — 앱 팔레트/스타일시트 2벌(dark 기본·light). 캔버스 배경·그리드·핸들색·아이콘 대비
   **동시** 전환. 보기 메뉴 + 상단 토글. 앱 설정(QSettings)에 저장(문서 아님).
   - 검증: 토글 시 캔버스·패널·아이콘·핸들 한 번에 전환, 재실행 시 유지.
3. **메뉴 → 버튼 이관 + 상단바 재구성** — 파일(새로/열기/저장/PDF/DXF)·보기(줌/스냅/직교) 항목을
   상단 아이콘 버튼으로. 메뉴바는 유지(접근성), 상단바가 1차 조작면. 구분선으로 `파일 | 그리기 | 보기`
   섹션 시각 분리.
   - 검증: 마우스만으로 주요 파일·보기 동작 수행 가능.
4. **단축키 도움말 분리 + 창 축소 유연성** — 상단바의 긴 안내 `QLabel` 제거 → `?` 도움말 다이얼로그로.
   이 라벨이 **창 최소폭을 붙잡던 주범** → 제거로 창을 더 좁게 줄일 수 있게 됨(요청의 "축소 유연성" 동시 해결).
   - 검증: 안내 라벨 없이 도움말 접근 가능, 창을 기존보다 좁게 리사이즈 가능.
5. **도형 패널 재구성** — dock을 상/하까지 허용(`setAllowedAreas` 4방향). 가로 dock일 땐 버튼을 가로
   flow로 재배치. dock 제목표시줄에 명시적 스타일(그립 `⋮⋮`·색·라벨)로 "잡아 옮기는 바"임을 시각화
   (현재 회색이라 상단바로 인지 안 되는 문제).
   - 검증: 패널을 상/하/좌/우 어디로든 옮기고, 이동 핸들이 한눈에 인지됨.
6. **줌 % 표시** — 상태바에 실시간 배율(%) + 클릭 시 100%/전체맞춤. 뷰 transform에서 계산.
   - 검증: 팬/줌 시 % 실시간 갱신.
7. **속성(Properties) 패널 — 표시** — 선택 객체의 색·두께·선스타일·폰트를 dock에 표시. M1은
   **읽기 전용 표시**(편집은 M2, undo 개편과 함께). 다중 선택 시 공통/혼합 표시.
   - 검증: 객체 선택 시 패널에 해당 값이 정확히 뜬다.

#### M2 — 편집 인터랙션 깊이
목표: "사소한 수정도 되돌아간다", 값을 직접 바꾼다.

8. **Undo 세분화 + Redo (아키텍처, M2 최우선)** — 현재 ad-hoc 스택(`add/delete/move/xform/geom` 5종만
   기록, **redo 없음**)이 "어떤 건 되고 어떤 건 안 됨"의 원인. 모든 변이(속성변경·라벨편집·리사이즈·삭제·
   삽입)를 기록하는 단일 경로로 통일 + **Redo** 추가.
   - 방침: 소규모 커맨드(do/undo 쌍) 또는 "변경 전/후 스냅샷" 일원화 — 기존 `push_undo_*`를 흡수.
     (원래 계획 §3-C의 `QUndoStack` 미도입분을 여기서 정산)
   - 검증(실조건): 색·두께·선스타일·라벨·이동·리사이즈·삭제·삽입을 각각 undo/redo 왕복, Ctrl+Z 연타로 초기 복원.
   - ⚠ 스코프 큼 — 이게 서면 이후 속성편집이 전부 자동 undo 가능. 그래서 M2 맨 앞.
9. **속성 패널 — 편집** — 색·두께·선스타일(solid/dash/dot)·폰트크기를 패널에서 직접 변경. 전부 #8 경로로 undo.
10. **점선 지원** — 아이템에 pen style 파라미터 추가(rect·ellipse·line·arrow 전부). `.ecad`·PDF 반영,
    DXF는 linetype 매핑(후속 정밀화).
11. **객체 색 변경** — 선택 객체 색을 팔레트/피커로 변경(속성 패널·플로팅 툴바 공용 경로).
12. **텍스트 Shift+휠 크기조절** — 기존 Shift+휠(두께/크기) 로직을 텍스트 아이템까지 확장.
13. **화살표 2종 통합** — 곡선·직선 화살표를 1개 도구로(하위옵션 곡선/엘보/직결). 기존 아이템 클래스는
    유지(도구 UI만 통합), 단축키 3 유지. 일반 '선'은 별도.
14. **Ctrl+D 복제** — 선택 객체 제자리+오프셋 복제(`clone` 재사용, 연결 화살표 동반).
    - M2 검증: 위 편집이 전부 값으로 보이고·바뀌고·undo/redo 된다.

#### M3 — 빠른 편집 UX — **완료 ✓ (2026-07-22, 실조건검증)**
목표: 클릭 한 번에 바꾼다.

> **완료 (커밋 `ca71d21`~`568065d`, 실조건검증 ✓):** 아래 3항목 구현. #15 화살표는 **방향 토글만**
> (곡선↔직선 통합=#4로 이월, 데이터모델 이질). 따라다니기=selectionChanged+스크롤바+scene.changed
> (코어 무수정). ⚠ #17은 실조건서 뷰가 내부 드래그를 가로채 금지커서 → 뷰포트 `eventFilter`로 해결(`568065d`).

15. **플로팅 컨텍스트 툴바** ✓ — 선택 위 미니 툴바(색 스와치·선스타일 순환·복제·삭제·화살표 방향 토글).
    속성 dock 편집 경로(#9) 공유, 상단 침범 시 아래 반전·창 클램프. (도형 교체·곡률은 스코프 밖 — #4로 이월.)
    - 검증 ✓: 객체 클릭→툴바 표시, 색·선스타일 즉시 변경, 화살표 방향 전환.
16. **우클릭 재정의** ✓ — 상태 분기: BUSY(무장·그리기중)=취소(M2 탈출구 보존) / 유휴=드래그 임계(6px) 팬·
    제자리 탭=컨텍스트 메뉴. `_rmb_is_busy`가 M2 취소 대상과 정확히 일치.
    - 검증 ✓: 유휴 우클릭 끌면 팬, 짧게 누르면 메뉴, 그리기 중 우클릭=취소.
17. **도형 패널 드래그앤드롭** ✓ — 좌측 「도형·심볼」 버튼을 캔버스로 드래그해 놓은 자리에 생성(클릭-무장과 병행).
    - 검증 ✓: 패널→캔버스 드래그로 도형 생성(금지커서 없이, 놓은 위치).

#### M4 — 편집 정밀화 & 커넥터 고도화 — **완료 ✓ (M4-1/2/3 2026-07-22, M4-4 2026-07-24, 전부 실조건검증)**
Lucid/FigJam 캡처 + 사용자 요청 반영, deep-interview 4가지 확정(2026-07-22).
커밋 `68b9c8f`~`bdb670b` + 실조건서 버그 3건(크래시·QC 스냅·비대칭 이탈) 발견·수정.

- **M4-1 라벨 정밀화** ✓ — 화살표 라벨-선 갭 5→2px, 수직 오프셋 3위치(선 위/양쪽)로 스냅,
  선 따라 슬라이드는 자유 유지.
- **M4-2 빠른연결·스냅 확대** ✓ — 네방향점 드래그=화살표만(클릭은 도형복제 유지) +
  화살표 스냅 대상에 선·화살표(끝점+몸통) 추가(바인딩은 도형만, 선·화살표는 기하 스냅).
- **M4-3 도형 바로 바꾸기** ✓ — 플로팅 툴바 드롭다운으로 도형 종류 즉석 변환(크기·라벨·연결
  유지, 단일 undo).
- **실조건 버그 수정 3건** ✓(2026-07-22) — ⓐ 크래시: 선·화살표 스냅(shape=None) 바인딩 호출부 5곳에
  None 가드. ⓑ QC 스냅: `_qc_snap_target`(도형 내부 드롭=최근접 포트 흡수)+고스트 스냅 마커.
  ⓒ 비대칭 이탈: `_rebind_arrow`가 new 외곽선에 투영+reroute. 스모크 3종 추가(총 161).
- **M4-4 직교 커넥터 고도화 + #4 라우팅 드롭다운** ✓(커밋 `2a50103`, 실조건검증 2026-07-22) —
  `_routing`(straight/ortho/ortho_curved)+`_apply_routing()` 통합 경로 생성, 기본=곡선 엘보.
  세그먼트(변) 드래그로 중간 정점 관리(자유 정점 핸들 제거), 플로팅 툴바 `⌐▾` 드롭다운.
  ⚠ 실조건 버그: 수동 직교 커넥터가 도형 이동 시 대각화 → `reroute`가 끝-이웃 변만 리플로우하게 수정.
  - **M4-4 잔여 ⓓ·ⓑ** ✓(실조건검증 2026-07-24) — ⓓ 선택된 속 빈 도형의 **내부 빈공간 드래그 이동**
    (실제 외곽선 기준, 그리기 도구 무장 중엔 비활성 — 도형 안에서 새로 그리기 보존).
    ⓑ 플로팅 툴바 **곡선 반경 스테퍼**(0~40px, 0=직각, `.ecad`·undo 연결).
  - **라우팅 모드 통합** ✓(실조건검증 2026-07-24) — `_routing`을 straight/ortho 2값으로 축소.
    각짐/둥긂은 모서리 반경(`_curve_r`, 0=직각)이 소유(「직각」=반경0 프리셋, 「곡선」=반경>0). 같은 그림을
    두 코드로 그리던 중복 해소. 옛 `.ecad` 하위호환(옛 "ortho"=반경0, "ortho_curved"=기본반경).
  - **화살표 도구 통합** ✓(실조건검증 2026-07-24) — 상단 툴바 화살표 **버튼 1개**, 종류(직선·곡선·직각)는
    선택 후 미니툴바 `⌐▾`에서(각짐 조절은 직각만). 직선·곡선=`_ArrowItem` 상태, 직각=`_PolyArrowItem`,
    전환은 `_swap_arrow` 클래스 교체(연결·라벨·스타일 이전, 단일 undo). 종류·반경 sticky, 단축키 3만(9 해제).
    클래스 자체 통합은 **하지 않기로 확정**(UI 통합으로 목적 달성, 진짜 통합은 분기지옥·회귀 위험만 큼).
    설계 근거: deep-interview 2026-07-24. UI 원칙 = 상단에 종류 두지 않고 선택 후 컨텍스트(memory
    `ui-simplicity-principle`).
  - 남은 것: **ⓐ 경로가 도형 변을 타거나 관통**(A* 장애물 판정 재설계, 우선순위 낮음).

#### M5 — 정렬 / 분배 — **완료 ✓ (2026-07-26, 실조건검증)**
§5 #4의 미흡수분. 커넥터 계단(축 어긋남)을 **사용자 손으로** 없애는 처방이기도 하다 —
코어 Stage4(`_absorb_near_alignment`)는 8px 이내만 흡수하고 그 밖은 맞출 도구가 없었다.

- **정렬 6종**(왼쪽·가로 가운데·오른쪽·위·세로 가운데·아래) — 기준은 **선택 bbox**.
  Qt가 선택 *순서*를 보장하지 않아 "먼저 고른 객체 기준"은 쓸 수 없다(Figma·Lucid 기본과 동일).
- **균등 분배 2종**(가로·세로) — 양 끝 고정, 사이 **여백**을 균등화(중심 간격이 아니라 —
  크기가 달라도 눈에 보이는 틈이 같아야 한다). 3개 미만은 no-op.
- **진입점 2곳** — 미니툴바 `≡▾` 드롭다운 + 우클릭 「정렬 / 분배」 서브메뉴(대상 2개 이상일 때만).
  UI 원칙대로 상단 툴바가 아니라 선택 후 컨텍스트.
- **대상 규칙** — 연결된 화살표 제외(도형이 움직이면 reroute가 따라오므로, 화살표까지 옮기면
  그 이동이 곧 덮어써지고 기준 bbox만 흐트러진다) · 용지틀 제외 · **자식 아이템(라벨) 제외**.
- 이동만이라 `push_undo_move` 한 엔트리로 Ctrl+Z 복원. 검증: 스모크 8종(총 193).
- ⚠ 함정 3건: ⓐ `sceneBoundingRect()`는 핸들·빠른생성 도트 자리를 포함해 도형마다 여백이
  달라(26px vs 19.75px 실측) 정렬 기준으로 쓸 수 없다 → `_content_rect` 기반 `_align_rect`.
  ⓑ 라벨도 selectable·movable이라 러버밴드에 딸려 오는데 위치를 부모가 소유하고(재투영)
  moveBy 델타의 좌표계도 다르다. ⓒ **그룹 선택 박스는 뷰 `drawForeground`가 그려서** 프로그램
  이동 시 옛 점선이 남는다(Qt는 움직인 아이템 bbox만 무효화) → `_repaint_overlays()`를
  정렬·분배·undo·redo에 연결. 이 잔상은 오프스크린 렌더로 재현 불가(전면 재도색) — 실조건서 발견.

#### M6 — 디자인 베이크오프(색·아이콘) — **진행 중 (2026-08-02 시작)**
기능은 Phase 0~6 M1~M5로 거의 완료 — 이 단계는 시각 스타일 자체를 다시 다듬는다. 새 스킬
`design-bakeoff`(전역)로 Claude 라이브 CSS/SVG + jbnu-gateway 이미지 시안을 한 Artifact에서
비교해 좁혀가는 절차를 도입, 결정은 `~/.claude/design-system/projects/easy-cad.md`에 누적.

- **1라운드(버튼 색·상태) — 완료(커밋 `6e8dc2e`)**: 머티리얼(elevation) 스타일 + 코랄
  accent(`#da7756`, Claude 브랜드톤) 확정, hover/checked/pressed QSS 코드 반영까지 끝남.
  상세: `docs/history/2026-08.md` "UI 디자인 베이크오프 1라운드" 항목.
- **2라운드(상단바 아이콘 판별성) — 완료(코드 반영까지)**: 대표 4종(선택·화살표·텍스트·저장)
  듀오톤 스타일(A2, save만 반전한 S2) 확정 후, 나머지 14종(새로 만들기·열기·선·펜·번호·
  직각화살·핀·되돌리기·다시실행·스냅·직교·격자·테마토글·도움말)까지 같은 규칙으로 새로 그려
  `easycad/resources/icons/*.svg` 18개로 저장하고 `_tool_icon()`/`_act_icon()`을 QPainter
  코드 드로잉 → SVG 래스터화(`_svg_icon()`, QSvgRenderer)로 전환. 메뉴 전용 8종(pdf/image/
  table/titleblock/mermaid/zoom_fit/zoom_100/align)은 스코프 밖이라 기존 QPainter 코드 유지.
  **색은 최종적으로 코랄 고정이 아니라 도형 팔레트와 같은 테마 적응 중립색으로 확정**(같은
  날 4~5차 재피드백 — 코랄이 화면 전체에 흔해 "진짜 활성 상태(checked)" 신호력이 떨어진다는
  지적, hover도 코랄→테마별 중립 회색/검정 전환, checked는 순간 활성(그리기 도구·핀·직교,
  강함)/상시 ON 가능(스냅·격자, 여림) 2단계로 분리). ⚠ 재칠 구현 중 `QPainter` 합성모드
  (`CompositionMode_SourceIn`)가 스모크 테스트에서 재현 가능한 네이티브 세그폴트를 일으켜
  픽셀 루프 방식으로 교체·해결(`docs/pitfalls.md` "렌더링" 항목). 스모크 345종 통과.
  상세: `~/.claude/design-system/projects/easy-cad.md`.

#### 후속 (Phase 6 이후 / 낮은 우선)
개별 열폭 드래그.

~~8포트(꼭짓점 추가)~~ — **완료**(2026-07-29, 실조건검증 ✓). 상세는 §8 "다음 순서" 항목 3 참조.

~~심볼 종류 추가~~ — **완료**(2026-07-29, 커밋 `11f1f22`, **실조건검증 ✓**). 표준 4종
(수동입력·수동작업·화면출력·지연) + 방송/CCTV 결선도 픽토그램 4종(카메라·증폭기·랙·안테나),
총 14종. `_SYMBOL_KINDS` 딕셔너리 확장만으로 팔레트·`.ecad`·DXF export·도형교체 메뉴에
자동 반영. 상세: CLAUDE.md 신규기능 항목.

~~그리드/스냅투그리드~~ — **완료**(2026-07-28, 커밋 `16c7551`, **실조건검증 ✓**). 씬 단위
고정 20유닛 점 격자(표시+스냅 통합 `Shift+G`, 기본 켜짐) + 이동·리사이즈·생성 항상 양자화. 상세:
CLAUDE.md 신규기능 항목.

~~미니맵~~ — **완료**(2026-07-28, 커밋 `bd9637d`, **실조건검증 ✓**). 속성 dock 아래 독립 dock,
메인과 scene 공유하는 축소 뷰 + 클릭/드래그로 메인 뷰 이동. 실조건서 인디케이터 좌표 이중변환
버그 발견·수정(`48b9160`) + 시인성 UX 수정(`983dd94`). 상세: CLAUDE.md 신규기능 항목.

~~스타일 복사(format painter)~~ — **완료**(2026-07-28, 커밋 `7b135bf`, **실조건검증 ✓**).
우클릭 메뉴 + `Ctrl+Alt+C`/`Ctrl+Alt+V` — 색·두께·선스타일·폰트크기·텍스트색·텍스트배경·화살표방향을
타입 불문(도형↔화살표) 항상 복사, 텍스트 내용은 제외. 상세: CLAUDE.md 신규기능 항목.

~~클립보드 이미지 붙여넣기~~ — **완료**(2026-07-28, `7baf69b`, 실조건검증 ✓). `_to_png_full`/
`_pixmap_from_data`(코드정리 3순위 스캔에서 발견한 clipboard_monitor 이식 로직) 활용.
실조건서 발견된 플로팅 툴바 파생 버그는 `a1146ab`로 수정. 상세: CLAUDE.md 신규기능 항목.

~~케이블/넷 번호 자동채번~~ — **완료**(2026-07-28, 커밋 `7e3e9fb`, **실조건검증 ✓**). 선택한
화살표에만 실행하는 우클릭 명령(전체 자동 적용 아님) — 위치순(좌상단→우하단) 자동 정렬로 접두사·
시작번호 다이얼로그 입력값을 매긴다. 기존 화살표 라벨 재사용(`.ecad` 스키마 변경 없음). 상세:
CLAUDE.md 신규기능 항목.

~~레이어 패널 UI~~ — **완료**(2026-07-28, 커밋 `e1661a0`, **실조건검증 ✓**). 사용자 정의 이름
레이어(AutoCAD식) — 좌측 dock에서 표시/잠금 토글, 우클릭 "레이어로 이동". 새 도형 자동 배정은
스코프 밖(명시적 이동만). `.ecad`에 레이어 목록+아이템 소속 직렬화(하위호환). 상세: CLAUDE.md
신규기능 항목.

## 5. 제안 추가 기능 (사용자 요청으로 검토 — 이 도메인에 특히 잘 맞는 것)

참고 도면(방송 결선도·CCTV 망·랙 배치)을 분석해 **실제 사용처에 killer가 될 기능**:

1. **심볼/스텐실 라이브러리** ★ — 참고 도면은 증폭기·카메라·랙·안테나 같은 블록을 반복 사용. Visio 스텐실처럼 재사용 심볼 팔레트를 두면 작도 속도가 급상승. (이 도메인 최고 가치)
2. **직교 커넥터 + 자동 라우팅 + 점프오버** ★ — AutoCAD 결선도는 대부분 직교선. 베지어(순서도)와 **직교선(결선도)** 둘 다 지원해야 함. 선 겹칠 때 점프오버.
3. **포트/커넥션 포인트** — 도형에 접속점을 정의(참고 도면의 포트 마커처럼). 스냅이 포트에 착 붙음.
4. ~~**정렬/분배**(align/distribute)~~ — 깔끔한 도면의 핵심, lucid에 있음. **완료**(Phase 6 M5).
5. ~~**그리드 + 눈금**~~ — **완료**(2026-07-28, 커밋 `16c7551`). ~~**미니맵**~~ — **완료**
   (2026-07-28, 커밋 `bd9637d`). ~~**스타일 복사**(format painter)~~ — **완료**(2026-07-28,
   커밋 `7b135bf`).
6. ~~(advanced) **케이블/넷 번호 자동채번**~~ — **완료**(2026-07-28, 커밋 `7e3e9fb`).

→ 우선순위: **1·2·3을 Phase 2~3에 흡수** 권장(결선도 실사용에 직결). 4·5·6 전부 완료.

## 6. Mermaid 판단 (사용자가 판단 위임)

- **Import = 채택(Phase 4). — 완료(실조건검증 2026-07-21 ✓).** 순서도 텍스트→도형 자동배치는 "빠르게"에 직결, 마크다운 생태계 공유에도 유용. 구현: `fileio/mermaid_import.py`(파서+자체 BFS 계층 배치, 외부 의존성 0), 붙여넣기 다이얼로그 `Ctrl+Shift+G`. 노드 8모양→우리 도형 매핑, 엣지→직교 자동라우팅 화살표(지속연결). deep-interview 2026-07-21로 핵심 부분집합 스코프 확정.
- **Export = 보류.** 베지어·좌표·직교라우팅이 Mermaid로 손실 매핑됨. CAD 공유 다리는 DXF가 담당하므로 Mermaid export의 한계효용 낮음.

## 7. 열린 질문 (계획 확정 전 정리 필요)

- [x] **AutoCAD 클립보드 독자 포맷** — **종결(2026-07-29)**. DXF INSERT/BLOCK 흡수 작업 중
  재조사: AutoCAD 클립보드는 AutoCAD 전용 폐쇄 포맷 + WMF(그림, 정밀도 소실)만 올리고, 파싱
  가능한 우회로(%temp% 임시 `.dwg` 추적 → ODA File Converter 변환)는 존재하나 버전마다
  임시파일 경로가 다르고 별도 외부 프로그램 설치가 필요해 복잡도 대비 이득이 낮다고 판단.
  **최종 결정: 클립보드 직행은 하지 않는다 — DXF 파일 경유(다른 이름으로 저장 → 가져오기)를
  공식 경로로 유지.** 아래 §8 우선순위 목록의 "DWG 자동변환"이 그나마 남은 절충안.
- [ ] pasteflow 편집기의 실제 모델 구조 완독 → Phase 3 문서모델 리팩터 규모 확정. **낮은 우선**
  (지금까지 재사용 방침으로 문제없이 진행돼 옴 — 실제로 큰 리팩터가 필요해질 때 참고).
- [x] **AI(Phase 5) 이미지→도면의 품질 기대치** — **종결**. Phase 5가 "초안 후 수동 보정"
  기대치로 이미 완료·실조건검증(2026-07-21 ✓)까지 끝났으므로 사실상 답변됨.
- [ ] 다중 페이지 도면 지원 여부 — §8 우선순위 목록의 "다중 도면 지원"과 함께 큰 설계 결정으로
  이관(단일 세션에서 못 끝냄, 시작 시 deep-interview로 재논의).

## 8. 실사용 피드백 기반 로드맵 (deep-interview 2026-07-29)

Phase 0~6 로드맵과 §5 제안기능이 전부 완료된 뒤, 실사용자 DXF 검증(§3 INSERT/BLOCK 흡수 항목)
과정에서 나온 사용자 피드백 9건 + 위 §7 잔여 열린 질문을 통합해 실행 순서를 정한 목록.

**정한 것(deep-interview로 확정):** 이번 라운드는 **순서만** 정한다 — 각 항목의 세부 설계(구현
방식·UI·스코프 경계)는 그 항목을 실제로 시작하는 시점에 다시 deep-interview로 정한다(이 프로젝트가
지금까지 매 기능을 그렇게 진행해 옴). 순서 기준은 **작은 것부터 빠르게** — 크고 아키텍처급인
항목(다중 도면·DWG 자동변환)은 뒤로 미뤄 체감되는 개선을 먼저 쌓는다.

### 다음 순서 (작은 것 → 큰 것)

~~1. **미니맵 시인성 개선**~~ — **완료**(2026-07-28, 실조건검증 ✓). 반투명 파란 채움 제거
   (테두리만) + 테마·accent와 무관한 고정 시안(`#22d3ee`)으로 교체. 상세: CLAUDE.md "미니맵
   완료" 항목의 UX 후속 참조.
~~2. **DXF/.ecad 가져오기·내보내기 통합**~~ — **완료**(2026-07-29, 실조건검증 ✓ 2026-08-01).
   deep-interview로 확정: 옛 「DXF 내보내기/가져오기」 전용 메뉴·
   단축키(`Ctrl+Shift+D/I`) 완전 폐지, 열기(`Ctrl+O`)/저장(`Ctrl+S`) 하나가 파일 다이얼로그에서
   고른 확장자로 분기(`_do_open_ecad`/`_do_open_dxf`, `_do_save_ecad`/`_do_export_dxf`). 저장
   다이얼로그 기본 필터는 DXF를 직전에 열었어도 항상 `.ecad`(무손실) 우선 — DXF 가져오기/
   내보내기는 `_doc_path`를 갱신하지 않아 자연히 다음 저장이 `.ecad`로 기본값. DXF로 저장할
   때·DXF를 열 때 각각 **앱 생애 처음 1회만** 안내창(QSettings 플래그) — 저장은 손실 항목
   경고(바인딩·라벨·심볼 kind·레이어), 열기는 "현재 도면을 통째로 교체함 + 외부 도형 근사
   변환 가능"을 고지(사용자 요청으로 열기 쪽도 추가). "DXF를 기존 도면에 추가 삽입"하는
   기능은 스코프 밖(열기는 항상 교체, 기존 동작 불변). ⚠ 개발 중 발견: 새 회귀 테스트가
   `_do_export_dxf`의 성공 경로(`QMessageBox.information` "저장 완료")를 모킹 없이 직접
   호출해 헤드리스(offscreen)에서 영원히 블로킹 — 전체 스모크 스위트가 멈춘 원인이었다.
   모든 QMessageBox 경로를 모킹하도록 테스트 수정 후 292종(신규 5종 포함) 전체 통과.
   **실조건검증 ✓**(2026-08-01) — 실제 창(오프스크린 아님)에서 저장 손실 경고·저장 완료·
   열기 안내 3개 안내창을 실제로 띄워 한글 문구 정상 렌더 확인 + 사각형·화살표·한글텍스트를
   `.dxf` 왕복해 정확히 복원됨을 스크린샷으로 확인. 상세: CLAUDE.md 해당 항목 참조.
~~3. **8포트(꼭짓점 접속점 추가)**~~ — **완료**(2026-07-29, 실조건검증 ✓, 스모크 301종). 예상보다
   범위가 커졌다 — 실조건 테스트 중 사용자가 라우팅 방향·안정성 버그 5건을 연쇄로 발견해 같은
   세션에서 함께 해결: 선택 도구 8포트 hover→커넥터 드래그 신설, 마름모 N/E/S/W·사각형 대각
   꼭짓점 법선 축정렬(자동 라우팅 경로까지 일관되도록 `_nearest_border`로 보정 위치 이동),
   끝점 드래그를 "새로 그리기"와 동일하게 재설계(스텁만 patch하던 방식을 사용자 재검토로 뒤집음
   — 스턱루프 3차 시도 끝에 전환), 그 과정에서 발견한 크래시(끝점 재부착 중 프로그램 종료) 수정.
   상세: CLAUDE.md "신규기능 — 8포트 확장 + 커넥터 라우팅 방향·안정성 대수술" 항목.
~~4. **성능 조사 스파이크 2건**~~(고정 시간 조사, 수정 약속 아님) — **완료**(2026-07-30,
   커밋 `dddafb2`, 스모크 308종). 합성 도면(~1600아이템) cProfile로 실측: 선택 점선
   테두리는 무혐의 재확인(추가비용 21ms뿐). 진짜 원인은 ⓐ 미니맵 `itemsBoundingRect()`
   매 페인트 재계산(71~139ms, `scene.changed` 캐싱으로 해결) ⓑ `boundingRect()` 체인의
   `_view_zoom_factor()`가 매번 `scene.views()` 순회(줌 20틱에 28만 회 호출, 뷰 참조
   캐싱으로 해결) — 리얼리스틱 벤치마크 173ms/tick→133ms/tick. 그리드(점 격자, +37ms)는
   범위 밖으로 분리. 상세: CLAUDE.md "성능 조사 스파이크 완료" 항목.
~~5. **외부에서 가져온 도형에 스냅 활성화**~~ — **완료**(2026-07-30, 커밋 `9075f8b`, 스모크
   307종). 원인대로 `_PathItem`을 연속 폴백(Pass 2, `_conn_paths()`)에 추가 — `_nearest_border`가
   `item.rect()`를 전제해 못 쓰던 것을 `_path_nearest` 헬퍼로 확장. 예상대로 이산 포트는
   제외 유지. 상세: CLAUDE.md "외부 DXF 폴백 도형에 화살표 스냅+지속연결 활성화" 항목.
~~6. **표 개별 열폭 드래그**~~ — **완료**(2026-07-31, 프록시검증 — 격리 테스트 11종 통과 +
   자체 스크린샷, 실조건 대기). 상세: CLAUDE.md "표 개별 열폭 드래그" 항목 참조.
~~7. **도형 채우기(fill) 개념 도입**~~ — **완료**(2026-07-31, 프록시검증 — 스모크 323종
   전체 통과, 실조건 대기, 커밋 `d03fe5e`). deep-interview로 DXF는 skip(HATCH 미구현, 앱·
   PDF만 반영) 확정 후 코드 조사 중 발견: `.ecad` 직렬화·클릭 판정·clone은 pasteflow 유산으로
   이미 브러시 기반이라 완성돼 있었음(무손실 왕복) — 실제로 빠진 건 UI뿐이었다. 상세:
   CLAUDE.md "도형 채우기(fill)" 항목 참조.
~~8. **가져온 심볼을 도형 팔레트에 등록(재사용)**~~ — **완료**(2026-08-03, 실조건검증 ✓ —
   스모크 352종 전체 통과 + 실제 창에서 등록/배치 직접 확인). deep-interview로 확정:
   저장 위치=앱 전역 라이브러리(AppData JSON, 문서 무관) · 등록 대상=현재 선택 전부
   (1개 이상, 가져온 심볼 한정 아님) · 썸네일=실제 렌더 캡처 · 관리=등록+삭제(이름변경은
   스코프 밖). 상세: `docs/history/2026-08.md` "커스텀 심볼 팔레트" 항목.
~~9. **화살표 자신의 `boundingRect`/`_content_rect`/`_head_points` 체인 최적화**~~ —
   **완료**(2026-08-01, 커밋 `9e218e9`). cProfile 재실측으로 원인을 뷰줌 재조회 중복으로
   확정·수정: `_view_zoom_factor` 호출 175,710→56,310회(-68%), boundingRect() 누적시간
   -31%. 상세: CLAUDE.md "화살표 boundingRect/_content_rect 체인 최적화" 항목.
10. **웹 트랙 병행개발(포트/커넥터 프로토타입에서 출발)**(2026-08-05 시작). Python(PyQt) 앱과
    **둘 다 끝까지 완성**시키는 게 목표 — 웹 쪽이 늦게 시작해 진도가 뒤처져 있어 당분간 웹
    개발에 자원을 집중하는 것뿐, "웹 전환 여부를 저울질하는 실험"이 아니다(2026-08-05 사용자
    확인). 시작 배경: PyQt 개발이 agent(Claude) 피드백 루프를 얻기 어렵다는 문제의식(마우스
    이벤트 자동 재현 불가, offscreen 스크린샷 왕복 의존) 대 웹은 playwright로 실제 드래그·DOM
    상태를 자동검증할 수 있다는 가설. `web_prototype/`(SVG+DOM, 이 리포 하위, 별도 리포 아님)에
    포트 호버·클릭=복제/드래그=화살표·A* 직교 라우팅·지속연결(도형이동 시 화살표 재라우팅)·
    다중선택(고무줄밴드)+그룹드래그·Delete/Escape를 재현. 자동검증만으로 실버그 2건 발견+수정
    (그리드 반올림 대각선 꺾임, A* 폴백 대각선 관통) + 성능 문제 1건 발견+수정(전체 화살표
    재계산이 20도형 규모에서 60fps 예산 초과 → 연결+근접만 재계산·드롭 시 전체 1회로 10배
    개선). 사용자가 실제 브라우저로 손맛 확인 완료("나쁘지 않다") → 병행개발 결정으로 이어짐.
    캔버스/인터랙션은 웹에서 새로 만들되, DXF/PDF·문서모델은 기존 Python/`ezdxf`를 로컬
    백엔드로 재사용하는 하이브리드 아키텍처가 현재 추천안(중복 구현 방지 목적, "완성" 여부와는
    별개 판단). 이후 저장/불러오기(파일 다운로드/업로드, JSON) 완료(2026-08-06) — 도형·화살표
    (포트 참조) 직렬화/역직렬화, id 시퀀스 재설정, 깨진 파일 업로드 시 기존 문서 보존. 이어서
    undo/redo 완료(2026-08-06, 같은 날) — Python `host_undo.py`와 다른 데이터모델(shapes Map +
    화살표 DOM 엘리먼트)에 맞게 엔트리 4종(move/createShapes/deleteShapes/createArrow)으로 새로
    설계, 삭제 시 연결된 화살표까지 cascade 복원, 툴바 버튼+`Ctrl+Z`/`Ctrl+Shift+Z` 단축키.
    이어서 팬/줌(무한캔버스) 완료(2026-08-06, 사용자 위임으로 자체판단 진행) — 고정
    800×500 뷰포트뿐이던 상태에서 SVG `viewBox` 직접 조작 방식으로 휠=줌(커서고정)/휠버튼
    드래그=팬 구현(Python `core_view.py` 관례 그대로), 히트반경 줌연동화, A* 라우팅 탐색범위가
    캔버스 크기로 하드코딩돼 있던 버그(원점에서 먼 도형끼리 연결 시 탐색실패→폴백)를 도형
    bounding box 기반 동적 범위로 함께 수정. 이어서 도형 라벨(텍스트) 완료(같은 날) — 더블클릭
    시 도형 위에 겹치는 플로팅 `<input>`으로 인라인 편집(Enter 커밋/Escape 취소/blur 커밋),
    undo/redo·복제·저장불러오기 전부 라벨 관통. 이어서 그리드/스냅투그리드 완료(같은 날) —
    SVG `<pattern>` 점격자 표시+`G`키/버튼 토글, 단일 도형 드래그·신규생성 좌표에 20단위 스냅
    (Python `core_view.py`처럼 다중선택 그룹드래그는 제외). 이어서 도형 리사이즈+포트 4개로
    축소 완료(같은 날, 사용자 스크린샷 정정 반영) — 8포트(웹 독자 설계) → 4포트(N/E/S/W,
    Python이 2026-07-30에 이미 축소한 것과 일치)로 축소, 포트 원을 테두리에서 8px 띄워
    표시(연결 앵커는 여전히 테두리 정확한 점), 단일 선택 시에만 나타나는 변(단축)+모서리(자유)
    리사이즈 핸들 추가(그리드 스냅·최소크기 40×30 클램프 포함), 기존 8포트 문서 로드 시
    크래시 대신 해당 화살표만 스킵하는 가드 추가. 이어서 도형 종류(타원·마름모) 완료(같은 날) —
    히트/이동/리사이즈 앵커는 계속 기존 `rect.body`(bbox)로 유지하고 시각만 `<ellipse>`/
    `<polygon>`으로 그 위에 얹는 surgical 설계(기존 이동·리사이즈·포트·라우팅 코드 무변경),
    툴바 라디오 버튼 3개로 다음 생성 종류 선택, `kind` 필드를 undo/복제/저장불러오기 전체에
    관통(옛 문서는 `kind` 없으면 사각형 기본값). 이어서 도형 채움색 완료(같은 날) — Python
    `_COLOR_PRESETS` 6색 스와치+리셋 버튼, 선택 도형(다중 허용)의 채움에 적용(선 색이 아닌
    채움색으로 설계를 가른 이유: 웹의 선택 강조가 테두리색 교체 방식이라 선 색과 충돌하기
    때문). 이어서 화살표 개별 선택+삭제 완료(같은 날) — 화살표를 `<path>` 단일 엘리먼트에서
    `<g>`(보이는 선+겹치는 넓은 투명 히트영역)로 구조 변경, 고유 id 부여로 포트쌍이 같은
    화살표가 여럿이어도 정확히 하나만 선택·삭제, 도형선택과 상호배타 설계, `deleteArrow`
    undo 엔트리. Python `_ArrowItem`이 `ItemIsSelectable`로 이미 되던 기본 조작(화살표만
    골라 지우기)이 웹엔 아예 없던 격차를 메움. 이어서 화살표 라벨(더블클릭 인라인 편집)
    완료(같은 날) — 도형 라벨과 동일 UX를 `kind` 파라미터로 일반화해 재사용, 라벨 위치는
    경로 호길이 중점(드래그 재배치는 Python에 있으나 범위 밖), `label` 필드를 직렬화·
    cascade복원·개별삭제복원 전부에 관통. 이어서 커스텀 색 다이얼로그("다른 색…") 완료(같은
    날) — Python `QColorDialog`를 HTML 네이티브 `<input type="color">`로 대응, 드래그 중
    `input`은 undo 없는 실시간 프리뷰만·피커 닫힘(`change`)에 1회 커밋해 스와치 클릭과 같은
    undo 단위 유지. 이어서 Ctrl+A 전체선택 + 키보드 줌(+/-/0) 완료(같은 날) — 기존
    다중선택·휠줌 인프라를 그대로 재사용하는 단축키 추가뿐이라 설계 결정 없이 착수. 이어서
    방향키 도형 미세이동(nudge) 완료(같은 날) — 기본 10px, Shift/Ctrl 시 1px, 기존 `move`
    undo 구조 재사용(단 Python의 연속입력 병합은 이 프로토타입 undo 설계와 안 맞아 의도적
    으로 생략). 이어서 Shift+H/V 도형 반전(mirror) 완료(같은 날) — 공통 bbox 중심 기준 좌우/
    상하 반사를 `move`와 같은 undo 엔트리로 표현(우리 도형이 전부 자기bbox 대칭이라 위치만
    옮기면 충분, Python의 좌표변환 리베이크 구조는 불필요). 이어서 Shift+드래그 이동 축
    고정 완료(같은 날) — 착수 전 Explore 조사의 "리사이즈 비율고정" 추정이 오독이었음을
    실제 코드로 재확인(진짜는 도형 이동 중 수평/수직 축 고정), 구현 중 그리드스냅이 고정축을
    되돌리는 상호작용 버그를 자체발견+수정. 다음 단계는 미정.
    상세: `docs/history/2026-08.md` "웹 전환 실험" 항목들.

### 큰 설계 필요 — 시작 시 deep-interview 필수

9. **DWG 자동변환 열기** — 위 §7 클립보드 항목에서 이미 언급된 ODA File Converter(외부 무료
   프로그램) 의존 경로. 사용자 PC에 별도 설치가 필요한 점을 수용할지부터 재확인 필요.
10. **다중 도면 지원**(탭 또는 새 창) — `CanvasWindow`가 단일 씬을 전제하는 구조라 탭이든
    다중 프로세스든 아키텍처에 영향. 탭 방식(단일 프로세스, 클립보드·설정 공유 쉬움) vs 새
    프로세스(구현 단순, 프로세스 간 공유 없음) 트레이드오프를 시작 시 논의.
11. **다중 페이지 도면 지원** — §7에서 이관. 현재 표제란/용지틀(`_TitleBlockItem`)은 단일
    프레임 전제라, 다중 페이지는 그 설계를 다시 봐야 함. 10번과 개념적으로 인접(여러 "문서
    단위"를 다루는 문제)해 함께 논의하는 게 나을 수 있음.
    - **사용자 아이디어(2026-08-03)**: 실제 캐드 관례 — 캔버스 위에 용지틀(표제란)을 여러 개
      놓고 각 용지틀에 블록명을 지정, PDF 내보내기 시 "이 블록명만" 지정하면 그 용지틀 안쪽
      도면만 골라 일괄 출력. 지금 `_find_title_frame`(fileio/pdf_export.py)은 씬에서 첫
      `_TitleBlockItem` 하나만 찾는 전제라 이 기능과 정면으로 맞물림 — 이 항목 착수 시
      같이 설계.

### 실제 도면 기반 신규 안건 (2026-08-03 deep-interview, 사용자가 `실제 도면/` KBS 송신소 계통도
사진 제시 후 4건 요청 — 순서대로 진행, 한 항목씩 착수 시 그 아래에 세부 스코프를 채움)

~~12. **포트-테두리 trim 워크플로우**~~ — **완료**(2026-08-03, 프록시검증 — 스모크 365종
    전체 통과 + 실제 창에서 삼각형·포트 부착/trim/커넥터 직접 확인·DXF는 ezdxf 재읽기로
    분절 확인, 실제 AutoCAD로 열어본 실조건검증은 아직). 장비(사각형·삼각형) 테두리에
    포트(사각형·원)를 드래그로 붙이면 자동 스냅 + 테두리 trim + 기존 8포트 A* 커넥터
    연동 + DXF 진짜 분절 반영까지 6단계로 구현. 제외(후속 후보): 타원 trim · 범용 CAD
    trim/extend 도구 · 회전된 포트의 정밀 trim(현재 무회전 가정).
    상세 경위·스턱루프 기록: `docs/history/2026-08.md` "포트-테두리 trim 워크플로우 §8
    항목12" 참조.
~~13. **안테나 심플화**~~ — **완료**(2026-08-04, 스모크 378종 전체 통과 + 실제 캔버스에
    8종 직접 렌더 확인). 실제 도면(IMG_7077, 모악산 안테나 타워)뿐 아니라 사용자가 추가로
    보내준 실물 사진 5장(MW 파라볼릭·CP 다이폴·CP RING·DTV·MESH 파라볼릭)까지 전부 대조해
    최종 7종(mw_side/mw_front·cp_dipole·cp_ring·dtv·mesh_filled/mesh_hollow — MW·MESH는
    구도 2종씩 모두 유지) + 안테나 전용이 아닌 범용 데칼 1종(`lightning`, Feather "zap"
    아이콘 폴리곤 이식 — 레이돔 로고 등 다른 심볼 위에 얹어 쓰는 용도)까지 8종 확정.
    artifact로 SVG 시안과 GPT(`gpt-image-2`) 생성 시안을 여러
    라운드 나란히 비교한 뒤 종류별 승자를 골라 `easycad/canvas/core_shapes.py`의
    `_SYMBOL_KINDS`에 경로 팩토리로 등록(팔레트·직렬화·DXF export 전부 기존 메커니즘
    그대로 자동 반영 — 코드 추가는 그것만으로 충분했음). MESH(채움) 중앙 급전부는 사용자의
    채움색 설정과 무관하게 항상 검게 보여야 해서 `_SymbolItem.paint()`에 한 kind 전용
    강제 채움을 추가(유일한 kind별 특례). 상세 경위: `docs/history/2026-08.md` "안테나
    심플화 §8 항목13" 참조.
14. **PDF 내보내기 메뉴 통합** — 세부 스코프 미정. 현재 `host_ui.py` 100~106줄에 "전체"(Ctrl+P)·
    "선택영역"(Ctrl+Shift+P) 2개 별도 메뉴 항목 존재. 사용자 제안: 메뉴 1개로 합치고, 선택 시
    미리보기 다이얼로그(옵션 선택→최종 이미지 확인→승인) 흐름으로 전환. 착수 시 미리보기
    다이얼로그의 정확한 옵션 구성(전체/선택 라디오, 용지크기, 방향)을 deep-interview로 확정.
15. **AI 이미지→도면 변환 리서치** — 세부 스코프 미정, 4건 중 가장 크고 불확실. 사용자가
    가진 사진(예: `실제 도면/`)을 도면에 최대한 가깝게 자동 이식한 뒤 사용자가 다듬는 워크
    플로우 검토. **손안의 카드 확인함**: 전북대 mindlogic 게이트웨이(`jbnu-gateway` 스킬,
    Paste_flow OCR/STT에서 이미 사용 중)는 텍스트→이미지 생성(gemini-2.5-flash-image,
    gpt-image-2)·비디오·TTS 스크립트는 있으나, "기존 사진을 분석해 구조화된 도형/좌표로
    추출"하는 비전 분석 스크립트는 없음(`chat` 카테고리로 41종 LLM 모델 조회는 가능해도
    이미지 입력용 스크립트 미작성 — 신규 개발 필요). 사용자가 언급한 대안(외부 웹 AI에
    프롬프트를 주고 결과값을 받는 방식)과 게이트웨이 확장 중 방향을 착수 시 deep-interview로
    확정.
16. **Lucid 대조 캔버스 UX 개선 시리즈** — 2026-08-03 세션에서 착수, 다른 PC에서 이어서 진행
    예정(로컬 memory는 PC별이라 여기 기록). 완료: 미선택 도형 호버 잔상/불규칙 수정·내부
    클릭으로 선택·테두리 전체 리사이즈 커서·qc-dot/모서리 핸들 테두리 밖 오프셋(선택·미선택
    동일 규칙)·화살표 선택 표시(단일=얇은 중심선, 다중=기존 바깥 밴드, 화살표는 항상 중심선)·
    직각 커넥터 세그먼트 알약 분할-드래그(고정 알약 끌면 전체 이동, 알약 아닌 위치 끌면 그
    자리에 새 정점 삽입 후 가까운 쪽만 이동 — rf 계정 Lucid 문서에서 직접 재현·확인)·
    화살촉 어깨 모따기 버그(아래).
    ~~미선택 도형 테두리 연속 호버~~ — **완료**(2026-08-04, 스모크 377종 전체 통과 + 실조건검증
    ✓ — 실제 창에서 직접 실행해 연속 지점 판정·커서 분기 수치 확인 + OS 커서를 테두리 임의
    위치로 이동시켜 스크린샷으로 강조점 위치 확인). 화살표 그리기 스냅의 기존 2단 패턴(이산
    포트 우선 → 테두리 연속 폴백)을 select-hover에도 적용해 통일, DXF `_PathItem`도 포함.
    상세 경위(안쪽/바깥쪽 판정에 `contains()`를 못 쓰는 이유 포함): `docs/history/2026-08.md`
    "미선택 도형 테두리 연속 호버 완료" 항목.
    ⚠ **화살촉 어깨 모따기(해결, 재현에 3라운드 걸림)** — "직각 화살표만 머리 양쪽이 깎인다"는
    보고를 세 번 받고도 재현에 실패했다. 원인은 `QPen`의 **기본 joinStyle이 `BevelJoin`**이라
    `_PolyArrowItem.paint()`가 화살촉 펜에 joinStyle을 명시하지 않아 30° 예각 어깨가 잘린 것
    (`_ArrowItem`은 처음부터 `RoundJoin` 명시 → 곡선·직선은 멀쩡, 그래서 "직각만"으로 보였다).
    재현 실패의 진짜 원인은 **줌 배율** — 화살촉 펜 폭이 1로 고정이라 깎임도 ~0.5 씬단위
    고정이고, 100% 줌에선 안티에일리어싱에 묻힌다(사용자 실측 배율은 2863%). 회귀는
    `test_arrowhead_shoulders_not_beveled`가 실제 렌더 픽셀로 막는다. 상세: 함정 위키
    `easycad-qpen-기본joinstyle-예각폴리곤-모따기.md`.
    ~~포트 상호작용 재설계~~ — **완료**(2026-08-04, 스모크 381종 전체 통과). 포트(호스트
    테두리에 부착된 작은 도형)를 호버·선택하면 장비 전체처럼 반응해 화살표+새 도형 생성
    고스트가 뜨던 버그를 4라운드 끝에 해결 — 1~3차는 포트 전용 특례를 상호작용 함수
    7곳에 흩뿌리는 방향으로 갔다가 매번 새 구멍이 드러났고(스턱루프), 4차에서 포트를
    완전히 평범한 도형으로 되돌리는 대신 큐닷 규칙 자체를 "클릭=복제/드래그=화살표만"
    (전 도형 공통)으로 바꿔 특례 없이 요구를 만족시켰다. 부가로 드래그 프리뷰 점선→실선,
    출구축(수평/수직) 10px 이내 스냅도 추가. 상세 경위(1~3차 실패 원인 포함):
    `docs/history/2026-08.md` "포트 상호작용 재설계" 참조.
17. **범용 CAD TRIM/EXTEND** — 2026-08-04, 위 포트 재설계 논의 중 사용자가 제안. 임의
    두 도형/선의 교차점에서 자르거나(TRIM) 늘리는(EXTEND) 진짜 CAD 기능 — 지금 있는
    포트-테두리 trim(항목12, `build_trimmed_border_path`)은 포트 전용 특수 케이스라
    범용이 아님. 필요 조각: ① 일반 도형-도형 교차 계산(원·심볼 포함 — 지금은 A*
    라우팅용 선분-사각형·선분-선분만 있음) ② "자를 기준 선택 → 자를 세그먼트 클릭"
    2단계 상호작용 도구(기존 그리기/편집 도구와 성격이 다름). 장기 과제로 보류 — 반복
    요구가 생기면 착수 시 deep-interview로 스코프 확정.

## 9. 검증 철학

각 Phase는 "작동"이 아니라 **검증 가능한 목표**로 닫는다. 특히 Phase 3(DXF)·Phase 1(PDF)은 실제 AutoCAD/인쇄물로 확인하는 **실조건검증**이 완료 기준 — 헤드리스·합성지표로 "됐다" 하지 않는다.
