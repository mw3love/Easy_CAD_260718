# Easy CAD — 함정 모음

`CLAUDE.md`의 진행 기록(`docs/history/2026-07.md`·`2026-08.md`)에서 ⚠ 표시된 항목만 골라
"재발 방지"용으로 짧게 정리했다. 각 항목은 한 줄 교훈 + 상세가 있는 기록 파일 포인터.
새 버그를 만나면 여기부터 훑고, 자세한 진단 과정이 필요하면 포인터를 따라간다.

## Qt 시그널·이벤트 발화 조건
- `itemChange(ItemPositionChange)`는 `ItemSendsGeometryChanges` 플래그 없으면 아예 발화하지
  않는다 — `_ConnectorLabel` 자유이동 구현 중 발견. (`2026-07.md` FigJam 라벨 갭)
- `scene.changed`는 콘텐츠 변경에만 뜬다 — 휠줌·팬 같은 순수 뷰 트랜스폼은 안 탄다. 미니맵·
  화살표 reroute를 이 시그널에만 걸면 줌/팬 후 갱신이 안 됨. (`2026-07.md` 미니맵,
  `2026-08.md` boundingRect 최적화)
- 합성 `QMouseEvent`(press+move)는 이 앱 창에서 씬(`mouseGrabberItem`)에 델타로 반영되지
  않는다 — 오프스크린/자동화로 드래그를 재현할 수 없다는 뜻. 결과 상태가 필요하면 API 호출로
  대체(예: `setPos`/`apply_*` 직접 호출). pyautogui 같은 OS 레벨 자동화도 이 창에서는 클릭·선택
  자체가 안 먹힘(원인 미확정). (`2026-07.md` M4-4, `2026-08.md` 스마트 정렬 가이드)
- `QTabWidget`은 탭 전환과 무관하게 내부 `QStackedLayout.sizeHint()`가 **가장 큰 탭 크기로
  고정**된다 — 콘텐츠 크기로 패널이 줄어들게 하려면 탭 대신 `setVisible()` 토글. 숨김→재표시
  직후 `adjustSize()`는 레이아웃 무효화가 아직 반영 전이라 stale — `layout().activate()`를 먼저.
  (`2026-07.md` 캔버스-퍼스트 레이아웃)
- Qt 레이아웃은 부모 체인을 따라 **지연 전파**된다 — 자식 하나만 `activate()+adjustSize()` 해도
  조상(중첩 3단 이상)의 캐시된 sizeHint가 그 시그널 핸들러 안에서 즉시 안 바뀔 수 있다. 조상까지
  전부 activate 후 adjustSize. (`2026-08.md` 속성 dock 크기 버그 2건)
  ⚠ [2026-08-19 재발, 더 정확한 근본원인] 이 조언대로 조상 레이아웃마다 `invalidate()+
  activate()`를 명시적으로 걸어둔 코드(`_relayout_left_panel()`)조차도 **위젯 경계를 새로
  하나 더 얹으면**(아코디언 안에 폴더별 접기를 중첩) 다시 막힌다 — `activate()`는 그 레이아웃
  "자신의" 캐시만 새로 읽을 뿐, 그 안에 든 **위젯 아이템**(`QWidgetItem`)이 내부적으로
  들고 있는 sizeHint 캐시까지 자동으로 새로 읽어오진 않는다. 위젯 X의 내용이 X 자신의
  `setVisible()`이 아니라 **X 안의 자식 레이아웃 변화로 인해 바뀌면**, X는 자기 `sizeHint()`가
  달라졌다는 걸 부모 레이아웃에 스스로 통지(`X.updateGeometry()`)해야 한다 — 안 하면 X의
  `sizeHint()`를 직접 물으면 정답이 나오는데도 부모 레이아웃의 계산엔 안 반영된다(실측:
  `panel.sizeHint()`는 맞는데 `panel.size()`는 그대로인 상태가 그대로 재현됨). 중첩 3단
  (그리드→폴더존→"내 심볼" 섹션)이면 `updateGeometry()`도 **경계마다 하나씩, 총 3번** 필요
  했다 — 하나라도 빠지면 그 지점에서 딱 멈춘다. `docs/history/2026-08.md` "내 심볼 폴더별
  접기 재개편" 참조.
- **`QGridLayout(widget)`처럼 생성자에 위젯을 바로 넘기면 그 위젯의 "자기 레이아웃"이 되어
  Qt 기본 여백(플랫폼별 ~9px×4방향)을 그대로 물려받는다** — `QGridLayout()`을 인자 없이
  만들어 `addLayout()`으로 다른 레이아웃에 중첩시키면 여백이 0인 것과 대조적. 같은 4열
  그리드 두 개가 코드는 거의 똑같은데 한쪽만 유독 넓어 보인다면 이 생성 방식 차이부터
  의심. 여기에 더해, 그 위젯을 감싸는 **중간 래퍼(zone처럼 자체 여백을 또 가진 컨테이너)가
  있으면 두 여백이 이중으로 겹쳐** 첫 번째 원인만 고쳐도(그리드 여백→0) 잔여 몇 px가 남을
  수 있다 — 실측으로 잔여가 있으면 "몇 단을 감싸고 있는지" 전부 짚어야 완전히 없어진다.
  (`2026-08.md` "'내 심볼' 재개편 후속 실사용 피드백 4건" — 폴더 그리드가 "기본도형"
  그리드보다 넓어 접었다 펼 때 패널 폭이 흔들리던 버그, 22px→4px→0px로 2단계 수정)
- `QListWidget`의 **기본 sizeHint는 항목·내용과 무관하게 고정값(256×192)**이다 — 실제 행
  위젯(`setItemWidget`)이 훨씬 좁아도(실측 180) 리스트 자체는 256을 계속 주장해, 이 리스트를
  포함한 컨테이너의 전체 폭이 계속 그 숨은 하한선에 발목 잡힌다. `setMaximumWidth()`로 캡
  가능하지만, 그 캡의 반영조차 **위젯이 실제로 `show()`되기 전까지 지연**되는 별개의 내부
  특성이라(오프스크린 재현도 동일 — "실제 창에서만"이 아니다) 생성 시점 계산은 캡을 무시한
  값으로 남는다. `.height()`를 직접 읽어도 그 시점(show 전)엔 settle 안 됨 — 해법은
  `showEvent()`에서 한 번 더 `.height()`를 읽고 레이아웃을 재계산하는 것뿐. (`2026-08.md`
  좌측 패널 폭 3차 조정 — 진짜 원인 발견)
- `setFixedSize()`로 고정한 값이 **콘텐츠(폰트·아이콘) 변경 후의 자연 sizeHint보다 작아지면**
  텍스트 하단이 잘린다 — 폰트를 키우는 등 콘텐츠 쪽만 바꾸고 고정 크기 상수를 안 맞추면
  재발하는 클래스 버그. `setFixedWidth()`만 걸고 높이는 그 폭 기준 `sizeHint().height()`를
  그대로 쓰면 폭은 고정하면서 잘림은 원천 차단된다. (`2026-08.md` 팔레트 라벨 하단 잘림 수정)
- `QToolButton` 등에 `.setStyleSheet(...)`를 조상 위젯(메인윈도우·패널 body)에 걸면, 그 스타일
  규칙이 특정 위젯을 안 가리켜도 **하위 위젯 전부**(형제로 있는 `QAbstractSpinBox` 포함)가
  `QStyleSheetStyle` 렌더 경로로 강제 전환돼 스핀박스 `sizeHint`가 짧아지고 폼 행이 겹친다 —
  2026-07-31 사고와 같은 메커니즘이지만 이번엔 최상위(`self`)에 걸어 재발했다. 스핀박스가 없는
  컨테이너(툴바 등)는 컨테이너 단위로 걸어도 되지만, 스핀박스와 형제인 버튼은 **위젯 각각에
  개별로** 걸어야 한다. (`2026-08.md` 버튼 코랄 accent 회귀)
- `QMessageBox`(또는 임의 모달)를 모킹 없이 테스트에서 직접 호출하면 헤드리스 환경엔 누를
  사용자가 없어 `.exec()`가 영원히 블로킹 — 스위트 전체가 "멈춘 게 아니라 조용히 도는" 상태로
  보인다. 모든 모달 경로는 테스트에서 모킹 필수. (`2026-07.md` DXF/.ecad 통합)
- `QGraphicsItem.prepareGeometryChange()`는 **`super()` 호출 안에서 `boundingRect()`를 동기로
  다시 부른다**(scene 공간인덱스 갱신용, 문서화 안 된 내부 동작 — 계측으로 확인). 그래서
  "버전 번호를 올린 뒤 super()"로 쓰면 *아직 옛 기하인데 이미 새 버전 번호*인 창이 열려,
  그 순간 계산된 값이 새 버전 캐시에 박히고 **이후 진짜 데이터 교체가 일어나도 영원히 무효화
  안 된다**(캐시 키가 이미 "최신"). `_geom_version` 같은 버전키 메모이즈를 쓸 땐 반드시
  **super() 먼저, 버전 증가는 그다음**. (`2026-08.md` 화살촉 한 세대 지연 캐싱 버그)
- `QGraphicsItem.prepareGeometryChange()`는 Qt C++에서 **virtual이 아니다** — Python에서
  오버라이드해도 이 코드베이스의 파이썬 메서드가 `self.prepareGeometryChange()`를 명시적으로
  호출할 때만 그 오버라이드가 걸린다. `QGraphicsRectItem.setRect()`/`QGraphicsTextItem.
  setFont()`/`setPlainText()` 같은 **Qt 네이티브 메서드가 내부적으로 부르는
  prepareGeometryChange는 이 오버라이드를 안 탄다** — `_geom_version` 버전키가 그 경로에서
  안 올라가 캐시가 조용히 stale해진다. `_PolyArrowItem`의 `_geom_version` 캐시(2026-08-08/09)가
  안전했던 건 그 클래스의 기하 변경이 전부 이 코드베이스 자체 파이썬 메서드(`_set_endpoint`
  등)를 거쳐서였다 — **네이티브 setRect()/setFont() 같은 Qt 자체 API로 기하가 바뀌는 클래스에
  같은 캐시 패턴을 그대로 옮기면 이 함정을 밟는다**. `_HandleResizeMixin`(도형 공용, `setRect()`
  보유) 확장 시도에서 실측(`_TextItem._fit_label_to_shape`의 `setFont()` 폰트축소 후에도 옛
  boundingRect가 캐시에 남아 라벨이 중앙에서 11~32유닛 어긋남, `test_sketch_build_roundtrip`
  스모크로 즉시 포착) 후 되돌림. (`2026-08.md` 다중선택 그룹드래그 성능 — 큐닷 캐시 시도·되돌림)
- **위 함정을 피하는 대안(같은 날 재시도·성공)**: `prepareGeometryChange()` 무효화 훅에
  의존하는 대신, `boundingRect()`가 어차피 매번 계산하는 저비용 값(`_content_rect()`·
  `scale`·`handle_px`)을 캐시 **키**로 직접 비교해 미스일 때만 비싼 부분(`_qc_dot_rects()`
  등)을 재계산한다 — "이벤트가 와야 무효화"가 아니라 "매번 직접 확인"이라 네이티브
  `setFont()`/`setRect()` 경로가 캐시를 우회할 수 없다(그 호출들이 만든 새 값을 다음
  호출이 그대로 다시 읽으므로). 다중선택 그룹드래그(200개) 실측 121.81ms→34.84ms(3.5배),
  스모크 543종(이전에 걸렸던 `test_sketch_build_roundtrip` 포함) 무회귀. **교훈**: 이
  코드베이스에서 native Qt 호출이 섞인 클래스는 "이벤트/시그널 기반 무효화"보다 "매 호출
  값-비교 캐시"가 구조적으로 더 안전하다 — 전자는 무효화 훅이 안 걸리는 경로가 있으면
  그대로 stale해지지만, 후자는 훅이 필요 없어 그 실패 모드 자체가 없다.
  (`docs/perf_group_drag_200.md` "구현 결과", `2026-08.md` "§8 항목0 후속 — 200개+
  그룹드래그")
- ~~`QGraphicsItem`에 Qt 자식(`setParentItem`)이 하나라도 있으면, `paint()`에서 분절된
  `QPainterPath`(moveTo/lineTo로 gap 있는)를 그려도 `QGraphicsScene.render()`/
  `QGraphicsView.grab()` 경로에서는 그 gap이 사라지고 닫힌 도형으로 보인다~~ — **2026-08-09
  재현 안 됨**. §8 항목17(TRIM/EXTEND) 계획이 이 성질에 걸려 있어 실제 `_RectItem.paint`를
  분절 렌더로 런타임 교체해 라벨(자식) 유무 × 채움 유무 × 선택 유무 8조건 × 두 렌더 경로를
  픽셀 검사한 결과 **8/8 통과**(Qt 6.10.0 / PyQt 6.10.2). 즉 지금은 자식이 있어도 진짜 분절
  렌더가 두 경로 모두에서 살아남는다 — 새 코드는 배경색 덮어그리기로 우회할 이유가 없다.
  ⚠ 단 **2026-08-03 당시의 실패를 재현하지도 못했으므로 왜 지금 되는지는 미확인**이다. 이
  성질이 되돌아가면 TRIM이 "화면은 잘렸는데 PDF는 안 잘림"으로 깨지므로 회귀 테스트
  `test_true_segmented_border_survives_scene_render_and_grab`
  (`tests/test_part4_ports_fileio.py`)이 지킨다. 기존 포트 trim의 덮어그리기 우회
  (`_paint_port_cover_if_needed`)는 아직 그대로이며 §8 항목17 7단계에서 흡수 예정.
  **2026-08-10 추가 확인**: 오프스크린 스파이크뿐 아니라 `QT_QPA_PLATFORM` 없는 실제 창
  (`w.grab()`, 플랫폼 플러그인 `windows` 확인)과 `export_pdf()`가 만든 진짜 `.pdf` 파일을
  PyMuPDF로 래스터화한 이미지 양쪽에서도 간격이 육안·픽셀 스캔으로 확인됨 — 다른 렌더 경로
  (실제 플랫폼 페인트 엔진·실제 QPrinter PDF 백엔드)에서도 재확인됐지만 원인 자체는 여전히
  미상.
  (`2026-08.md` 포트-테두리 trim, 위키
  `easycad-qgraphicsitem-자식있는아이템-paint-gap-무시.md` — 위키도 갱신 필요)
- **헤드리스 진단/벤치마크 스크립트를 함수로 감싸면 `QGraphicsScene`/`QGraphicsItem`이 함수
  반환 후 조용히 죽는다** — `CanvasWindow()`를 만드는 로컬 함수 안에서 `app = QApplication.
  instance() or QApplication([])`처럼 `QApplication`을 **지역변수로만** 잡으면, 함수가
  반환된 뒤 그 참조가 사라지는 순간부터 `scene.items()` 등 호출이 "wrapped C/C++ object of
  type QGraphicsScene has been deleted"로 죽는다(창·아이템을 리스트로 붙잡아 둬도 안 고쳐짐
  — 진짜 원인은 QApplication 쪽). 해법: `QApplication`을 모듈 전역에 한 번만 만들어 스크립트
  끝까지 참조 유지. (`docs/route_review_2026-08.md` §8 항목19 4단계, `tools/
  profile_obstacle_scan.py` 작성 중 발견)
- **`mouseReleaseEvent`처럼 조기 `return`이 많은 이벤트 핸들러에서, 함수 맨 끝에 둔 뒷정리는
  "실행되기도 하고 안 되기도 한다" — `try/finally`로 보장할 것.** 이 앱의
  `_AnnotatorView.mouseReleaseEvent`는 조기 return이 **13곳**이다(러버밴드·그룹변형·그룹본체·
  세그먼트·열폭·접속점 드래그가 각자 자기 자리에서 커밋하고 빠져나간다). 그런데 종료 처리
  (`_move_active` 해제 + 미뤄둔 재라우팅 flush + 리페인트)가 함수 맨 끝에 있어, **다중선택 이동에
  해당하는 그룹 본체 드래그가 하필 조기 return 경로**라 그 경우 종료 처리가 통째로 건너뛰어졌다.
  증상은 처음엔 사소해 보였다("드래그가 끝났을 때 어쩔 땐 파란 밴드가 다시 나타나고 어쩔 땐 안
  나타남") — 그러나 `_move_active`가 True로 남으면 `is_drag_session()`이 계속 참이라
  **미뤄둔 A\* 재라우팅이 영영 안 돌고, 그 뒤의 모든 지오메트리 변경도 계속 미뤄져 화살표 경로가
  옛 모양에 굳는다.** 보이는 것보다 훨씬 심각한 쪽이 같은 자리에 있었다.
  ⚠ **이 플래그는 2-B 이전부터도 남을 수 있었지만 그땐 스마트 정렬 가이드에만 영향이라 아무도
  몰랐다 — 나중에 그 플래그에 더 많은 것을 걸면서 잠재 결함이 실제 버그로 승격했다.** 기존
  플래그를 재사용해 새 최적화를 매달 땐, 그 플래그의 **해제 경로가 모든 return에서 보장되는지**를
  먼저 감사할 것. 해법은 13곳에 각각 넣는 게 아니라 실제 처리를 `_mouse_release_impl`로 옮기고
  핸들러를 `try/finally`로 뒷정리만 보장하는 얇은 래퍼로 만드는 것 — 앞으로 return이 늘어도
  자동으로 덮인다. (`2026-08.md` 500~1000개 부하 감소 2-B 후속, 커밋 `7670bd7`)
- **드래그 중 무언가를 숨겼다면 release에서 `viewport().update()`를 명시적으로 불러야 한다** —
  마지막 프레임 이후 씬에 변화가 없으면 Qt가 리페인트를 안 보내 **숨긴 것이 숨겨진 채로 남는다.**
  (`2026-08.md` 2-C, 커밋 `059cde2`)
- `signal.connect(self._method)`는 **연결 시점의 바운드 메서드 객체**를 붙잡는다 — 나중에
  `dlg._method = lambda: ...`로 인스턴스 속성을 덮어써도 이미 연결된 시그널은 원본 메서드를
  계속 호출한다(속성 조회가 아니라 바운드 객체 저장이라 재바인딩 안 됨). 테스트에서 메서드를
  스텁으로 갈아끼우려면 `eventFilter`처럼 테스트가 직접 호출하는 경로를 쓰거나, `patch.object`
  로 클래스/모듈 쪽을 패치해야 한다 — 인스턴스 속성 덮어쓰기는 이미 connect된 시그널엔 안 먹힘.
  (`2026-08.md` §8 항목20 B단계 — Enter키 트리거 테스트가 이 함정으로 실제 게이트웨이까지
  호출해 2분 타임아웃, 강제종료 후 API 키를 비우는 방식으로 재작성)
- **`QGraphicsItem.setVisible(False)`는 그 아이템의 「선택」도 해제한다**(마우스 그랩도 잃는다)
  — 실측 2026-08-15(성능계획 2-D): 5개를 선택해 두고 4개를 숨기니 `scene.selectedItems()`가
  1개로 줄었다. 성능을 위해 "잠깐 감췄다 되돌린다"를 하려 했다면 이게 조용한 지뢰다 — 이
  레포에서는 선택이 풀리는 순간 `_uniform_translation`(전체선택=강체 평행이동) 판정이 깨져
  **멈춰 있던 A\* 가 다시 돌기 시작해 프레임이 오히려 나빠졌다**(148ms). 감추고 싶으면
  `ItemHasNoContents` 플래그를 쓴다 — 선택·이벤트·히트테스트·`shape()`를 전부 그대로 두고
  `paint()` 호출만 건너뛰며, 되돌리기도 플래그 하나라 멱등하다.
  (`2026-08.md` "2-D(드래그 프록시)")
- **QThread를 필드 하나(`self._worker`)로만 추적하면, 이미 도는 워커가 있는 상태에서 그
  필드를 새 워커로 덮어쓰는 순간 이전 워커가 고아가 된다.** `closeEvent`의
  `isRunning()` 가드는 최신 필드만 보므로, 고아 워커가 여전히 도는 중에도 다이얼로그가
  닫혀 파괴될 수 있다 — 이 프로젝트가 반복 겪은 QThread-vs-destroyed-widget 크래시와
  같은 뿌리. 트리거는 "생성자에서 시작한 백그라운드 조회를 사용자가 다시 트리거할 수
  있는 진입점(설정 버튼 등)이 있고, 그 진입점이 빠르게 재호출될 수 있을 때" — 시작하기
  전에 기존 워커가 도는 중이면 새로 안 띄우는 가드 한 줄로 막는다(실제 재현은 안 했으나
  구조적으로 명백해 self-review 단계에서 선제 수정). (`2026-08.md` "§8 항목20 후속 —
  Mermaid/SVG 다이얼로그 UX 개선 8건")
- **`QEvent.Type.WindowDeactivate`는 `changeEvent()`가 아니라 `event()`로 직접 전달된다.**
  `changeEvent()`는 그 대신 `QEvent.Type.ActivationChange`만 받는다(구분하려면
  `isActiveWindow()`를 같이 봐야 함). "창이 비활성화되면 스스로 닫히는 팝업"을
  `changeEvent()`에서 `WindowDeactivate`를 잡는 방식으로 구현했더니 pytest는 통과했지만
  (`changeEvent()`를 **직접 호출**하는 합성 테스트라 Qt가 실제로 그 경로를 타는지 자체를
  검증 못 함 — 규칙 11-c 위반: 메서드 직접호출은 프록시검증이지 확인이 아니다) 실사용에서
  바깥 클릭이 전혀 안 먹혔다. 진짜 top-level 창 두 개를 띄우고 하나를 `activateWindow()`
  하는 실측으로 두 이벤트 타입이 서로 다른 통로로 배달됨을 확인 후 `event()` 오버라이드로
  교체 — 회귀 테스트도 `changeEvent()` 직접호출 대신 같은 실측 기법(진짜 창 두 개+
  `activateWindow()`)으로 다시 썼다. (`2026-08.md` "SVG 에셋 생성창 5건" 후속 —
  확대창 갤러리 뷰어화)
- **캔버스 단축키를 지키려고 스핀박스를 `FocusPolicy.NoFocus`로 막으면 클릭해서 타이핑하는
  것도 함께 막힌다 — `ClickFocus`가 두 요구를 동시에 만족한다.** Del·Ctrl+D·도구 숫자키는
  `_AnnotatorView.keyPressEvent`가 처리하므로 속성패널 스핀박스가 (휠 조작만으로도) 포커스를
  가져가면 그 단축키가 캔버스로 안 간다는 이유로 반경 스핀박스를 NoFocus로 만들었던 게
  2026-07-24부터 있던 설계였는데, 회전 각도 필드를 그대로 복사했다가 "클릭해서 숫자를 치려는데
  커서 깜빡임도 없다"는 실사용 재현으로 드러났다. 오프스크린에서 `QWheelEvent`를 포커스 없는
  스핀박스에 직접 보내 실측한 결과 `ClickFocus`는 휠만으로는 포커스를 안 뺏기면서(값은 바뀜)
  명시적 클릭 시엔 정상적으로 포커스를 얻어 타이핑도 된다 — NoFocus의 상위 호환. (`2026-08.md`
  "회전 스핀박스 타이핑 불가 버그 + 화살촉 크기(배율) 신설")
- **`QDialog.reject()`/`accept()`는 `closeEvent()`를 거치지 않고 곧장 `done()`으로 간다 —
  `closeEvent()`에만 건 "워커 도는 중엔 안 닫음" 가드는 X 버튼(WM_CLOSE)만 막고 Cancel
  버튼·Esc 키는 무방비로 샌다.** `_MermaidDialog`/`_SvgAssetDialog`가 창을 열면 자동으로
  도는 모델목록 조회(`_ModelListWorker`, parent=다이얼로그)가 안 끝난 채 Cancel을 누르면
  `reject()`가 그 가드를 안 거치고 다이얼로그를 곧장 없애 살아있는 자식 QThread까지 함께
  파괴돼(`docs/pitfalls.md` 위 "QThread-vs-destroyed-widget" 계열과 같은 뿌리) 프로그램
  전체가 죽었다(실사용 재현: 창 열자마자 X는 안 먹히고(가드가 걸림) 그 상태에서 Cancel을
  누르면 크래시). 근본 수정은 가드 위치를 옮기는 게 아니라 설계를 바꾸는 것 — 닫기는
  무엇이 돌든 항상 즉시 허용하고(X·Cancel·OK·Esc 전부 `done()` 오버라이드 하나로 수렴),
  아직 도는 워커는 `disconnect()`+`setParent(None)`으로 다이얼로그와 분리해 결과를 버리고
  백그라운드에서 조용히 끝내게 한다(모듈 전역 집합에 잠깐 보관해 GC를 막고, `finished`
  시그널에서 스스로 정리 — `_detach_worker`). "닫는다는 건 결과가 필요없다는 뜻"이라
  애초에 막을 이유가 없었다는 게 핵심 통찰. (`2026-08.md` "Mermaid·SVG 생성창 X/Cancel
  크래시 수정 — 닫기 즉시 허용 + 워커 분리")
- **`QWidget.setAcceptDrops(True)`가 걸린 자식 위젯은 그 위젯이 커서 아래 있는 동안
  드래그/드롭 이벤트를 전부 자신이 받는다** — 부모(`QMainWindow` 등)가 같은 플래그를
  가지고 있어도 그쪽으로 안 넘어온다(부모의 오버라이드는 자식이 없는 영역에 떨어졌을
  때만 쓰이는 폴백). 캔버스 뷰포트(`QGraphicsView`)에 `viewport().setAcceptDrops(True)`가
  걸려 있으면(2026-08-19 팔레트 드래그 지원 때 추가) 파일 URL 드래그도 `CanvasWindow`가
  아니라 뷰포트로 직접 가는데, 뷰 자신의 기본 드래그 처리는 "그 위치에 드롭을 받는 씬
  아이템이 있는지"로 매번 재판정해 우리 도형·화살표엔 그런 아이템이 없어 `dragMoveEvent`가
  항상 거부됐다. 실측(`event.isAccepted()`)으로 `dragEnter`=True(낙관적으로 일단 받아줌)인데
  `dragMove`=False(실제로 위치별 재판정에서 거부)로 갈리는 걸 확인 — **커서 아이콘(허용/
  금지)은 `dragMove` 기준**이라 "캔버스 중앙에 파일을 끌면 항상 금지 커서만 뜬다"로
  보였다. 이런 자식 위젯이 이미 있는 상태에서 부모가 처리해야 할 새 드래그 종류를 추가할
  땐, 부모의 `dragEnterEvent`/`dropEvent`를 확장하는 걸로는 부족하고 그 자식이 이미 걸어둔
  `eventFilter`(있다면, M3 #17처럼)를 같이 확장해야 한다 — 부모 핸들러는 애초에 호출될
  기회가 없다. (`2026-08.md` "캔버스 드래그앤드롭 확장 — .ecad/.dxf·.dwg/.svg 지원 +
  뷰포트 이벤트필터 버그 수정", 2026-08-24 — 첫 진단("재시작 안 한 옛 프로세스")이
  틀렸음을 프로세스 시작시각 실측으로 스스로 확인한 뒤 원인 재조사)
- **`__init__`에서 무조건 만든 `QTimer`가 pytest 스위트 전체 한정 플레이크를 만들 수 있다**
  — 아이템 900개 가까이 되는 이 프로젝트 pytest 스위트는 `CanvasWindow()`를 만드는 테스트
  마다 창을 안 닫는 관례가 있어(기존 관례, 이번에 발견한 것뿐) 창·씬·타이머가 계속 쌓인다.
  이 상태에서 `_AnnotatorView.__init__`에 새 `QTimer(self)`를 무조건 추가했더니(실제로 그
  기능을 한 번도 안 쓰는 나머지 테스트까지 전부 여분의 QTimer를 갖게 됨) 완전히 무관한
  테스트(`test_minimap_bounds_cached_and_invalidated_by_scene_change` — `QGraphicsScene.
  changed`의 비동기 발행을 이미 알고 방어 코드까지 있던 테스트)가 격리 실행은 항상 통과인데
  전체 스위트에서만 10~40% 확률로 실패했다. 증상이 난 지점(그 테스트에 processEvents 3x
  추가)을 고치는 시도 3번은 실패율을 낮췄을 뿐 0으로 못 만들었다 — 진짜 원인은 무조건
  생성한 QTimer 자체가 Qt 전역 이벤트루프의 타이머 스케줄링 순서를 미세하게 흔들어, 이미
  존재하던 "다른 창들의 지연 시그널이 언제 플러시되는가"의 타이밍을 건드린 것. **지연
  생성**(실제로 그 기능을 처음 쓸 때만 `QTimer` 인스턴스화)으로 전체 스위트 12회 연속
  0% 실패로 확정 해결. 상세 함정→해법 전문은 개인 위키
  (`~/.claude/wiki/easycad-pytest전역QTimer-이벤트루프타이밍교란-지연생성.md`).
  (`2026-08.md` "팬도 드래그 프록시 대상으로 확장 — v2 후속(정적캐시+줌)", 2026-08-24)
- **위젯이 한 번이라도 `setStyleSheet()`를 받으면, 그 뒤 `QApplication.setPalette()`만으로는
  재도색이 안 된다** — 색을 QSS가 지정했든(콤보박스 배경) 안 했든(글꼴만 지정한 라벨) 상관
  없다. Qt/Fusion이 스타일시트 걸린 위젯을 "폴리시된 그 순간의 팔레트로 고정"해버려, 이후
  테마 토글로 앱 팔레트가 바뀌어도 다시 안 읽는다. 이 프로젝트에서 세 번 연달아 재발했다 —
  ⓐ 콤보박스·SVG 후보 스크롤영역(테두리 QSS), ⓑ 섹션 제목 라벨(글꼴만 지정하는
  `_SECTION_TITLE_QSS`, **색은 지정도 안 했는데 재도색이 막힘**), ⓒ 아이콘 버튼(색을 인라인
  QSS `color:` 로 구웠거나, `_act_icon()`이 돌려주는 QIcon을 생성 시점에 한 번만
  `setIcon()`한 경우). 세 경우 모두 "테마를 바꾸고 재사용 인스턴스를 다시 열면(2026-08-25부터
  Mermaid/SVG 생성창이 인스턴스 재사용) 처음 열 때 색으로 고정돼 배경과 구분이 안 된다"는
  같은 증상. 유일한 해법은 **테마가 바뀔 때마다 그 위젯에 같은(또는 새로) 스타일시트/아이콘을
  다시 걸어 강제 재폴리시하는 것** — `_refresh_theme_colors()` 안에 관련 위젯 전부를
  등록해두지 않으면 새 QSS 위젯을 추가할 때마다 재발한다. (`2026-08.md` "라이트/다크 전환 시
  콤보박스 잔존 다크 + 타이틀바 지연갱신 수정" 2026-08-26, 및 그 후속 "제목 라벨·버튼 색
  고정 수정" 2026-08-26~27)
- **`QDialog` 인스턴스를 재사용하면(2026-08-25부터 관례) `done()`에서 정리 안 한 참조가
  다음 열기까지 살아남아 죽은 객체를 건드릴 수 있다.** `_detach_worker()`로 워커를
  고아로 뗀 뒤 `finished`에서 `deleteLater()`로 C++ 객체를 지우지만, 다이얼로그 필드
  (`_model_list_worker`/`_workers`)는 안 비웠다 — 다음 번 `done()`이 이미 파괴된 QThread에
  `isRunning()`을 불러 RuntimeError, 그게 Qt 가상함수(`QDialog.done`) 밖으로 새 나가
  `qFatal()`→`abort()`로 프로세스가 즉사했다(minidump fastfail 7 = 명시적 abort, 메모리
  손상 아님 — 커스텀 `sys.excepthook`+stderr 미러링 진단 런처(`tools/diag_run.py`)로
  traceback 포착). 인스턴스를 한 번만 만들고 버리던 시절엔 이 클래스의 버그가 원리적으로
  안 생겼다 — **인스턴스 재사용으로 전환한 코드는 "이번에 쓰고 버릴 필드"를 다음 열기 전에
  누가 비우는지 감사해야 한다.** (`2026-08.md` "생성창 반복 열닫기 크래시 근본 원인 수정",
  2026-08-26)
- **같은 `QPlainTextEdit`이 매번 다시 포커스를 받는 재사용 다이얼로그는, 그 위젯에 남은
  포커스를 안 떼고 숨기면 IME 컨텍스트가 안 풀리고 쌓일 수 있다** — 타이핑 여부와 무관하게
  포커스만으로도 Windows가 그 위젯에 IME 컨텍스트를 붙인다. Mermaid/SVG 생성창을 X로
  3~6회 반복 닫으면 정확히 4회째에 크래시(재현 100%, 자동화 클릭)했는데, 원인 후보로
  세운 Fasoo DRM 후킹(`f_sps.dll`)은 삭제+재부팅 후에도 재현되는 것으로 직접 반증 —
  모든 크래시 덤프에 공통으로 남은 유일한 모듈이 한글 IME(`imkrtip.dll`/`msctf.dll`)였다.
  `done()`에서 `super().done(r)` 직전 현재 포커스 위젯을 `clearFocus()`하면 재현이 크게
  줄었다(medium confidence — 심볼 없는 네이티브 크래시라 100% 근절은 미증명). **스택
  트레이스에 실제로 보이는 모듈(DRM·IME·테마 DLL)을 원인으로 단정하기 전에 "삭제해도
  재현되는가"부터 반증하라**는 전역 규칙 3-b와 같은 사례. (`2026-08.md` "Mermaid/SVG 창
  반복 X닫기 크래시 완화", 2026-08-26)
- **`DwmSetWindowAttribute(DWMWA_USE_IMMERSIVE_DARK_MODE)`가 HRESULT 성공을 반환해도, 그
  뒤 `SWP_FRAMECHANGED`만으로는 타이틀바가 그 자리에서 다시 그려지지 않을 수 있다** — 이
  프로젝트에서 이 API 자체는 이미 2026-08-13에 "성공해도 시각 변화 없음"으로 한 번, 이번에
  `SWP_FRAMECHANGED`를 추가해도 재현되어 두 번째로 막혔다. 사용자가 반복 보고한 "창 크기를
  조절하면 반영된다"를 그대로 흉내내(세로 1px 늘렸다 즉시 되돌리는 **진짜 리사이즈**,
  `SetWindowPos` 2회) `SWP_FRAMECHANGED` 단독보다 신뢰도가 높았다. 별개로, 토글 이벤트
  핸들러가 "그 순간 열려 있던 창"만 훑는 구조라면 **토글 이후에 새로 열리거나 재사용
  인스턴스가 다시 보여지는 창은 원리적으로 못 잡는다** — 그런 창은 자기 `showEvent`에서
  스스로 "지금 실제 테마가 뭔지" 물어 동기화해야 한다(부모 체인이 `CanvasWindow`가 아닌
  손자뻘 다이얼로그는 `self.parent()`가 아니라 `self.parent().parent()`까지 타고 올라가야
  `_dark`에 닿는 경우도 있음 — 부모 체인 가정하지 말고 실제로 확인). (`2026-08.md` "라이트/
  다크 전환 시 콤보박스 잔존 다크 + 타이틀바 지연갱신 수정" 2026-08-26, 후속 재보고 대응
  2026-08-27)
- **"Qt 슬롯 안 처리 안 된 파이썬 예외 → `qFatal()`→`abort()`"는 이 문서에 여러 번 등장한
  크래시 클래스의 공통 뿌리다(위 항목들 다수, `tools/diag_run.py` 신설 계기가 된 건도
  포함).** `sys.excepthook`을 앱에 직접 설치하면 이 abort 자체가 안 일어난다 — 진단
  도구(`diag_run.py`)로만 쓰다가, 2026-08-31에 이를 상시 크래시 리포터
  (`easycad/crash_report.py`)로 승격해 이 클래스 전체를 예방으로 전환했다. 대조군(훅 미설치)
  은 이 프로젝트 특유의 `exit 127` 시그니처로 죽고, 설치 후엔 `exit 0`으로 생존함을 실제
  창에서 직접 재현·확인. (`2026-08.md` "크래시 리포트 시스템 도입", 2026-08-31)
- **`QStandardPaths.AppDataLocation`은 `QCoreApplication`의 조직/앱 이름을 명시하지 않으면
  실행 파일명을 그대로 폴더명으로 쓴다** — `python run.py`(dev, "python")와 빌드된
  `EasyCAD.exe`(배포, "EasyCAD")가 같은 코드로도 서로 다른 저장 경로를 갖게 된다(실측
  확인). `if not QCoreApplication.organizationName(): ...`처럼 "안 정해져 있으면만"
  가드를 써도 소용없다 — Qt가 이미 기본값을 채워둔 상태라 그 가드 자체가 항상 거짓이 된다,
  무조건 덮어써야 한다. (`2026-08.md` "크래시 리포트 시스템 도입", 2026-08-31)

## 좌표계·변환
- `drawForeground`의 painter는 Qt가 이미 **씬 좌표계**로 매핑해 넘긴다 — 여기에 다시
  `mapFromScene()`을 적용하면 이중 변환이 되어 원점 근처에서만 우연히 맞아 보인다. 미니맵
  인디케이터가 이 버그로 실사용에서만(데모 좌표는 우연히 통과) 어긋났다. 같은 증상이 재발하면
  트리거보다 **핵심 계산 자체**부터 재검증. (`2026-07.md` 미니맵)
- `fitInView(KeepAspectRatio)`는 뷰포트 종횡비가 극단(가로로 김)이면 레터박스가 커져 안의
  인디케이터가 실제보다 작아 보인다 — 미니맵류 패널은 좁고 세로로 긴 비율로 설계. 두 번
  반복된 함정(2026-07-28 dock, 2026-07-29 플로팅 패널). (`2026-07.md` 미니맵·캔버스-퍼스트 레이아웃)
- `_HandleResizeMixin.boundingRect()`가 회전 핸들(좌상단에만 존재)과 접속점을 union할 때, 한쪽만
  비대칭으로 튀어나오면 도형 중심이 실제 콘텐츠 중심에서 어긋난다 — 대칭 패딩으로 상쇄해야 함.
  (`2026-08.md` qc-dot/hover-port 통합)
- 아이템의 rect가 절대(씬) 좌표를 그대로 품고 `pos()`는 (0,0)에 남는 패턴(마우스로 그린 도형)이
  흔하다 — 정렬·스냅 기준점은 `pos()`나 로컬 원점이 아니라 `_content_rect()` 좌상단의
  `mapToScene()` 값을 써야 한다. (`2026-07.md` 그리드/스냅투그리드)
- `_scale_or_1`류 헬퍼가 아이템 자체 `.scale()`만 보고 뷰의 줌 배율을 무시하면, 확대할수록
  선택 테두리·핸들이 도형과 함께 커진다 — 뷰의 `isInteractive()`인 메인 캔버스 줌을 별도로 곱해야
  화면 픽셀 크기가 고정된다(Figma/AutoCAD 관례). (`2026-07.md` 실사용자 DXF 검증)
- 위 헬퍼가 이미 뷰 줌을 곱해서 반환한다는 걸 잊고 새 핸들 코드에서 뷰 줌을 **한 번 더** 곱하면
  반대 방향 버그가 난다(줌에 비례해 커지는 게 아니라 반비례해 작아짐) — 세그먼트(직교 커넥터
  중간점) 핸들만 다른 핸들과 다르게 줌마다 크기가 달라 보였던 원인. 같은 이유로 "안티에일리어싱
  여유분" 같은 사소한 상수도 씬 단위로 남겨두면 확대할수록 화면에서 그대로 커진다(포트-테두리
  커버 마스크의 `+1.0`) — 화면에서 항상 같은 크기여야 하는 값은 전부 `_scale_or_1()`로 나눠야
  한다는 원칙을 새 코드를 추가할 때마다 다시 확인할 것. (`2026-08.md` 후속 2 — 줌 이중스케일)
- 위 원칙의 **네 번째 재발**: `_box_edge_side`의 tol 계산이 씬단위 상수 `_EDGE_HIT_MIN`을
  `_scale_or_1()`로 나누지 않고 그대로 썼다 — 같은 상수를 쓰는 다른 세 호출부(`_base_shape`
  스트로크 폭)는 전부 나누는데 이 한 곳만 빠져 있었다. 저배율에선 눈에 안 띄다가 고배율(사용자
  2164% 줌)에서 리사이즈 밴드가 화면 86px까지 벌어져 "테두리에 닿지도 않았는데 리사이즈
  커서"로 나타났다. **같은 클래스 안에 스케일 나눗셈이 있는 형제 함수가 있으면, 새/수정 함수도
  나눗셈을 넣었는지 형제들과 나란히 grep 대조할 것** — 값 하나가 몇 줄 안 걸려도 사람 눈으로
  "이건 이미 됐겠지"로 넘어가기 쉽다. (`2026-08.md` 포트 테두리 리사이즈 밴드 줌 비례 확대)
- `_content_rect()`는 도형마다 재귀 방지·hit-test 여유로 펜폭만큼(선/화살표/DXF패스는
  펜폭/2+1~+2, 정사각·심볼은 Qt 기본값 펜폭/2) **부풀린** 값이다 — "이 도형의 보이는
  외형"이 필요한 곳(정렬 스냅의 `srect()` 등)에 그대로 쓰면 "닿았다"고 판정해도 실제
  경계와 간극이 남는다(실측: 펜폭 1.0 기준 1.5유닛, 고배율에선 정렬 문턱보다 커져 아예
  안 붙는 것처럼 보임). 패딩 없는 진짜 기하가 필요하면 `.rect()`/`.path().boundingRect()`/
  `_conn_polyline_scene()`처럼 그 클래스의 원본 기하 접근자를 직접 쓸 것 — `_content_rect()`
  는 이름과 달리 "콘텐츠 그 자체"가 아니라 "콘텐츠 + 핸들/hit-test 여유"다.
  (`2026-08.md` §8 항목17 후속 — 포트 정렬-스냅 간극)
- 위 항목은 선/화살표/타원/DXF패스 4종만 고쳤었는데, `_RectItem`/`_SymbolItem`도 `_content_
  rect()`를 override 안 해 **똑같이** Qt 기본 `boundingRect()`(펜폭/2 패딩)를 물려받는다는 걸
  놓쳤다 — 사각형은 패딩이 작아(펜폭 1.0→0.5유닛) 저배율에선 안 보이다가, 4354% 줌에서 "붙였다"
  고 여긴 도형 중심점이 실제 테두리 선이 아니라 그 0.5유닛 바깥에 얹혀 보이는 걸로 재발했다.
  **패딩 있는/없는 두 종류의 후보를 같은 스냅 풀에 섞는 것 자체가 위험**하다는 것도 이번에
  확인 — 값이 우연히 같은 도형(정사각형 등)에서 0.5유닛 어긋난 "거의 같은데 다른" 후보 두
  개가 생겨 동률 판정(tie-break)이 깨졌다(1차 구현 회귀). 해법은 소스를 하나로 통일하는 것
  (`_tight_scene_bbox`) — "패딩 계산을 후보마다 보정"하는 방향은 시도하지 말 것.
  (`2026-08.md` 스마트 정렬 스냅 시리즈, 2026-08-10)
- 위 `_content_rect()` 오용의 **재발**(최종 검수 Phase 5, 2026-08-26) — 메뉴 기반
  정렬(`host_context.py::_align_rect`)이 드래그 스냅과 별개 코드 경로라 2026-08-10에
  고친 `_smart_snap_srect`를 안 타고 있었다. 펜 두께가 다른 두 도형을 "왼쪽 맞춤"해도
  실제 시각 경계가 pen/2만큼 계속 어긋났다(실측 4유닛) — **같은 판정을 하는 두 번째
  경로가 있으면 한쪽만 고치고 반대쪽은 놓치기 쉽다**, 재발 감사 때 "이 판정 로직을
  쓰는 다른 진입점이 또 있는가"를 함께 grep할 것. (`2026-08.md` "최종 검수 Phase 5")

## 히트테스트·법선(포트/스냅)
- 도형 꼭짓점(마름모 E/W, 사각형 대각)은 두 변이 만나는 자리라 "최근접 변 하나"로 법선을 구하면
  변 선택이 임의가 되어 좌우 포트가 위아래로 드나든다 — 마름모는 의도된 축(N/S=수직·E/W=수평)
  강제, 사각형은 가까운 변 기준으로 강제. 보정은 **공유되는 저수준 함수**(`_nearest_border`)
  자체에 둬야 한다 — 상위 호출부 중 하나(포트 목록)에만 넣으면 다른 호출부(자동 라우팅)는
  여전히 옛 버그를 갖는다. (`2026-08.md` 8포트 확장)
- 재부착(끝점) 드래그 중 화살표가 **자기 자신의 다른 구간**에 스냅될 수 있다 — 스냅 함수에
  `exclude=self`를 반드시 넘겨야, 시각적으론 붙어 보여도 바인딩이 None으로 남는 사고를 막는다.
  (`2026-07.md` M4-4 최종 잔여)
- `QGraphicsItem.contains()`/`shape()`는 "클릭하기 쉬운 히트 밴드"이지 실제 기하 경계가 아니다
  — 속 빈(NoBrush) 도형은 `_base_shape()`가 `_EDGE_HIT_MIN`만큼 부풀린 스트로크를 반환해,
  테두리 2px 바깥도 `contains()`가 `True`를 준다. "테두리 두께 중심 안쪽/바깥쪽" 같은 기하
  판정엔 `contains()`를 쓰지 말고 도형별 실제 경로(사각형 `rect()`, 원 `addEllipse`, 심볼
  `_sym_path()`, PathItem `path()`)로 직접 확인해야 한다. (`2026-08.md` 미선택 도형 테두리
  연속 호버)
- ⚠ **시각효과로 기하를 위장하면 그 순간 진실이 두 벌이 된다.** 포트 트림(호스트 테두리가
  포트 자리만큼 끊겨 보이는 것)은 진짜 기하 분절이 아니라 포트가 자기 자리를 배경색으로
  덮어 그리는 것이다(2026-08-03 Qt 버그 우회, `_paint_port_cover_if_needed`) — 픽셀만
  가릴 뿐 기하는 온전한 사각형이라, 히트/스냅/호버는 *화면에 그려지지도 않은* 테두리
  조각을 계속 유효한 것으로 봤다(포트 몸통 한가운데서 십자 커서 + 화살표가 보이지 않는
  선에 실제 바인딩). 고칠 땐 `_nearest_border`(원본)를 바꾸지 말 것 — 포트를 테두리에
  *부착*하고 슬라이드시키는 데도 쓰여서, 트림을 반영하면 포트가 자기가 만든 구간에서
  밀려난다. 호버·스냅 호출부만 `_nearest_border_visible`(트림 구간이면 `None`)을 opt-in
  하고, 트림 판정은 화면 렌더와 **같은 함수**(`build_trimmed_border_path`가 쓰는
  `_host_outline_local_polygon`/`_port_edge_gap`)로 해 정의를 한 벌로 유지한다.
  위장을 도입할 땐 그 기하를 소비하는 곳 전부(히트·스냅·라우팅·내보내기)를 같이 세어 볼 것.
  (`2026-08.md` 포트 트림 구간 스냅 차단)
- **심볼이 자기 bbox를 그대로 안 채우면(비율 유지를 위해 내접·패딩시키면), 그 도형을 다루는
  bbox 기반 시스템(리사이즈 핸들·qc-dot·자국 핸들 등) 전부가 어긋나고, 그때마다 도형 종류별
  특례 코드가 늘어난다.** 삼각형을 "항상 정삼각형이어야 한다"며 `_tri_rect`로 bbox 중앙에
  내접시켰더니, 리사이즈 핸들 위치→중복 마커→qc-dot 자리 다툼까지 여러 라운드에 걸쳐 특례를
  쌓아가며 고쳤는데, 매번 다른 증상으로 재발했다(근본 원인은 하나: 패딩). 결국 Lucid 같은
  기존 도구 대조로 "도형이 그냥 bbox를 채우면(비정삼각형이어도) 이 문제 자체가 안 생긴다"는
  걸 확인하고, 비율 유지는 **렌더링 함수가 아니라 기본 생성 크기**(`_PALETTE_TRIANGLE_WH`
  같은)가 담당하도록 옮겨 특례 코드를 전부 삭제했다. **도형 kind를 새로 추가할 때 "비율을
  유지해야 한다"는 요구가 나오면, 렌더 함수에서 bbox를 내접/패딩시키지 말고 기본 생성 크기의
  가로세로 비율로 그 비율을 미리 정해 두는 쪽을 먼저 검토할 것** — 리사이즈하면 다른 도형처럼
  비율이 깨지는 걸 받아들이는 게(원→타원과 동일), 매 핸들 시스템에 특례를 심는 것보다 싸다.
  (`2026-08.md` 삼각형 bbox-fill 재설계, 2026-08-10)
- **"다른 도형에 겹쳐 가려짐" 억제 로직을 `sh in occluders`처럼 짜면 도형이 자기 자신도
  occluder 목록에 들어가(자기 몸통 안이 곧 자기 interior) 자기 자신을 스스로 배제한다.**
  겹침 억제(2026-08-09, `_port_dot_target`)의 의도는 "**다른** 도형에 가려질 때"였는데
  구현은 자기 자신 포함 여부만 봐서, 겹침이 전혀 없는 평범한 도형도 자기 몸통 한가운데를
  호버하면 예고 마커가 영영 안 뜨는 부작용이 났다(사용자가 실사용 중 발견하기까지 이틀).
  `sh in occluders` → `any(o is not sh for o in occluders)`로 "자기 자신 말고 다른
  점유자가 있을 때만" 억제하도록 바꿔야 의도한 규칙이 된다. **"X가 Y를 가리면 안 된다"는
  요구를 구현할 때 X==Y(자기 자신) 케이스가 조건식에 실수로 포함되지 않았는지 항상 확인.**
  (`2026-08.md` 도형 안쪽 호버 마커 미표시 버그 수정, 2026-08-11)
- **후보 목록(`_conn_shapes_near`)에서 아예 빠진 아이템 종류는 occluder로도 존재하지
  않는다.** 클립보드 이미지(`_ImageItem`)는 포트가 없어 애초에 이 함수의 isinstance
  필터에서 제외돼 있었는데, 그 필터가 "포트 후보"와 "occluder 후보"를 겸하고 있어서
  이미지가 화면 맨 위에 그려져도 호버·포트 히트테스트가 그 존재 자체를 몰라 뚫고 지나갔다
  (뒤 도형 예고점이 이미지를 뚫고 뜨고, 이미지를 드래그해도 뒤 도형의 커넥터가 뽑혔다).
  "포트를 제공하는가"와 "다른 걸 가릴 수 있는가"는 별개 질문이다 — 후보 필터를 좁힐 땐
  그 목록이 이후 몇 가지 다른 목적으로 재사용되는지 먼저 확인할 것. 또한 `_shape_interior_
  contains` 기반 겹침 판정(위 항목)은 "포트가 호스트 테두리 위에 얹혀 호스트도 내부로
  오판" 함정이 있어 포트-호스트 관계엔 못 쓴다(`_hover_port_at`에 그대로 옮기면
  `test_port_participates_normally_in_hover_and_qc_systems` 회귀) — 이런 부모-자식
  관계가 있는 대상엔 실제 **Qt 화면 스택 순서**(`scene().itemAt(scene_pt, transform)`)로
  판정하는 쪽이 더 정확하다: 포트는 호스트의 진짜 Qt 자식(`setParentItem`)이라 같은
  지점에서 Qt가 항상 자식을 topmost로 돌려주므로, "topmost가 후보 자신이거나 그 부모-자식
  체인 안이면 안 가려진 것"이라는 판정이 포트-호스트는 자동으로 통과시키면서 이미지처럼
  진짜 무관한 아이템만 걸러낸다. (`2026-08.md` 클립보드 이미지가 뒤 도형을 가려도
  호버·드래그가 뚫고 반응하는 버그 수정, 2026-08-11)
- **커서 판정과 `shape()`(Qt 히트영역)는 별개 코드 경로라, 한쪽만 넓히면 어긋난다.** 변
  리사이즈 밴드(`_box_edge_side`, 테두리 안팎 tol)는 호버 커서(`_box_handle_cursor`)에서만
  쓰이고, `shape()`는 모서리·회전 핸들만 union하고 이 밴드는 넣지 않았다 — 테두리 바깥쪽
  tol 안(리사이즈 커서가 뜨는 자리)을 클릭하면 Qt `itemAt()`이 이 도형을 히트로 못 잡아
  이벤트가 캔버스 빈 공간으로 새(선택 해제·러버밴드 시작), "커서는 리사이즈인데 클릭하면
  도형 밖을 누른 것처럼 동작"하는 버그가 됐다. 커서 판정에 쓰는 tol 밴드를 넓히거나 새로
  추가할 땐 그 밴드를 `shape()`에도 반드시 같이 union해야 한다 — tol 계산을
  `_box_edge_tol()` 하나로 공유시켜 커서·히트영역이 항상 같이 움직이게 고쳤다. (`2026-08.md`
  테두리 바깥쪽 리사이즈 커서 클릭이 캔버스 빈 공간처럼 새던 버그 수정, 2026-08-11)
- **"변 목록 하나가 곧 순환 루프 하나"라고 가정한 알고리즘에 다중 서브패스(저장소=원기둥:
  윗면 타원+몸통 2개) 도형의 flat 변 목록을 그대로 먹이면, 서로 다른 서브패스끼리 우연히
  안 이어진다는 사실 하나에만 기대게 된다** — TRIM 파괴적 재설계(2026-08-30)에서
  `_closed_shape_trim_fragments`(닫힌 루프 하나를 자르는 함수)에 심볼의 flat
  `_host_outline_edges` 결과를 그대로 넘기면, 몸통만 잘라도 안 건드린 윗면 타원까지
  "루프가 안 이어지니 그냥 열린 조각"으로 잘못 나올 뻔했다(정방향 오염은 좌표가 우연히
  안 맞아 자연히 막히지만, 역방향 — 멀쩡한 닫힌 서브패스가 열림으로 오판되는 것 — 은 안
  막아준다). `_host_outline_edges`를 서브패스별 (변, 닫힘여부) 공용 제너레이터로 리팩터해
  변 인덱스 경계(`_host_outline_edge_spans`)를 뽑고, cuts를 서브패스별로 나눠 독립
  판정해야 한다. **"이 자르기 알고리즘이 도형 전체를 한 바퀴 도는 순환으로 가정하는가"를
  먼저 확인하지 않고 다중 서브패스 데이터를 그대로 먹이면, 테스트가 단일 서브패스 도형
  (사각형·원·다각형)만으로는 절대 못 잡는 조용한 오염이 생긴다.** (`2026-08.md` TRIM
  근본 재설계 1~4단계, 2026-08-30)
- **"진짜 모서리 vs 곡선 근사 조각"을 각도(양옆 변의 방향 차이)로 판정하면, 설계가
  일부러 접선 방향으로 매끄럽게 잇는 전환(진짜 원기둥처럼 벽과 곡면이 꺾이지 않는 것)을
  못 잡는다 — 각도가 거의 0이라 "같은 곡선의 일부"로 오판한다.** 저장소(원기둥) 심볼의
  아랫면 호를 커터 없이 클릭해도 근사 조각 하나만 지워지는 문제를 각도 기반으로
  고치려다 자체검증에서 발견: `_sym_database`의 벽→호 전환이 접선이라, 호를 클릭하면
  양쪽 벽까지 통째로 삭제됐다(고치기 전보다 더 나쁜 결과 — 데이터를 더 많이 잃음).
  **각도(평탄화된 폴리곤의 국소 형태)가 아니라 원본 명령 구조(`QPainterPath.
  elementAt()`의 `LineTo`/`CurveTo` 타입)로 "이 지점이 실제로 다른 그리기 명령으로
  전환되는가"를 판정해야 한다** — 평탄화 이후엔 이미 정보가 사라져 있다. 여기 더해
  같은 좌표가 서로 다른 서브패스에 우연히 등장하면(원기둥 벽 끝점과 타원 최좌단이
  같은 물리적 위치) 이 hard-vertex 집합도 서브패스별로 분리해야 한다(안 그러면 한
  서브패스의 진짜 모서리가 다른 서브패스로 새어나가 확장이 엉뚱한 데서 멈춘다 — 실측:
  타원이 정확히 절반에서 멈춤). **평탄화 이전 원본 구조로 내려가 판정을 다시 세울 땐,
  그 판정 기준이 서브패스 경계를 넘어 공유되지 않는지 항상 확인할 것.** (`2026-08.md`
  "TRIM 근본 재설계 후속 3", 2026-08-30)

## 상호작용 설계(특례 vs 규칙)
- "이 아이템 종류만 다르게 반응해야 한다"는 요구가 나오면, 그 종류를 판별하는 `if` 를 관련
  상호작용 함수마다 심는 방향(특례 산개)으로 먼저 가지 말 것 — 이 앱은 호버·큐닷·드래그가
  이미 병렬 시스템 여러 개(코스 매칭용/이산 매칭용/선택 전용/렌더 전용)로 나뉘어 있어서,
  특례를 하나씩 넣을 때마다 다른 병렬 시스템에 같은 구멍이 남는다 — 3라운드 동안 매번 다른
  함수에서 같은 증상이 재발했다. 먼저 "이 요구를 특수 케이스가 아니라 **전체에 적용할 보편
  규칙 변경**으로 풀 수 있는가"를 검토한다(예: "포트는 화살표만" → "드래그는 항상 화살표만,
  전 도형 공통"). 되면 특례 코드가 아예 필요 없어지고, 병렬 시스템 개수와 무관하게 한 번에
  막힌다. (`2026-08.md` 포트 상호작용 재설계 — 4라운드, 사용자가 직접 패턴을 지적하며 전환)
- `mousePressEvent`/`mouseDoubleClickEvent`는 조기-반환 체인이 여러 겹인데, 그중 다수가
  "선택된 도형 위 클릭은 어느 도구에서든 우선 처리된다"는 오래된 설계 원칙 때문에
  `current_tool`을 아예 확인하지 않는다 — 새 도구(TRIM/EXTEND)를 추가했을 때 이 분기들이
  전부 그 도구의 클릭을 도구 검사 없이 가로챘다: qc-dot 접속점(`_connect_port_at`),
  더블클릭 라벨편집(같은 자리 빠른 재클릭이 Qt에서 `mousePressEvent`가 아니라
  `mouseDoubleClickEvent`로 새는 경우까지 포함), 선택된 끝점 드래그 핸들
  (`_selected_endpoint_item`) — 최소 3곳. 전부 수정 모양이 같았다
  (`... and self._owner.current_tool != "trim"` 가드 추가). 이 클래스의 첫 사례는
  Alt+드래그 버그(모디파이어 미확인)로 이미 겪었고 그때 "새 전역 제스처를 추가할 땐
  모든 조기-반환 분기를 먼저 나열하고 우선순위를 정하라"는 교훈까지 남겼는데, 이번에
  새 **도구**를 추가할 때는 그 감사를 먼저 안 하고 시작해 사용자가 새 증상을 보고할
  때마다 하나씩 순차 발견했다. **새 도구/모드를 추가하기 전에 `current_tool`을 확인
  안 하는 early-return 분기를 `mousePressEvent`·`mouseMoveEvent`·
  `mouseDoubleClickEvent` 전체에서 먼저 grep(`grab`·`hit is not None`·`return` 패턴)해
  그 도구가 통과해야 하는 우선순위표를 만들 것.** (`2026-08.md` §8 항목17 후속 — qc-dot·
  더블클릭·끝점핸들 3종, 개인 위키 `easycad-이벤트우선순위-특수히트존-모디파이어누락.md`
  에 재발 기록 포함)
- **의미가 다른 두 컨트롤이 "단순화하자"며 같은 오프셋 상수를 공유하면, 한쪽만 값을 바꿔야
  할 때가 오면서 서로를 침범한다.** 모서리 리사이즈 핸들과 큐닷(커넥터 점)이 2026-08-03에
  같은 `_HANDLE_GAP_FACTOR`(6px)를 공유하도록 묶였다 — 당시 이유는 "핸들이 도형 일부처럼
  보인다"는 지적 하나였는데, 정작 그 지적의 실제 해법(Figma/Lucid 스크린샷 실측 결과 색
  대비로 구분하지 오프셋으로 구분하지 않음)과는 무관한 상수 공유였다. 그 결과 나중에
  큐닷만 훨씬 더 멀리 떨어뜨려야 하는 요구(변 리사이즈 밴드와 겹침)가 왔을 때, 계수를
  공유하는 두 용도를 갈라야 했다. **"같은 값을 쓰면 간단하다"는 것과 "같은 개념이라 같은
  값이어야 한다"는 것은 다르다** — 상수를 공유하기 전에 "이 두 용도가 앞으로도 항상 같은
  값이어야 할 이유가 있는가, 아니면 지금 같은 값일 뿐인가"를 구분할 것. 레퍼런스 앱과
  대조할 땐 "얼추 비슷하다"는 인상이 아니라 스크린샷을 픽셀 실측해야 정확하다 — 이번에도
  실측 전엔 "리사이즈 핸들도 살짝 띄운다"고 잘못 짐작했었다. (`2026-08.md` 리사이즈 핸들
  오프셋 원복 + qc-dot 전용 gap 분리, 2026-08-11)
- **네이티브 `QDrag`의 고스트 픽스맵은 `exec()` 도중 앱이 위치를 재정의할 수 없다**(OS가
  직접 그림 — 플랫폼 공통 제약). "드래그 중 실시간 스냅"처럼 드롭 전에 위치가 계속
  바뀌어야 하는 상호작용은 QDrag로 아예 못 낸다 — 가이드선 미리보기 정도는 `dragMoveEvent`
  로 가능해도, 실제 아이템이 눈에 보이게 움직이는 체감은 안 나온다. 이런 요구가 오면
  QDrag 대신 씬에 진짜 임시 아이템을 만들고 `grabMouse()`로 전역 마우스를 잡아 손으로
  이동시키는 합성 드래그로 바꾼다 — 이러면 기존 "선택된 아이템 이동" 로직(스냅·가이드선
  포함)을 그대로 재사용할 수 있어 오히려 코드가 준다. (`2026-08.md` 정렬 가이드선 개선 —
  팔레트 드래그 실물 도형화, 2026-08-19)

- **기존 아이템 종류가 새 종류 하나를 놓치는 자리는 "명시적 게이트"가 아니라 "타입을 안
  다루는 침묵"으로 나타난다.** 독립 텍스트(`_TextItem`)를 화살표 접속점 시스템에 편입할 때,
  `_conn_shapes`/`_conn_shapes_near`/`_qc_dot_at` 등 5곳의 isinstance 튜플에 처음부터
  빠져 있었다 — 명시적으로 "텍스트는 제외"라고 쓴 코드는 하나도 없었고, 그냥 그 목록을
  만들 당시 텍스트를 고려 안 했을 뿐이다. 이런 부재는 에러도 경고도 없이 "그 기능이 그
  타입엔 조용히 작동 안 함"으로만 드러나므로, 새 타입을 기존 시스템에 편입할 땐 그 시스템이
  후보를 모으는 함수 전부를 grep(`isinstance(it, (_RectItem, _EllipseItem, ...`)해 병렬
  목록을 한 번에 확인해야 한다 — 위 "이 아이템 종류만 다르게" 항목과 반대 방향(기존 종류
  특례가 아니라 새 종류 누락)이지만 원인은 같다: 병렬 시스템 여러 개에 후보 목록이 흩어져
  있으면 하나씩 순차 발견하게 된다. (`2026-08.md` "독립 텍스트가 화살표 접속점 시스템에서
  통째로 빠져있던 것 수정", 2026-08-22)
- 같은 패턴의 **재발**(최종 검수 Phase 5, 2026-08-26) — `_conn_shapes()`(전체스캔)는
  §8 항목21 후속(2026-08-18)에서 닫힌 `_PolygonItem`을 포함하도록 넓혔는데, 병렬
  후보목록인 `_conn_shapes_near()`(select 유휴 호버 예고점·미선택 도형 큐닷 근접이
  거치는 실제 경로)는 그때 같이 안 넓혀져 다각형을 계속 놓치고 있었다. 이 프로젝트에
  `_conn_shapes*` 계열 병렬 목록이 최소 3벌(전체스캔/근접/호버) 있다는 뜻 — 한 종류를
  새로 편입할 때 grep 대상에 `_near`/`_visible` 접미사 변형까지 반드시 포함할 것.
  (`2026-08.md` "최종 검수 Phase 5")
- **"계산 로직을 넓혔다"와 "그 기능의 진입 게이트를 넓혔다"는 별개의 일이다.** 정렬/분배가
  연결된 화살표까지 다루도록 계산 로직(`_arrow_gap_targets` 등)을 확장했을 때, 서브메뉴를
  띄울지 말지 판정하는 진입 게이트(`_align_targets` 개수만 셈)는 안 건드려서 도형 없이
  화살표만 선택하면 "정렬 / 분배" 메뉴 항목 자체가 안 보였다 — 계산 코드는 맞는데 그
  코드에 도달할 입구가 옛 조건 그대로 막혀 있었던 것. 새 대상 종류를 어떤 기능에 편입시킬
  땐 그 기능의 계산 본문뿐 아니라 "이 기능을 보여줄지/실행할지 먼저 판정하는 게이트"까지
  같은 grep으로 같이 찾아 넓혀야 한다(위 "부재는 침묵으로 나타난다" 항목과 자매 격 — 이번엔
  아예 안 보임이 아니라 "보여야 할 상황에서 숨음"으로 나타났다). (`2026-08.md` "화살표만
  선택해도 정렬/분배 서브메뉴가 뜨도록 수정", 2026-08-23)
- **한 클래스가 여러 출처(손그림/DXF 폴백/SVG 가져오기)를 겸하면, 그중 하나만 겨냥한
  억제를 `isinstance(그 클래스)`로 걸면 나머지 출처까지 통째로 걸린다.** select 도구
  유휴 호버에서 예고점을 끈 건 "손그림 펜 낙서가 노이즈"라는 지적 때문이었는데,
  `isinstance(_PathItem)`로 걸어 SVG 가져오기/생성 결과(대부분 곡선이라 같은 클래스로
  매핑됨)까지 같이 꺼져 "SVG에서 화살표를 시작할 방법이 없다"는 실사용 보고로 이어졌다.
  타입 하나에 서로 다른 사용자 의도(낙서 vs 의도적 배치)가 섞여 있으면 타입이 아니라
  생성 경로에서 세우는 표식(`_freehand` 불리언, 펜 도구만 True)으로 좁혀야 한다.
  (`2026-08.md` "실사용 피드백 3건 — 팔레트 등록 폴더 서브메뉴 + SVG 큐닷 1차 + 그룹
  호버 억제 + 도구 고정 핀 위치 이동", 2026-08-25)
- **위 항목의 거울상 — 특정 목적으로 만든 안전장치(자유단 보호)가, 그 장치의 대상
  클래스에 새로 합류한 다른 출처(TRIM으로 방금 잘려나온 조각)에도 똑같이 걸려 원치 않는
  제약이 된다.** 열린 도형 TRIM의 "커터 없이 자유단에 닿은 세그먼트는 못 지운다"(2026-08-28,
  독립된 선을 한 번에 통째로 지우는 사고 방지)가, 재설계로 새로 생긴 "닫힌 도형에서
  잘려나온 열린 `_PolygonItem` 조각"에도 그대로 적용돼 "사각형 한 변은 지워지는데 남은
  세 변은 트림이 안 먹는다"는 실사용 버그가 됐다(4점짜리 열린 조각은 `0 < best_seg <
  n-2`를 만족하는 변이 가운데 1개뿐이라 자유단에 닿은 나머지 2변이 영원히 막힘). 여기도
  똑같이 클래스가 아니라 **생성 경로 표식**(`_trim_derived`, TRIM 파괴적 변환이 만든
  조각만 True)으로 안전장치를 우회시켜 풀었다 — 신뢰(우회를 허용)와 불신(계속 보호)이
  같은 클래스 안에서 갈릴 땐 표식이 유일한 해법이라는 게 두 번째로 확인됨. **새 기능이
  기존 클래스의 인스턴스를 새로운 방식으로 만들어낼 때, 그 클래스를 겨냥한 기존
  안전장치·특례 코드를 전부 훑어 "이 조건이 새 출처에도 여전히 맞는 전제인가"를 확인할
  것.** (`2026-08.md` "TRIM 근본 재설계 후속 — 별도 제스처에서 나머지 변이 안 지워지던
  버그 수정", 2026-08-30)
- **바로 위 항목과 같은 세션, 같은 진단이 필요했던 세 번째 재발** — 열린 도형 TRIM의
  "cutter와 cutter 사이에 낀 꼭짓점째로 지운다"는 규칙(진짜 자유형 폴리라인엔 의도된
  설계)도 TRIM 파괴적 조각에 그대로 적용돼, 근처에 cutter 역할을 할 다른 도형이 하나만
  있어도 그 cutter까지 가는 길에 낀 모든 중간 변(꼭짓점 여러 개)이 한 번에 트림 후보로
  묶였다("첫 변 자르니 나머지 세 변이 한꺼번에 잘린다", 사용자가 스크린샷으로 재현).
  같은 표식(`_trim_derived`)으로 이번엔 아예 별도 분기를 둬 닫힌 도형 TRIM과 동일하게
  "클릭한 변 하나 안에서만 cutter를 찾는다"로 고쳤다. **한 세션에서 같은 클래스에
  새 출처가 생기면, 그 클래스를 겨냥한 안전장치·특례가 보통 하나가 아니다 — 첫 번째를
  고치고 끝내지 말고, 그 클래스를 조건으로 삼는 다른 함수들도 같이 훑어볼 것**(이번엔
  두 함정이 같은 함수 `_trim_candidate_open_segment` 안에 나란히 있었다). (`2026-08.md`
  "TRIM 근본 재설계 후속 2", 2026-08-30)
- **가상 프록시가 실제 QGraphicsItem 계약의 절반만 흉내내고 있으면, 그 나머지 절반을
  건드리는 새 호출부가 생길 때마다 크래시가 하나씩 새로 발화한다.** `_GroupBindProxy`
  (그룹 전체를 화살표 바인딩 대상으로 삼는 가상 호스트, 2026-08-25 도입)는 처음엔
  `isSelected()`만 있었다(당시 크래시를 잡은 호출부 하나만 필요했으므로). 이번엔 그룹
  자신의 큐닷을 드래그 없이 클릭만 하는 별개 경로(`core_view.py._mouse_release_impl`의
  `_hp_dragging` 종료 처리)가 `setSelected(True)`를 무조건 호출해 `AttributeError`로
  또 죽었다 — **"이 클래스가 실제 QGraphicsItem을 대체하는 프록시"라는 설계를 택했으면,
  새 크래시가 날 때마다 메서드 하나씩 땜질하지 말고 애초에 그 타입에 어떤 메서드들이
  호출되는지(`isSelected`/`setSelected`/`scene`/`mapToScene`/... ) grep으로 전수
  나열해 한 번에 다 갖추는 편이 낫다** — 이번 경우 `isSelected()`가 있다는 사실 자체가
  "짝인 `setSelected()`도 당연히 있다"는 잘못된 안심을 줬다(대칭 메서드 쌍의 한쪽만
  구현된 상태는 특히 위험 신호). 부수 함정 — `setSelected(False)`를 멤버별 단순 루프로
  구현하면 `scene.selectionChanged`가 멤버마다 즉시 발화해 `_sync_group_selection`
  (그룹 멤버 하나라도 선택되면 전체를 함께 선택하는 단방향 규칙)이 중간 상태에 끼어들어
  방금 해제한 멤버를 도로 선택시켜버린다 — `setSelected(True)`는 결과가 우연히 같아
  이 버그를 가려버렸다(단방향 대칭 검증의 함정: 한쪽만 테스트하면 반대쪽 회귀를 놓친다).
  대량선택 때 쓰는 `_bulk_select`(host_selection.py)와 같은 원리로 `blockSignals`로
  루프 동안 신호를 죽이고 끝에 한 번만 재발화해 해결. (`2026-08.md` "TRIM 근본 재설계
  후속 8", 2026-08-30, `tools/diag_run.py`로 진단 — 이 도구 자체가 예외를 잡아버려서
  직접 재현 스크립트로는 크래시 자체가 안 나던 것과 대비된다)
- **"파괴적 확정을 제스처 릴리즈까지 미룬다"는 설계(여러 변을 한 번에 병합하기 위한
  의도적 결정)는, 그 미뤄진 대상을 *다른* 아이템이 같은 제스처 안에서 커터/경계로
  참조하는 순간 "한 번에 이어서 했을 때"와 "따로따로 했을 때"의 결과가 달라지는
  부작용을 낳는다.** 원(닫힌 도형)을 트림하는 커밋 자체는 릴리즈까지 미뤄지지만,
  같은 펜스 드래그로 이어서 지나간 수염(열린 도형, 원을 커터로 삼음)은 원이 "아직
  릴리즈 전이라 원본 그대로"인 상태를 보고 커터가 있다고 판단해 절반만 잘랐다 — 원을
  먼저 트림해 릴리즈(파괴적 확정 완료)한 뒤 수염을 별도 드래그로 지나가면 그 자리
  테두리가 이미 실제로 없어져 커터가 안 걸리고 완전히 다른 규칙(그룹 전체소실)이
  적용됐다. **한쪽만 지연 커밋이고 다른 쪽(또는 제3자의 조회)은 즉시 반영이면, "지연
  커밋 대상을 아직 안 끝난 걸로 볼지 이미 끝난 걸로 볼지"를 조회하는 모든 지점에서
  명시적으로 결정해야 한다** — 이번엔 `_item_local_edges`(커터/경계 조회 공유 지점
  하나)에서 미확정 `_cuts`도 `_destructive_trim_result`로 미리 반영해 "이미 끝난
  것처럼" 보이게 통일했다. 지연 커밋 자체(`finalize_closed_trim`)는 안 건드리고 그걸
  *조회*하는 함수만 고쳐, 지연 설계의 원래 이점(여러 변 병합)은 그대로 유지하면서
  불일치만 없앤 것이 핵심 — 지연 자체를 없애려 했다면 훨씬 큰 재설계가 필요했을 것.
  (`2026-08.md` "TRIM 근본 재설계 후속 9", 2026-08-30, 오프스크린 재현 스크립트로
  수정 전/후 결과를 나란히 비교해 확정)
- **정책을 확정한 근거 예시 하나가 우연히 그 정책이 안 맞는 경우까지 대표하지는
  않는다 — 반대 사례가 나오면 "재확인"이 아니라 "그 축이 맞는 축인지부터" 재검토한다.**
  "다중세그먼트 펜/선은 부분 트림"을 사용자에게 확인받은 근거 테스트가 마침 **펜**
  이었는데, 몇 분 뒤 다른(더 복잡한) 펜 사례("닫힌 루프로 그린 얼굴 윤곽")가 그
  정책과 안 맞아 보이자, 재검토 결과 애초에 갈랐어야 할 축은 "선 vs 펜"이 아니라
  "공학적 폴리라인(케이블·배선, 꺾인 지점마다 의미 있는 변) vs 손으로 그린 펜/SVG
  (하나의 장식 단위)"였다. 첫 확인 질문의 예시가 우연히 "펜"이었을 뿐 실제로 갈라야
  할 기준(선/폴리라인 vs 펜/SVG)과 정확히 겹치지 않았던 것 — **사용자가 확정한 정책도
  그 결정에 쓰인 예시가 바뀌는 케이스(다른 세그먼트 수·다른 열림/닫힘)까지 일반화되는지
  후속 사례로 계속 검증할 것.** 별개로 순수 버그 하나도 같이 발견됐다 — 닫힌 루프
  (시작=끝)인 펜은 애초에 `_open_item_local_pts`가 스코프 밖으로 통째 배제해 예고조차
  안 떴다(2026-08-28 §8 항목17 5단계 결정과는 다른, 후속 6이 도입한 별개 제외 규칙).
  (`2026-08.md` "TRIM 근본 재설계 후속 10", 2026-08-30)
- **정책을 "그룹 소속"으로 게이트하면, 그 정책이 정말 필요한 대상 중 "그룹 밖에
  홀로 있는 것"이 조용히 예외로 새어나간다.** 바로 위 항목에서 "펜/SVG는 전체삭제"를
  확정했지만 구현을 `_trim_allows_full_erase`(그룹 또는 `_trim_derived`) 게이트에
  얹은 채로 했더니, 즉석에서 그린 낱개 체크마크 펜·다각형 도구 선(그룹 없음, SVG
  가져오기가 아니라 방금 이 세션에서 직접 그린 것)이 곧바로 재현됐다 — "펜/SVG
  전체삭제"의 진짜 근거("손으로 그린 하나의 도형이라는 심상모델")는 애초에 그룹
  여부와 무관한 성질인데, 처음 구현할 때 기존 게이트를 그대로 재사용하면서 조용히
  "그룹일 때만"으로 좁아졌다. **정책의 근거가 특정 조건(그룹 소속)과 무관한데
  구현이 그 조건에 얹혀 있으면, 그 조건 밖의 사례가 반드시 다음 재현으로 나온다 —
  새 정책을 기존 게이트에 얹기 전에 "이 게이트가 정말 이 정책의 필요조건인가"를
  먼저 물을 것.** (`2026-08.md` "TRIM 근본 재설계 후속 11", 2026-08-30)
- **"안전장치가 막는 대상"과 "그 안전장치가 진짜 막아야 하는 위험"이 갈라지면,
  나중에 정책이 바뀌어도 안전장치가 여전히 옛 범위 그대로 남는다.** "다중 서브패스
  펜은 통째로 트림 제외"(후속 7)의 진짜 위험은 "부분 절단이 나머지 서브패스를
  `setPath()`로 지워버림"이었는데, 안전장치 자체는 "다중 서브패스 펜"이라는 더 넓은
  범주 전체를 막아버렸다 — 이후 "커터 없으면 펜은 전체삭제"(후속 10~11)로 정책이
  바뀌면서 "오브젝트 전체를 지우는 것"(위험 없음)까지 같이 막혀버린 채 남아 있었다.
  같은 고양이 코 증상이 세 번째 재보고됐을 때(후속 10 닫힌루프, 11 그룹무관, 12 이번)
  **매번 다른 메커니즘**이었다는 게 핵심 — 표면 증상("코가 안 지워진다")의 반복만
  보고 "또 그 버그"로 뭉뚱그리지 않고, 이번엔 사용자가 준 실제 `.ecad` 파일을 직접
  로드해(`load_document`) 정확한 오브젝트 구조(서브패스 3개)를 확인한 뒤에야 세
  번째 원인이 진짜 다르다는 걸 알 수 있었다 — **같은 표면 증상이 반복 보고되면
  추측을 더 쌓지 말고, 재현 가능한 실물(파일·스크린샷)을 확보해 구조를 직접 확인할
  것.** (`2026-08.md` "TRIM 근본 재설계 후속 12", 2026-08-30)

## 라우팅(A*/직교 엘보)
- A* 격자(Hanan grid)에 화살표 자신의 좌표를 노드로 넣으면, 다른 화살표와의 교차점에 노드가
  얹혀 회피 벌점이 "눈멀게" 된다 — 회피 레인은 도형 팽창 모서리만으로 충분. (`2026-07.md`
  화살표-화살표 교차 회피)
- "부재"(증상: 안 나온다·안 걸린다)를 조사할 땐 **발생한 사례가 아니라 차단된 지점**을 찾아야
  한다. 게이트 플래그를 로그에 심어도, 발화한 줄만 grep하면 차단당해 안 찍힌 줄을 놓친다 —
  스턱루프 3라운드의 근본원인. (`2026-08.md` 스마트 정렬 가이드, 전역 규칙 11-b 「부재 확인」)
- 매 프레임(드래그 중) 국소 재정렬로 경로를 patch하는 접근은, 서로 다른 프레임의 판단이 서로의
  결과를 덮어쓰는 구조라 다중 프레임 드래그에서 표류한다 — "새로 그리기와 동일하게 전체
  재계산"으로 메커니즘 자체를 바꿔야 안정된다(3차 시도 끝에 확정). (`2026-08.md` 8포트 확장)
- 드래그 재설계로 점 개수가 매 프레임 바뀔 수 있게 되면, press 시점에 캡처한 "몇 번째 점"
  인덱스가 다음 프레임엔 범위 밖(IndexError)이 될 수 있다 — 인덱스는 매 프레임 유효 범위로
  재계산. (`2026-08.md` 8포트 확장, 끝점 드래그 크래시)
- "승자 하나 고르기" 안정화 규칙은, 두 후보의 거리가 **수학적으로 항상 일치**하는 경우(같은
  크기 도형의 좌/중심/우)엔 미세조정이 아니라 상시 발동하는 규칙이 되어 특정 정렬(변 정렬)이
  원리적으로 표시 불가능해진다 — 동률이면 하나만 고르지 말고 역할 전부를 동시 표시.
  (`2026-08.md` 스마트 정렬 가이드)
- 인접(마주보는 변) 후보에만 "교차축 범위가 겹쳐야 후보 인정" 게이트를 걸면, 두 도형이 정확히
  일치해도 게이트가 안 열려 후보에 못 오른다 — 같은 역할 매칭엔 이 게이트가 없는데 인접에만
  건 비대칭이 원인. 게이트 완화(+여유)가 아니라 **제거**가 정답이었다. (`2026-08.md` 스마트
  정렬 가이드)
- 그리드 A*에서 실좌표 시작/끝점(포트 등)을 `Math.round(x/cellSize)`로 그리드 셀에 스냅할 때,
  두 축을 독립적으로 반올림하면 원래 축 정렬(수평/수직)이었던 진입 방향이 반올림 오차로 대각선이
  될 수 있다 — 진입 방향과 무관한 축까지 통째로 반올림하는 게 원인. 첫/끝 구간에 직각 코너를
  강제 삽입(`fixOrthogonal`)해야 항상 축정렬이 보장된다. Python/Qt A*가 아니라 JS 프로토타입
  (`web_prototype/astar.js`)에서 발견 — 좌표 반올림이 있는 격자 라우팅이면 언어 불문 재발
  가능. (`2026-08.md` 웹 전환 실험)
- A* 탐색이 아예 실패하는 경우(예: start/end가 `bounds` 밖)의 폴백 경로는 위 `fixOrthogonal`을
  거치지 않고 원시 좌표를 그대로 반환하면 대각선 한 방으로 장애물을 관통한다 — "찾은 경로"와
  "폴백 경로"를 별도 반환문으로 나눌 땐 축정렬 정규화를 둘 다에 적용해야 한다. 대규모(도형
  17개·화살표 22개) 자동 스트레스 테스트 중 발견. (`2026-08.md` 웹 전환 실험 — 인터랙션
  스트레스테스트)
- **[Python/Qt 본체, 2026-08-07 재현·해결] `_on_scene_changed`(host_canvas.py)의 "region이
  화살표 bbox와 겹치면 reroute" 필터는 순수 선택(클릭, 이동 없음)의 선택강조 리페인트 region도
  "겹침"으로 잡는다** — 실제 도형이 안 움직여도 트리거된다. 화살표가 캔버스 폭넓게 뻗은
  밀집 도면(장애물 18개짜리 KBS 실도면 변환본)에서는 이 리페인트 region이 거의 모든 화살표
  bbox와 겹쳐, 클릭 5회 → `reroute()` 39회 → `_astar_ortho` 174회 → 총 56.7초(cProfile 실측,
  `docs/history/2026-08.md` "§8 항목15 실사용 피드백" 참조)로 이어졌다. 바로 아래 웹
  프로토타입 항목과 같은 계열의 함정(관련 없는 리페인트/이동이 전체 재계산을 유발)이 Python
  본체에서도 재현된 것 — "선택했을 뿐인데 재계산이 도는가"는 두 스택 모두에서 재발 가능한
  패턴으로 간주. **해법**: `boundingRect()`는 선택되면 핸들 여백만큼 커져(`_HandleResizeMixin`
  공통 동작) `sceneBoundingRect()`로 "실제 변경 여부"를 판정하면 선택 자체가 이미 오탐이 된다
  — 핸들 여백이 빠진 `_content_rect()`(도형별 override, 타이트 경계)를 씬좌표로 매핑해 매
  `_on_scene_changed` 호출마다 이전 스냅샷과 비교하고, 실제로 달라진 아이템의 rect 합집합만
  화살표 bbox 겹침 판정에 쓰도록 교체(`host_canvas.py` `_sync_geom_snapshot`). region=None(
  테스트 강제호출)은 기존처럼 무조건 전체 재검토 유지. 결과: 같은 재현 스크립트로 5클릭
  0.59초(120배)·`reroute()` 0회, 스모크 387종 전원 통과. 검증 중 발견: `load_document` 직후
  바인딩 정규화 reroute의 지연 `scene.changed` 신호가 **두 번째** `processEvents()` 틱에야
  전달된다 — 실사용은 마우스 클릭이 도착하기 전에 이미 소진되는 1회성 정착 비용이라 무해하지만,
  벤치마크 스크립트가 이 정착을 클릭 1회차 비용으로 오산할 수 있어 `tools/profile_reroute.py`도
  로드 후 `processEvents()` 1회를 추가해 정착시킨 뒤 측정하도록 함께 수정.
- **[Python/Qt 본체, 2026-08-08 재발·해결] 위 수정 이후에도 화살표 자신의 세그먼트(직각 알약
  핸들)를 드래그하면 여전히 프레임당 최대 ~600ms씩 버벅였다** — 이번엔 선택 오탐이 아니라
  **진짜 지오메트리 변경**(드래그 중인 화살표 자신의 `_pts`)이라 `_on_scene_changed`가 정확히
  트리거된 것까지는 맞았지만, 그게 유발한 재계산 자체가 낭비였다: `_obstacle_rects()`는 도형
  (`_RectItem`/`_EllipseItem`/`_SymbolItem`)만 반환하고 화살표는 절대 포함하지 않으며(2026-07-26
  "화살표-화살표 soft 회피 철회" 결정, `avoid_segs`/`cross_penalty`는 죽은 훅으로 남음),
  바인딩도 "도형만 지속 바인딩"이라 화살표는 다른 화살표의 어떤 바인딩 대상도 될 수 없다 —
  즉 **화살표 A가 움직여도 화살표 B의 라우팅 입력은 하나도 안 바뀌므로, B를 reroute해 봐야
  항상 같은 결과만 다시 계산하는 순수 낭비**였다. 해법: `_sync_geom_snapshot`이 화살표
  (`_ArrowItem`/`_PolyArrowItem`) 자신의 지오메트리 변경을 아예 집계하지 않도록 제외(도형
  변경은 그대로 집계 — 도형이 화살표 경로에 끼어드는 실제 회피는 보존). 결과: 같은 밀집
  도면(kbs_1tv_test.ecad) 최악 케이스 세그먼트 드래그가 프레임당 ~623ms → ~6.16ms(100배+).
  같은 부류 함정을 또 만나면(이 도면에서 뭔가를 드래그·이동할 때마다 버벅임) "무엇이 바뀌었을
  때 이 아이템의 라우팅 *입력*이 실제로 달라지는가"부터 먼저 확인 — region/bbox가 겹친다고
  다 트리거할 필요는 없다.
- **`_route_ortho`의 clearance 사다리(4단×스텁유무 2가지)는 "이 조합은 원래 못 찾음"(None)을
  정상 결과로 기대하며 최대 8~10회 `_astar_ortho`를 던지는 구조다** — None이 뜬 이유를
  "장애물/회랑이 부족해서"로 짐작하고 "실패하면 더 큰 범위로 재시도"하는 안전망을 걸면,
  원래도 실패할 예정이던 호출마다 비용이 그대로 2배가 된다(2026-08-08 실측: 도형드래그
  중앙값 146ms→447ms로 역효과). 이 코드에서 "실패=입력 부족"을 전제하는 재시도 로직을 쓰기
  전엔 먼저 장애물을 그대로 다 줘도 여전히 실패하는지부터 디버그로 확인할 것. (안전하게
  장애물 후보를 줄이려면 재시도 없이 "축소해도 성공/실패 여부 자체는 안 바뀐다"를 별도
  전/후 대조 스크립트로 직접 검증한 뒤 적용 — `_astar_ortho`의 시작~도착 주변 회랑 사전필터
  참조, `docs/history/2026-08.md` "도형 드래그 잔여 지연" 항목.)
- 매 pointermove마다 전체 화살표를 재계산하는 방식(무관한 도형 이동도 장애물이 될 수 있어
  도입)은 도형 17개·화살표 22개 규모에서 이미 프레임당 평균 21.8ms(60fps 예산 16.7ms 초과)까지
  올라간다 — "드래그 중엔 연결된 화살표 + 이동한 도형과 bbox가 근접한 화살표만 재계산하고,
  드롭 순간에만 전체 재계산 1회로 정확성 보장"으로 바꿔 20개/31개 규모에서 프레임당 평균
  2.15ms(10배 개선)로 해결. 다만 이 근접(bbox margin) 판정은 휴리스틱이라, 아주 먼 거리를 빙
  돌아가는 화살표 경로 위로 무관한 도형이 옮겨지는 극단적 케이스는 드래그 도중엔 놓칠 수
  있음(드롭 시점엔 항상 correct) — 실사용에서 체감되면 재검토. (`2026-08.md` 웹 전환 실험 —
  인터랙션 스트레스테스트)
- **[수정 완료, §8 항목19 F1, 2026-08-14] 제3자 장애물이 연결 도형에 밀착하면 A*가 관통
  경로를 조용히 채택한다** — 장애물이 시작/끝 도형 중 하나와 거의 맞닿아 있으면, 스텁
  이탈점이 그 장애물의 팽창 사각형 **안**에 갇힌다(반대 방향엔 도형 자신의 몸통이 있어
  격자에 그쪽 좌표가 아예 없음 — 도형 자신은 라우팅 obstacle 목록에서 제외되기 때문).
  클리어런스 사다리 4단×스텁유무 2가지 전부 `None`을 반환해 최초의 관통 경로로 폴백한다.
  간격을 스윕해 확인한 발동구간: 0~10유닛(관통)·13~20유닛(정상 우회)·30유닛(반대쪽 도형이
  가까워져 다시 관통) — "장애물 크기"가 아니라 "장애물이 두 연결 도형 중 하나와 너무
  가깝다"가 조건. 리포에 있는 실제 KBS 도면(`kbs_1tv_test.ecad`)에도 경미한 형태로 이미
  1건 존재 확인. **1차 시도(실패)**: 스텁을 장애물에 안 걸릴 때까지 같은 방향으로 더 밀어내는
  방식은 무효했다 — 원래 부착점→밀린 지점까지의 스텁 구간 자체가 여전히 장애물을 관통해야
  하므로 "더 멀리 밀기"가 구조적으로 의미 없었다(장애물이 부착점 바로 옆에서 시작해 부착점보다
  크게 뻗어 있는 경우). **2차 시도(채택)**: `_astar_ortho(s,e,[wall],clearance=0)`을 스텁 없이
  직접 호출하면 성공한다는 걸 발견 — clearance로 안 부풀린 **원본** 경계라면 시작점이 경계에
  딱 붙어 있을 뿐 "안"은 아니라서(`_seg_hits_rect`의 접촉=통과 규약) A*가 격자 노드를 하나
  얻어 우회를 찾는다. `_route_ortho` 끝에 "사다리를 다 돌고도 여전히 관통이면, 그 장애물만
  원본 경계로 최후수단 재시도" rung 추가로 해결(안전 여백은 포기 — 벽에 바짝 붙어 지나감,
  그래도 관통보다는 낫다는 판단). 재현·수치·성능 대조: `docs/route_review_2026-08.md`
  3·6단계.
- **[수정 완료, §8 항목19 F2, 2026-08-14] 부분선택 그룹드래그가 화살표 있는 문서에서
  8배+ 느렸다(82.92ms)** — 원인 오진단 정정: 처음엔 "클리어런스 사다리(A*)의 반복
  `_path_hits_rects`"로 봤는데, 실제로는 `_route_ortho`가 **매 호출마다 무조건** 도는
  "preferred가 장애물을 관통/재진입하는지" 사전체크가 지배 비용이었다(cProfile로
  `_astar_ortho` 호출 수를 직접 세어보니 이 시나리오에선 사다리가 한 번도 안 돌았음 —
  "많이 불리는 함수가 범인"이라는 직관이 실제 호출 여부 확인 없이는 틀릴 수 있다). 해법:
  `_astar_ortho`가 매번 인라인 계산하던 회랑(코리도) 계산을 `_corridor_rect()`로 추출해
  `_route_ortho` 시작부에서 장애물 목록(`infl`)을 함수당 1회만 걸러 이후 모든 체크가 좁혀진
  목록을 쓰게 함. **⚠ 구현 중 자체 발견·즉시 수정**: 처음엔 `[r for r in infl if r.
  intersects(_corridor_rect(s,e,clearance))]`처럼 **리스트 컴프리헨션 조건절 안에서** 그
  함수를 호출해, 결과가 각 원소와 무관한데도(loop-invariant) 원소마다(장애물 50개면 50번)
  재계산됐다 — 절약한 비용을 정확히 상쇄해 개선 효과가 안 나타났다. cProfile로 그 함수의
  호출 수가 의도(함수당 1회)보다 50배 많은 걸 보고 원인을 추적, 루프 밖에서 `_corr = ...`로
  한 번만 계산하도록 고치자 실제 효과(cProfile 1.939→1.109초, 2.05배)가 나타났다. **교훈:
  컴프리헨션/제너레이터 조건절에 함수 호출을 직접 쓰면 원소 수만큼 반복된다 — 그 결과가
  루프 변수와 무관하면 반드시 밖에서 1회만 계산해 이름에 담아 넣을 것.** 실측·검증 전체:
  `docs/route_review_2026-08.md` 7단계.
- **[Python/Qt 본체, 2026-08-14 재발·해결, F3] `_seg_hits_rect(a,b,r)`이 같은 세그먼트
  (a,b)를 여러 사각형과 반복 대조하는 호출부(`_path_hits_rects`의 rects 루프,
  `_astar_ortho_grid.edge_ok`의 candidate 루프)에서 매 사각형마다 a.y()/b.x() 등 QPointF
  접근·축판정(수평/수직)·좌표정렬을 처음부터 다시 계산했다** — 바로 위 `_corridor_rect`
  항목과 같은 "루프불변 재계산" 계열이지만, 이번엔 컴프리헨션이 아니라 **함수 재호출** 자체가
  원인(함수가 세그먼트+사각형을 한 번에 받는 시그니처라, 호출부가 사각형마다 통째로 다시
  부르는 것 말곤 방법이 없었다). 사다리(클리어런스 A*)가 실제로 도는 새 스트레스 픽스처
  (`tools/route_ladder_stress.py`, 기존 `route_stress.ecad`는 간격이 넉넉해 사다리가 안
  돎)로 cProfile 실측: `_seg_hits_rect` 133,851회, tottime 0.755초(전체 3.165초의 24%).
  해법: 세그먼트의 축판정 결과를 `_seg_probe(a,b,eps)`로 1회만 뽑아 `_probe_hits_rect
  (probe,r,eps)`(사각형만 받는 저비용 판정)에 반복 전달 — 호출부가 세그먼트당 1회만
  `_seg_probe`를 부르고 사각형 루프는 가벼운 함수만 반복하도록 재구성. `edge_ok`는 그리드
  간선이 이미 ay==by/ax==bx로 축이 확정된 상태(cands를 고르는 조건과 동일)라 QPointF 생성
  자체도 생략 가능했다. **교훈: "함수 하나가 두 인자를 받고 그중 하나만 루프마다 바뀐다"는
  시그니처 자체가 루프불변 재계산의 신호 — 바뀌지 않는 인자 쪽을 먼저 "준비"하는 별도
  함수로 분리하면 반복 계산을 제거할 수 있다.** 실측·검증 전체: `docs/route_review_2026-08.md`
  9단계.
- **[Python/Qt 본체, 2026-08-14 시도·되돌림, F4] `_obstacle_rects()`의 `scene.items()`
  전체스캔을 라우팅 코리도로 좁힌 `scene.items(rect, IntersectsItemBoundingRect)` BSP
  쿼리로 바꾸면 빠를 것이라 가정했으나 실측은 정반대였다(오히려 3배 이상 느려짐)** —
  `_on_scene_changed`(host_canvas.py)가 2026-08-08에 쓴 검증된 패턴이라 "그대로 재사용하면
  이길 것"이라 예상했지만, 두 함수의 조건이 달랐다: `_on_scene_changed`는 "변경 개수가
  적을 때만" 공간쿼리를 쓰는 조건부 폴백이 있었는데(넓은 영역엔 전체스캔이 더 빠름을 이미
  실측해 둠), `_obstacle_rects()`는 화살표 1개를 라우팅할 때마다 불리는 데다 코리도 패딩
  (`_CORRIDOR_PAD_MIN`=400, 최소 여유)이 실사용 규모 도면 대부분을 이미 통째로 덮어 후보를
  거의 못 줄인다 — 즉 `_on_scene_changed`의 "넓은 영역" 나쁜 케이스가 이 함수에선 **상시
  케이스**였다. 게다가 Qt의 `items(rect, mode)`는 BSP 트리가 좁힌 후보 전부의
  `boundingRect()`를 계산해 교차를 재확인하는데, 이 프로젝트의 `boundingRect()` 구현이
  가볍지 않다(2026-08-13 "200개+ 그룹드래그" 조사에서 확인된 `_qc_dot_rects()`→
  `_shape_ports()`→`_nearest_border()` 체인) — cProfile로 `items()` 호출 자체가 전체
  비용의 43%까지 치솟는 것을 확인. **교훈: 검증된 최적화 패턴이라도 "조건부였는지, 무조건
  적용했는지"까지 그대로 옮겨야 한다 — 원 코드가 조건부였다면 그 조건이 새 호출부에서도
  성립하는지 먼저 실측해야 한다(성립 안 하면 그 패턴의 '나쁜 쪽 케이스'를 상시로 켜는 셈).**
  구현→실측→되돌림 전체: `docs/route_review_2026-08.md` 9단계.

- **A\* 경로가 「아이템 열거 순서」에 민감하다 — 순서만 바뀌어도 도면 전체가 다시 그려진다.**
  `setItemIndexMethod(NoIndex)`를 성능 노브로 검토하다 발견(2026-08-15). 인자 없는
  `scene.items()`는 `BspTreeIndex`와 `NoIndex`에서 **집합은 같고 순서만 다른데**, 그 순서
  차이만으로 화살표 500개의 경로가 **전부** 달라졌다(최대 4620px 이동). 공간쿼리
  `items(rect)` 쪽은 36/36 집합·순서 모두 완전 동일했으므로 범인은 전체 열거를 쓰는 장애물
  스캔이다. 대조 실험으로 확정: BSP 유지 + 전면 재라우팅은 **경로 불변**(재라우팅 자체는
  멱등), NoIndex로 바꾸면 500/500 변경, 다시 BSP로 되돌리고 재라우팅하면 **원본으로 정확히
  복귀**. 지금 실사용엔 영향이 없지만(인덱스가 고정이라 열거 순서가 안정적), **열거 순서에
  영향을 주는 변경은 무엇이든 도면을 조용히 다시 그린다** — 씬 구성 방식·z값·Qt 버전 업까지
  포함. 라우팅 관련 최적화를 검토할 땐 "이 변경이 아이템 순회 순서를 건드리는가"를 먼저
  묻고, 건드린다면 성능 이전에 경로 지문부터 대조할 것.
  (`docs/perf_plan_500_1000.md` §4 2-E, §7)
- **"2점짜리로 테스트하면 절대 못 잡는" 하드코딩 인덱스 버그**: `_arrows_bound_to`(도형
  스왑 재바인딩 대상 탐색)가 `_PolyArrowItem`의 머리쪽 인덱스를 하드코딩된 `1`로 돌려줬다
  — 직선(2점, `len(pts)-1 == 1`)은 우연히 맞았지만 A* 자동배선으로 꺾인 화살표(4점 이상,
  실제 머리는 `len(pts)-1 == 3`)에서는 어긋났다. `set_bound(idx,...)`가 `idx==0` 또는
  `idx==len(self._pts)-1`만 인식하고 그 외는 **조용히 무시**하는 설계라(경고·예외 없음),
  스왑 후 재바인딩이 아무 효과 없이 실패해 화살표 머리가 삭제된 옛 도형을 계속 가리켰다
  ("종류에서 도형 바꾸면 화살표 머리쪽만 자석이 떨어진다"는 실사용 보고). 재현 함정: 직접
  구성한 재현 스크립트 3회가 전부 실패했는데, 매번 `arr.set_points(p1, p2)`로 만든 2점짜리
  화살표를 썼기 때문 — 이 클래스의 버그는 **점 개수가 3개 이상일 때만** 드러난다. 원인은
  결국 진단 로그(`easycad_debug_shapes.log`)의 `sh.scene()=None`(바인딩이 삭제된 아이템을
  여전히 가리킴) 한 줄로 확정됐다 — 재현 실패가 이어지면 "만든 재현이 진짜 사용자 경로와
  같은가"부터 의심할 것(여기선 "직선 vs 꺾인 경로"가 숨은 변수였다). 진단 로그를 여러
  파일로 안 나누면(이번엔 core_view.py의 기존 `_dbg`와 파일을 공유했다가 pytest 851종
  실행으로 오염돼 사용자 재현 기록을 못 찾는 사고도 났다) 셀프 오염도 주의.
  (`docs/history/2026-08.md` "화살표 스왑 머리쪽 재바인딩 버그")
- **`_dedup_pts`의 공선(collinear) 판정이 "직선 위"만 보고 "구간 안"은 안 봤다** —
  외적(cross product) 0은 세 점이 한 직선 위라는 것만 보장하지, 중간 점이 양끝 *사이*에
  있다는 것까진 보장 안 한다. `_route_ortho`의 진입/이탈 스텁(도형 밖으로 `conn_clear`
  ≈36px만큼 밀었다가 안쪽으로 되꺾이는 형태)이 대칭 포트쌍(예: 남↔북처럼 법선이 서로
  마주보는 두 포트)에서 특정 근접도가 되면, 되꺾인 지점이 이웃 두 점과 여전히 "같은
  직선" 위에 있어(값 자체는 그 직선 구간 반대편으로 넘어간 반전점인데도) "불필요한
  경유점"으로 오판돼 제거됐다 — 의도한 36px 여백이 통째로 증발하고 화살촉이 꺾임에
  거의 맞닿아 보였다. 수정은 betweenness 체크(`min(a,c) ≤ b ≤ max(a,c)`) 한 줄
  추가뿐(2026-08-31, `docs/history/2026-08.md` "화살표 속성 리뷰 인계 항목2"). **교훈:
  '공선이면 지운다'류 dedup 로직은 전부 이 함정을 안고 있다 — 방향 반전(되돌아오기)이
  가능한 경로(스텁·후크 형태)를 다루면 공선 판정에 항상 betweenness를 같이 걸 것.**
- **위 수정의 직후 후속 — "고쳐진 줄 알았던" dedup 수정이 예전에 조용히 지워주던 진짜
  기하 결함(두 스텁의 escape 거리가 좁은 간격에서 서로를 지나침)을 그대로 드러냈다.**
  `_own_stub`은 부착 도형이 있으면 방향·간격 무관하게 항상 conn_clear(≈36px)만큼 자기
  도형 밖으로 미는데, 두 부착 도형의 실제 간격이 conn_clear×2(72px)보다 좁으면 양쪽
  스텁이 상대 도형의 영역까지 밀고 들어가 순서가 뒤바뀐다 — `_ortho_elbow`의 중간점(두
  스텁 좌표의 평균)이 이 뒤바뀐 순서 안에서 계산되니 진행 방향을 거스르는 반전점이
  생긴다. 예전엔 위 `_dedup_pts` 버그가 이 반전점을 "공선이니 불필요"로 지워버려 결과가
  그냥 '짧은 스텁'처럼 보였을 뿐 — dedup을 바로잡자 원래 있던 기하(180도에 가까운 되돌아
  오기 루프)가 그대로 드러났다. 수정: 같은 축(H-H/V-V)의 마주보는 포트쌍일 때만 escape
  거리를 `min(conn_clear, 실제 간격/2)`로 상한(간격이 충분하면 무회귀). **교훈: 오래
  묵은 dedup/정리 로직을 고치면, 그 로직이 그동안 가려온 '진짜' 기하 결함이 새로 드러날
  수 있다 — 수정 직후엔 "숨겨졌던 것이 이제 보이는 건 아닌가"를 한 번 더 의심할 것.**
  (2026-08-31, `docs/history/2026-08.md` "화살표 속성 리뷰 인계 항목2 후속")

## 렌더링(QPainter/QIcon/오프스크린)
- 오프스크린 플랫폼 플러그인(`QT_QPA_PLATFORM=offscreen`)에서 `QPainter`의
  `CompositionMode_SourceIn`/`DestinationIn`으로 SVG 아이콘을 런타임 재칠하면, 짧은 시간에
  CanvasWindow를 수십 개 생성하는 스모크 테스트에서 재현 가능한 네이티브 세그폴트가 났다 —
  QPixmap↔QImage 전환·premultiplied 포맷 전환 등 4가지 변형을 다 시도해도 재현됐고, 캐싱으로
  호출 빈도를 줄여도 재현됨(호출 "횟수"가 원인이 아니었음). 해법: 합성모드를 아예 안 쓰고
  `QImage.setPixelColor()` 픽셀 루프로 RGB만 교체 — 상세와 시도한 변형 전부는 wiki
  `easycad-svg아이콘재칠-QPainter합성모드-세그폴트.md` 참조. (`2026-08.md` 아이콘 색 중립화)
- `QPen`의 **기본 joinStyle은 `BevelJoin`**이다 — 명시하지 않고 예각 폴리곤을 `drawPolygon`
  하면 꼭짓점이 모따기된다. 화살촉 삼각형(30° 어깨)에서 실제로 어깨가 45°로 잘렸고,
  `_ArrowItem`은 처음부터 `RoundJoin`을 명시해 둬 멀쩡했기에 "직각 화살표만 깎인다"로 보였다.
  펜을 만들 땐 capStyle·joinStyle을 항상 명시할 것. wiki
  `easycad-qpen-기본joinstyle-예각폴리곤-모따기.md` 참조. (`2026-08.md` Lucid 대조 UX 시리즈)

- `QGraphicsItem.setCacheMode(DeviceCoordinateCache/ItemCoordinateCache)`는 "작은 아이템이
  아주 많은" 밀집 도면(1600개)에서 **직관과 반대로 렌더를 크게 느리게 만든다** — render_fit
  실측: 캐시 없음 78ms → DeviceCoordinateCache 165ms(2.1배 악화, 캐시 워밍 후에도 동일)
  → ItemCoordinateCache 700ms(9배 악화). 아이템 하나하나가 화면에서 몇 픽셀에 불과하면
  아이템별 QPixmap 캐시 생성·관리·합성 오버헤드가 원래의 벡터 drawPath 비용보다 커진다(캐시는
  "적은 수의 무거운 아이템"에 맞는 최적화이지 "많은 수의 가벼운 아이템"엔 역효과). 픽셀 지문도
  당연히 깨진다(3px 샘플 그리드에서 378개 픽셀 차이). 이 축은 재시도하지 말 것 — 대신
  `_geom_version` 키 메모이즈(이미 `_content_rect`/`boundingRect`/`_head_points`/
  `_trimmed_body_pts`에 적용, 2026-08-08~09)처럼 "Qt 캐시 기능"이 아니라 "우리 쪽 파이썬
  재계산을 건너뛰는" 방향이 이 도면 규모엔 맞는 축이다. (`2026-08.md` §8 항목0 4단계)
- 저배율(전체축소)에서 안티에일리어싱을 꺼도(painter.setRenderHint(Antialiasing, False)를
  모든 item.paint()의 무조건 재호출을 몽키패치로 우회해 실측) render_fit 성능 이득은
  81.71→76.84ms(오차범위, 6% 미만)뿐인데 확대 크롭 스크린샷으로 대조하면 도형 테두리·연결선에
  **눈에 띄는 계단현상(staircase)**이 생긴다 — 이득 대비 화질 손실이 뚜렷이 불리해 이 축은
  폐기. "AA 끄면 빨라진다"는 일반 통념이 이 아이템 규모·zoom 조건에선 안 맞았다(대부분의
  시간이 AA 자체가 아니라 QPainterPath 구성·객체 생성에 있었던 것으로 추정). (`2026-08.md`
  §8 항목0 4단계 후속 — AA 시각비교)
- **`QApplication.setStyle("Fusion")`을 이미 Fusion인데도 매 `CanvasWindow()` 생성마다
  무조건 재호출하면, 수백 개 창을 안 닫고 누적 생성하는 스모크 스위트 후반부에서 Windows
  access violation이 난다.** `setStyle()`은 그 순간 살아있는 위젯 전부를 새 스타일로
  재polish하는 무거운 전역 연산 — 평소엔 안 보이다가 창당 위젯 수가 늘면(좌측 패널 아코디언
  개편으로 +50개/창) 정확히 같은 자리(스위트 487~497번째 테스트, 매번 동일)에서 재현됐다.
  `faulthandler.enable()`로 잡은 크래시 지점이 정확히 이 줄이었다 — 위젯 수를 늘린 새 코드
  자체는 무관하고, **기존에 잠재해 있던 재현 조건(반복 호출)을 창당 오버헤드 증가가 앞당긴
  것**이 전부였다. 진단 순서: GDI/USER 핸들 수(`GetGuiResources`/`GetProcessHandleCount`,
  둘 다 오프스크린에선 0으로 무의미)·widget 참조 순환(`gc.collect()`로 반박됨, 항상 완전
  회수됨)·이분탐색(part 1~9 조합별로 재현 최소 세트를 좁힘, part7 신규 테스트는 무관하다고
  증명)까지 전부 헛다리였고, **`faulthandler`로 실제 크래시 줄을 바로 찍는 것이 가장 빨랐다**
  — 다음에 원인불명 세그폴트를 만나면 추측성 이분탐색보다 이 방법을 먼저 쓸 것. 해법은
  `app.style().objectName().lower() != "fusion"`으로 멱등화(idempotent)하는 한 줄. 부수
  발견: 이 재호출이 실은 테스트 중이던 별개의 버그(좌측 패널 아코디언 접기/펼치기 왕복 시
  패널이 원래 크기로 안 돌아오는 문제)를 우연히 가려주고 있었다 — `setStyle()`의 전역
  재polish가 내 수동 `invalidate()`/`activate()` 체인이 놓친 레이아웃 갱신을 부수효과로
  대신 처리해 준 것. 멱등화 후 그 버그가 드러나 별도로 고쳤다(원인은 무관한 곳: `QListWidget`
  커스텀 행의 `sizeHintForRow()`가 `show()` 직후 첫 `.height()` 접근 전까지 실제보다 큰 값을
  낸다 — 테스트가 그 접근을 먼저 강제해야 왕복 비교가 안정적). **"우연히 통과하던 테스트"는
  무관해 보이는 다른 수정으로 갑자기 깨질 수 있다 — 그 자체가 감춰진 의존성의 신호.**
  (2026-08-12, 좌측 패널 아코디언 개편)
- **새 예고 마커를 기존 핸들과 "같은 자리·같은 크기·같은 색"으로 얹으면 자동 테스트는 다
  통과해도 실제 화면에선 아무 변화가 없다.** 화살표 끝점을 뗐다 다시 붙이는 드래그에
  `_draw_snap_marker`(반경 5/s 파란 원)를 켰는데, `_HANDLE_PX`가 애초에 그 마커와 지름이
  같게 설계돼 있어(주석에 명시돼 있었는데 놓침) 끝점 핸들 사각형과 완전히 겹쳤다. 게다가
  `drawForeground`가 아이템의 `paint()`보다 나중에 그려져 그 마커가 (다른 수정으로 바뀐)
  핸들 색까지 위에서 덮어버렸다 — 디버그 로그로 "매 프레임 정상 계산·페인트됨"까지 확인해
  놓고도 "그래서 왜 안 보이냐"는 재보고를 받았다. 새 시각 신호를 기존 요소 위에 겹쳐 그리기
  전에 그 요소의 위치·크기·색을 먼저 확인할 것 — 안 겹치면 다행이지만 겹치면 새 마커 대신
  **기존 요소 자체의 속성(색 등)을 바꾸는 쪽**이 항상 눈에 띈다. (2026-08-19, 화살표 끝점
  재부착 예고점)
- **`QScrollArea`(=`QAbstractScrollArea`)에 QSS `border`+`border-radius`만 주면 꼭짓점이
  각져 보인다.** 일반 `QFrame`은 기본 `frameShape`가 이미 `NoFrame`이라 QSS `border`만
  줘도 그대로 둥글게 그려지지만(Mermaid `preview_frame`이 그 경우), `QScrollArea`의 기본
  `frameShape`는 `StyledPanel`(사각 네이티브 프레임)이라 그 프레임이 QSS 둥근 테두리
  위에 겹쳐 그려져 모서리만 사각으로 튀어나온 것처럼 보인다. `setFrameShape(QFrame.Shape.
  NoFrame)`을 먼저 호출해 네이티브 프레임을 끈 뒤 QSS `border`를 줘야 한다 — 같은 QSS를
  다른 QFrame 서브클래스에 복붙할 때 서브클래스별 기본 `frameShape`가 다를 수 있음을
  기억할 것. (2026-08-20, SVG 에셋 생성창 후보 갤러리 테두리)
- **`QGraphicsScene.render(painter, target, source, KeepAspectRatio)`는 종횡비가 안 맞으면
  남는 여백을 target 좌상단으로 몰아준다 — 가운데 정렬이 아니다.** Qt 문서에 명시돼 있지
  않고, "AspectRatioMode"라는 이름이 정렬까지 다뤄줄 것 같은 착각을 준다. 정사각 소스를
  가로가 긴 target에 렌더하는 최소 재현으로 실측 확인(검정 배경 위 빨강 target을 그리면
  콘텐츠가 완전히 왼쪽에 붙고 남는 여백이 전부 오른쪽에만 생김). 가운데 정렬이 필요하면
  `target` 자체를 source와 같은 비율의 중앙 부분 사각형으로 미리 좁혀서 넘겨야 한다(직접
  가운데로 옮기는 API는 없음). PDF/PNG/SVG 미리보기·내보내기가 전부 이 함수를 공유해 셋
  다 한 번에 걸려 있던 "여백이 이상하다"는 실사용 피드백의 원인이었다. (2026-08-20, PDF
  내보내기 여백 중앙정렬 버그)
- **다른 세션(다른 창)이 같은 리포에서 동시에 작업 중일 수 있다 — 커밋 전 `git log`로
  HEAD가 세션 시작 시점과 같은지 반드시 재확인할 것.** 세션 시작 스냅샷엔 없던 커밋이
  중간에 `git log`에 나타났고(다른 모델 공저 트레일러로 다른 Claude Code 세션임을 확인),
  마침 같은 파일 3개(아이콘 크기·색 관련)를 건드리고 있었다 — 두 세션이 겹쳐서 편집을
  덮어썼는지 `git diff`를 hunk 단위로 전부 대조해서야 안전을 확인했다(다행히 서로 다른
  줄이라 클린하게 레이어링됨). Edit 도구는 매 호출마다 디스크의 실제 현재 내용을 상대로
  매칭하므로 조용한 데이터 유실은 안 났지만, 세션 시작 시 한 번 읽은 파일 내용을 그 이후로도
  "내가 알고 있는 최신 상태"로 착각하면 안 된다 — 특히 커밋 직전엔 `git status`/`git log`로
  HEAD가 안 바뀌었는지, 바뀌었다면 겹치는 파일의 diff를 사람이 검토하듯 다시 확인한다.
  (2026-08-20, 실사용 피드백 9건 + 후속 3건)
- **QMenu 구분선을 QPalette로 못 고친다**: Fusion 스타일의 `CE_MenuItem` 분리선 렌더는
  `R.Mid`를 포함해 시도한 어떤 `QPalette.ColorRole`도 안 읽는다(빨간색 강제 대입 실측으로
  확인 — 반영 안 됨). `QMenu::separator` QSS만 먹힌다. 단, `app.setStyleSheet()`(전역)나
  `self.setStyleSheet()`(메인윈도우)에 걸면 위 "조상 스타일시트→QStyleSheetStyle 강제전환"
  함정이 **가장 넓은 범위로 재발**한다(오프스크린 스위트에서 무관해 보이는 속성패널 스핀박스
  테스트가 깨짐) — QMenu는 팝업 최상위 위젯이라 **인스턴스 각각**(`menu.setStyleSheet(...)`)
  에 걸면 다른 위젯에 안 번짐(실측 확인). 생성 지점이 여러 곳이면 헬퍼 함수로 공유.
  (2026-08-21, 우클릭 메뉴 구분선 시인성 수정)
- **여러 서브패스로 이루어진 `QPainterPath`(예: `addEllipse()`+`moveTo`로 만든 원기둥
  심볼)를 `toSubpathPolygons()`로 평탄화해 점 목록만 이어붙이면, 그 목록을 "하나의 원형
  폴리곤"처럼 `(i+1)%n`으로 순회하는 기존 코드가 서브패스 경계마다 화면에 없는 대각선을
  만든다.** cut/변 인덱싱 모델(TRIM)이 이 폴리곤을 그대로 렌더에 쓰면 그 대각선이 실제로
  그려지고(가로선이 저절로 생김), 최근접 변 탐색이 그 대각선에 가로채여 엉뚱한 위치가
  선택된다. 점 목록(bbox 등)과 변 목록(인덱싱·렌더용)의 계약을 분리해, 변 목록은 서브패스
  경계를 명시적으로 존중(닫힌 서브패스만 자기 닫는 변 포함, 서브패스 간 변 없음)해야 한다.
  (2026-08-28, TRIM 서브패스 이음매 변 제거)
- **`QPainterPath`에 변(edge)마다 독립적으로 `moveTo()`+`lineTo()`를 찍으면, 끝점이
  정확히 일치해도 화면에선 점선처럼 뚝뚝 끊겨 보인다** — 사각형처럼 변이 4개뿐이면 안
  티 나지만, 곡선을 수십 개 변으로 근사한 도형(타원·심볼)은 눈에 확 띈다(200% 줌에서
  재현·확인). 인접 변이 실제로 이어지는 경우(둘 사이에 진짜 gap이 없는 경우) `moveTo`
  대신 `lineTo`로 이어 그려 하나의 연속 subpath를 유지해야 한다 — 직전에 실제로 그은
  끝점을 추적(`last_pt`)해 다음 시작점과 좌표가 일치하는지로 판정. "끝점이 겹치니 알아서
  이어질 것"이라는 가정은 QPainterPath 렌더에서 성립하지 않는다.
  (2026-08-28, TRIM 곡선 연속 렌더 수정)

## 검증 방법론
- 오프스크린(headless) 통과는 "해결"이 아니다 — 지속연결 초안이 offscreen은 통과했지만 실제
  GUI에서 버그가 났다(플로팅→고정 부착점으로 재설계). 전역 규칙 11-c "정직한 상태표기" 참조.
- 헤드리스는 한글 폰트가 없어 tofu(□)로 렌더된다 — 텍스트 정렬·크기 확인은 반드시 비-헤드리스
  `QGraphicsView.grab()` 또는 `python run.py` 실화면으로. (`2026-07.md` Mermaid import)
- `python run.py`를 사용자에게 대신 실행해 달라고 요청하기 전에, 이 환경은 Bash가 사용자와
  같은 대화형 데스크톱 세션이므로 `QT_QPA_PLATFORM=offscreen`만 빼고 직접 실행하면 실제 창·
  콘솔 로그를 스스로 확인할 수 있다(전역 규칙 11-d, 2026-07-31 확립). 정말 대리 불가능한 것
  (마우스 드래그 손맛·실기기)만 사용자에게 남긴다.
- 같은 증상이 두 번째로 재발하면(스턱루프 트립와이어) 트리거만 좁혀가지 말고 **핵심 계산 자체**
  부터 재검증한다 — 트리거를 의심하는 건 계산이 이미 맞다는 전제가 깔린 것인데, 그 전제가
  틀렸을 때는 헛수고다(미니맵 이중변환 사례).
- **시각 버그를 "재현 안 됨"으로 닫기 전에 사용자와 같은 「줌 배율」인지 확인한다.** 크기가 펜
  폭·화면 px에 고정된 결함(예: joinStyle 모따기 ~0.5 씬단위)은 저배율에서 안티에일리어싱에
  원리적으로 묻힌다 — 화살촉 어깨 모따기를 3라운드 동안 재현 못 한 유일한 이유가 이것이었고,
  사용자 스크린샷의 미니맵 배율 표시(2863%)를 읽고 같은 배율로 렌더하자 즉시 재현됐다.
  (`2026-08.md` Lucid 대조 UX 시리즈)
- **"A만 이상하고 B는 멀쩡"할 땐 두 경로의 같은 부분을 나란히 diff한다** — 직각 화살표만
  머리가 깎였는데 곡선·직선은 멀쩡했다면, 두 클래스의 *화살촉 그리는 코드 두 줄*을 대조하는
  게 가장 빠른 진단이다(실제로 그렇게 원인이 즉시 나왔는데 3라운드 뒤에야 했다).
- **시각 결함의 회귀 테스트는 렌더 픽셀로 못 박는다** — 로직 어서션으로 못 잡는 종류이므로
  `QGraphicsScene.render()`로 QImage에 그린 뒤 특정 좌표의 채움 여부를 검사한다. 판정 지점은
  추정하지 말고 **정상/버그 두 버전을 각각 렌더해 실측**으로 고르고, 테스트를 넣은 뒤 반드시
  **수정을 일시 되돌려 실제로 실패하는지** 확인한다(통과하는 테스트가 그 버그를 잡는다는 보장은
  없다). (`2026-08.md` `test_arrowhead_shoulders_not_beveled`)
- **커서·미리보기처럼 「위치마다 상태가 다른」 결함은 점이 아니라 「지도」로 찍는다** — 포트
  안쪽 십자 커서를 3라운드 동안 못 잡은 이유가 매번 몇 개 지점만 찍어봤기 때문이다.
  포트 몸통 전체를 21×21 격자로 훑어 커서 모양을 문자 지도로 출력하고 **원인별로 집계**하자,
  십자 영역이 몸통을 세로로 관통하는 *한 줄*(= 뒤쪽 호스트의 테두리 위치)이라는 패턴이
  즉시 보였다. 줌 배율별로 반복하면 "반경(뷰 px) 때문인 것"과 "기하 때문인 것"도 갈린다
  (전자는 줌에 따라 커지고 후자는 그대로). (`2026-08.md` 포트 트림 구간 스냅 차단)
- **`QDialog`를 `.show()`/`.exec()` 없이 만들어 자식 위젯의 `setVisible()` 결과를 테스트할 땐
  `isVisible()`이 아니라 `isHidden()`을 본다** — `isVisible()`은 최상위 창이 실제로 화면에
  떠야 True가 나오므로, 다이얼로그를 생성만 하고 안 띄운 헤드리스 단위테스트에선 `setVisible
  (True)`를 호출해도 항상 False다. `isHidden()`은 명시적 hide 여부만 반영해 최상위 표시상태와
  무관하게 정확하다. (`2026-08.md` PDF 내보내기 메뉴 통합 §8 항목14, `_PdfExportDialog` 테스트)
- **콘솔에 찍힌 한글이 깨져 보여도 데이터 자체는 멀쩡할 수 있다 — 파일 raw 바이트로 재확인
  없이 "손상"으로 단정하지 말 것.** Windows 한국어 로캘(cp949)에서 `sys.stdout.encoding`이
  UTF-8이 아니면 `print()`로 찍은 한글이 콘솔·터미널 캡처를 오가며 이중 인코딩되어
  `[미확인]`처럼 아스키 리터럴조차 `[��Ȯ��]`로 깨져 보인다. B단계 실도면 검증 중 이걸
  실제 데이터 손상으로 오인할 뻔했으나, `.ecad` 파일은 `sketch_build.save()`가 항상
  `encoding="utf-8"`을 명시해 콘솔 인코딩과 무관하다 — `open(path, "rb")`로 raw 바이트를
  읽어 `"미확인".encode("utf-8") in raw`처럼 직접 대조하면 파일 자체는 처음부터 정상임이
  바로 드러난다. 같은 증상(한글이 `�`로 깨짐)을 보면 먼저 **콘솔 출력이 문제인지 파일이
  문제인지부터 raw 바이트로 갈라볼 것** — 원인에 따라 고칠 곳이 완전히 다르다(콘솔이면
  `sys.stdout.reconfigure(encoding="utf-8")`, 파일이면 저장 코드의 `encoding=` 인자).
  (`2026-08.md` §8 항목18 A+B단계, 2026-08-11)
  ⚠ **한글은 cp949로 인코딩은 되니 "깨져 보이는" 정도로 끝나지만, 한글이 아닌 일부
  문장부호(em dash `—` U+2014 등)는 cp949 repertoire 자체에 없어 `print()`가
  `UnicodeEncodeError`로 그 자리에서 죽는다** — C단계 실제 창 검증 스크립트가 이걸로
  실제로 크래시했다(`_fix_console_encoding()`을 프로덕션 코드엔 넣어 뒀지만 즉석
  검증용 스크립트에는 깜빡해 재발). 새 스크립트를 급히 짤 때도 이 함정이 반복될
  수 있으니, 한글·이모지·특수기호를 `print()`하는 스크립트는 시작부에 습관적으로
  `sys.stdout.reconfigure(encoding="utf-8")`를 넣을 것(cp949 로캘 Windows 한정).
- **말로 설명하기 어려운 GUI 상호작용 버그는 스크린샷·동영상 프레임 추측보다 실시간
  재현 로그가 원리적으로 더 빠르다.** 사용자가 이미 화면을 보고 있어 시각 정보는
  충분하고, 부족한 건 "그 순간 코드 내부 어느 분기를 탔는가"인데 이건 스크린샷이
  못 담는 정보다. 코드 최상단에 파일 로거 하나(`_dbg(msg)`, datetime + try/except)를
  두고 의심 분기마다 상태변화 시에만(`if prev != new: _dbg(...)`) 한 줄씩 찍은 뒤,
  "사용자가 재현하고 됐다고만 알려주면 Claude가 로그 파일을 직접 Read로 읽어 확정"하는
  순환으로 바꾸자, 스크린샷 여러 라운드로 못 잡던 원인(qc-dot/더블클릭/끝점핸들이 TRIM
  클릭을 가로챔)이 로그 한 줄씩으로 확정됐다. 2회차 이상 스크린샷 기반 진단이 실패하면
  (스턱루프 징후) 세 번째 시도 전에 이 방식으로 전환할 것 — 진단이 끝나기 전엔 로그를
  성급히 지우지 말고 사용자가 명시적으로 정리를 요청할 때까지 유지한다(다음 증상을 더
  보고할 수 있으므로). (`2026-08.md` §8 항목17 후속, 개인 위키
  `easycad-실시간재현로그-스크린샷추측대체.md`)
- **위 기법의 후유증(최종 검수 Phase 7, 2026-08-26)**: "진단 끝나도 지우지 말 것"이라는
  결정 자체는 맞았지만, `_dbg`/`_dbg2`를 **무조건 실행**(게이트 없이 매 호출마다 파일을
  열어쓰고 f-string까지 조립) 상태로 몇 주를 방치한 결과, 성능 핫패스(`reroute()`·
  `_on_scene_changed`)에 박힌 로거가 실사용·pytest 양쪽에서 조용히 비용을 내고 있었다
  (1000개 문서 드래그 123.8ms→2224ms, pytest 1회로 로그 파일 64MB). **원인이 "문제
  진단"이 아니라 "왜 이 코드가 느려졌는가"로 뒤바뀐 뒤에야 발견됐다** — 성능 저하의
  원인 후보를 찾을 때 최근 추가된 진단 로그도 후보 목록에 넣을 것. 예방책: 이런 로거를
  심을 때 처음부터 `if not _DBG_ENABLED: return`(환경변수 게이트)을 **호출부**(로거
  내부뿐 아니라, f-string을 조립하는 호출 지점 자체)에도 걸어두면 나중에 "지울지 말지"
  판단이 필요 없어진다 — 끄면 완전 무비용, 필요할 때만 환경변수로 켠다.
- **"회귀를 잡았다"고 확신하기 전에, 그 회귀 검증 스크립트 자체가 이미 알려진 함정을
  그대로 재현하고 있지 않은지 의심할 것.** 라우팅 성능 최적화(사다리 조건부 건너뛰기)를
  검증하며 자체 스크립트로 "화살표 38개 중 5개가 도형을 관통"하는 걸 발견해 회귀로 확신하고
  수정까지 했는데, 같은 수정된 코드에 같은 스크립트를 다시 돌려도 여전히 5건이 나와
  재확인해보니 **위반 판정 자체가 검증 스크립트의 버그**였다 — 도형 경계를 `_obstacle_
  rects()`가 실제로 쓰는 원본 `.rect()`가 아니라 펜폭 패딩이 포함된 `.boundingRect()`로
  재고 있었다. 이건 이 문서 "좌표계·변환" 절에 **이미 기록된 함정**(`_content_rect()`류가
  "콘텐츠 그 자체"가 아니라 "콘텐츠+여유"라는 패턴)을 검증 스크립트를 급히 짜면서 그대로
  다시 밟은 것 — 올바른 경계(`.rect()`)로 다시 재니 수정 전/후 코드 모두 위반 0건, 애초에
  실제 회귀가 없었다. **"같은 스크립트로 재확인했는데도 여전히 실패한다"는 사실 자체가
  검증 방법이 틀렸을 가능성의 신호일 수 있다** — 수정을 더 파기 전에 검증 스크립트가 프로덕션
  코드와 같은 정의(같은 헬퍼 함수·같은 경계 기준)를 쓰고 있는지부터 대조할 것. 다행히 이번엔
  기존 pitfalls 문서를 훑다가 알아챘다 — **자체 진단 스크립트를 새로 짤 때도 이 문서의
  관련 절(좌표·히트테스트 등)을 먼저 확인하는 습관이 값어치를 증명한 사례.**
  (`2026-08.md` §8 항목18 — 클리어런스 사다리 조건부 건너뛰기, 2026-08-12)
- **`QMessageBox.addButton`처럼 클래스 슬롯 자체를 몽키패치하면 다음 테스트에서 복원이
  깨질 수 있다** — DWG 열기 안내 다이얼로그(`_prompt_odafc_missing`)를 테스트하며
  "찾아보기 버튼 클릭"을 흉내내려고 `QMessageBox.addButton`을 가짜 함수로 바꿨다 되돌리는
  방식을 썼더니, 스위트를 이어서 돌리면 **다음 테스트**에서 진짜 `addButton` 호출이
  `TypeError`(오버로드 불일치)로 죽었다 — PyQt6가 클래스 슬롯 재할당·복원을 항상 매끈하게
  보장하진 않는다는 뜻. 해법은 `addButton` 자체를 안 건드리는 것 — 실제 `_prompt_odafc_
  missing`이 `box.addButton(...)`으로 진짜 버튼을 만들게 그냥 두고, `clickedButton()`만
  `self.buttons()`를 텍스트로 스캔해 원하는 버튼을 찾아 반환하도록 패치했다(진짜 QPushButton
  객체를 그대로 씀, `addButton`은 원본 그대로). **교훈: Qt 다이얼로그의 "버튼 눌림"을
  흉내낼 땐 버튼을 만드는 메서드(`addButton`)가 아니라 결과를 묻는 메서드(`clickedButton`)만
  패치할 것 — 전자는 클래스 전체의 생성 경로를 바꾸는 것이라 다른 테스트까지 오염시킬 수
  있고, 실측으로 그 오염이 실제 발생함을 확인했다.** (`2026-08.md` "DWG 자동변환 열기",
  2026-08-14)
- **성능 벤치 하네스는 그 자체가 결함 덩어리일 수 있다 — 최적화를 시작하기 전에 하네스부터
  검증할 것.** 500~1000개 부하 감소 착수 직전 `tools/perf_bench.py`를 감사하니 **결함이 4종**
  나왔고, 그전까지 이 하네스로 낸 절대 ms는 **전부 무효**였다(2026-08-15). 네 가지 모두 다른
  프로젝트의 Qt 벤치에도 그대로 재발할 종류다:
  1. **줌 미통제** — 기본 줌에선 문서 대부분이 화면 밖이라 250개를 끌어도 `paint`가 프레임당
     8회뿐이었다. 즉 **렌더 비용을 거의 안 재고 있었다**. `--zoom fit`을 기본값으로 고정
     (문서 전체가 화면에 = 실사용 조건). 화면 안 아이템 수를 측정 헤더에 항상 찍어 확인한다.
  2. **`processEvents()` 1회는 페인트를 누락** — 250개가 움직였는데 `paint`는 125회만 돌았다.
     3회 flush하면 정확히 250회. 프레임 비용을 재는 벤치는 "이벤트를 한 번 돌렸으니 그려졌겠지"를
     가정하면 안 되고, **실제 `paint` 호출 수를 세어 기대치와 대조**해야 한다.
  3. **`viewport().repaint()`는 반대로 과대측정** — 더티 영역과 무관하게 뷰포트 전체를 다시
     그려 1개만 움직여도 `paint` 304회. 부분 드래그를 실제보다 훨씬 비싸게 잰다. (2·3의 세 방식
     실측 비교표는 `perf_bench._paint_tick` 주석에 남겼다.)
  4. **시나리오 간 배치 오염** — 드래그 시나리오가 도형을 옮긴 채 끝나 다음 시나리오가 어긋난
     배치에서 시작하고, best-of-N의 각 시도가 **서로 다른 배치**를 쟀다. 즉 "노이즈 제거"가
     아니라 **"가장 싼 배치 고르기"**였다. 발견 계기가 중요하다 — 30개 드래그(504ms)가 250개
     전체 드래그(197ms)보다 느린 **물리적으로 불가능한 값**이 나와서 알아챘다. 매 시도 직전
     배치를 복원하도록 수정. **교훈: 벤치 결과에 물리적으로 말이 안 되는 대소관계가 보이면
     코드가 아니라 하네스를 먼저 의심할 것.**
  (`2026-08.md` 500~1000개 부하 감소 1-0, `docs/perf_plan_500_1000.md` §5)
- **시각 회귀 게이트가 「선택 상태」를 안 찍으면 선택 강조 경로는 통째로 사각지대다.**
  `tools/perf_baseline_check.py`는 아무것도 선택하지 않은 화면만 찍고 있었고, 실제로 그 자리에서
  회귀가 났는데도 통과시킨 전례가 있다(커밋 `de71498`의 심볼 밴드 회귀 — `_SymbolItem`이
  `QGraphicsRectItem` 하위라 사각형 밴드로 그려졌는데 게이트는 못 잡았다). 선택 상태 뷰 2종
  (`sel_multi`·`sel_solo`)을 추가하고, **새 뷰가 실제로 선택 강조를 담는지 픽셀로 확인**했다
  (미선택 대비 다중선택 6.02% / 단일선택 0.13% 차이, 둘도 서로 5.98% 차이). 게이트에 뷰를
  추가할 땐 "추가했다"로 끝내지 말고 그 뷰가 **의도한 것을 실제로 담는지**를 수치로 증명할 것 —
  안 그러면 사각지대가 사각지대인 채로 게이트만 늘어난다. (`2026-08.md` 1-0)
- **회귀 테스트를 「결과」로 판정하면 우연히 통과한다 — 「트리거 집합」을 직접 검사할 것.**
  재라우팅 대상을 "움직인 도형에 연결된 화살표"로 좁힌 뒤(`549e98e`) 회귀 테스트를 **경로 좌표**
  로 판정했더니 **옛 코드에서도 통과**했다 — 무관한 화살표까지 재계산하던 옛 코드도 그 케이스에선
  우연히 같은 경로가 나왔기 때문이다. 즉 그 테스트는 "무엇이 재계산됐는가"를 전혀 못 지키고
  있었다. **재계산 대상 집합 자체를 검사**하도록 바꾸자 그제서야 옛 코드에서 실패했다.
  일반화: 최적화의 본질이 "덜 하는 것"일 때, 테스트가 봐야 하는 건 **결과물이 아니라 일한 양**
  이다(호출된 대상 집합·호출 횟수). 결과만 보는 테스트는 최적화가 통째로 되돌려져도 조용히
  통과할 수 있다. 같은 이유로 이 프로젝트의 규약은 **새 회귀 테스트를 반드시 버그/수정 전
  버전에 돌려 실제로 실패하는지 확인**하는 것이다 — 이번 라운드에서 이 확인이 실제로 잘못된
  테스트를 두 번 걸러냈다. (`2026-08.md` 500~1000개 부하 감소, `docs/perf_plan_500_1000.md` §2)
- **`QGraphicsScene.items()`(인자 없이)는 삽입 순서가 아니라 "쌓임 순서 역순"(같은 z값이면
  나중에 추가한 게 먼저)으로 반환한다 — 실측 확인.** 다중 페이지 지원 정렬 테스트를 짜며
  "먼저 넣은 게 먼저 나오겠지"로 순서를 하드코딩했다가 곧바로 실패, `QGraphicsRectItem`
  4개를 순서대로 추가해 `scene.items()`를 찍어보니 `[3,2,1,0]`(역순)이었다 — Qt 문서의
  "topmost 먼저(그려지는 순서의 역)" 관례가 실제로 그렇게 동작함을 보여준 사례. 씬 아이템
  순서에 기대는 로직·테스트를 짤 땐 "삽입 순서 보존"을 가정하지 말 것 — 필요하면 z값이나
  별도 인덱스/타임스탬프로 명시적으로 관리해야 한다(안정 정렬의 "동률 시 원래 순서 유지"도
  이 역순 위에서 일어난다는 점에 유의). (`2026-08.md` "다중 페이지 도면 지원 v1", 2026-08-14)

- **성능 노브를 A/B로 잴 땐 그 노브가 「기하까지 바꾸는지」 먼저 의심하고, 매 측정에 기하
  지문을 가드로 박아라.** `NoIndex` 재평가(2026-08-15)에서 하네스가 **연달아 세 번** 오염됐고
  셋 다 뿌리가 하나였다 — 이 레포의 라우팅 결과가 인덱스 방식에 의존한다는 것(위 "라우팅" 절).
  ⓐ 조합마다 새 창을 만들며 **로드 전**에 인덱스를 걸었다 → 로드 시 바인딩 정규화 reroute가
  그 인덱스로 돌아 두 팔이 **서로 다른 도면**이 됨. ⓑ 한 씬에서 인덱스를 번갈아 바꿨더니
  드래그 시나리오의 배치 복원(`_on_scene_changed(None)`)이 NoIndex 구간에서 경로를 그쪽으로
  굳힘. ⓒ 측정 직전 **선택 변경**조차 reroute를 유발해, 인덱스를 먼저 걸면 그 reroute가 오염됨.
  ⓒ는 사람이 눈으로 못 잡았고 **스크립트에 넣어둔 지문 가드가 잡았다**("측정 중 경로가
  BSP형→NoIndex형으로 바뀜"). 이 오염 때문에 원래 2-E 표는 "단일 드래그 −59%"라는 존재하지도
  않는 이득을 기록했고(기하 고정 시 32.0 vs 32.1로 **차이 없음**), 그 잘못된 수치가 항목을
  "재평가 가치 큼"으로 한 라운드 더 살려뒀다. **교훈 셋**: ① A/B 하네스는 "두 팔이 정말 같은
  입력을 재고 있나"를 **스스로 검사**해야 한다(지문·해시를 측정값 옆에 항상 출력). ② 상태를
  바꾸는 설정은 **씬이 완전히 정착한 맨 마지막에** 걸어야 한다. ③ 개선폭이 유난히 크면
  (−59%!) 기뻐하지 말고 하네스를 먼저 의심할 것 — §5 결함 4종의 "물리적으로 불가능한
  대소관계" 교훈과 같은 계열이다. (`docs/perf_plan_500_1000.md` §4 2-E)

- **벤치 시나리오는 자기가 켠 상태를 자기가 꺼야 한다 — 안 끄면 「뒤따르는 시나리오」가
  오염된다.** `perf_bench.scn_release`만 뒤에 `end_drag()`가 없어서 **드래그 세션을 켠 채로**
  끝나고 있었다(`_time`이 마지막에 한 번 더 부르는 reset이 `prime()`이고, 그 끝이
  `begin_drag()`다). 그래서 zoom/pan/render가 전부 "드래그 중" 화면으로 측정돼 왔다. 2-D를
  켜자 `render_fit`이 97.7→58.3으로 뚝 떨어져 **그제서야 드러났다** — 드래그와 무관한
  시나리오가 그렇게 좋아질 리가 없다. 교훈은 바로 위와 같다: **개선폭이 말이 안 되면 코드가
  아니라 하네스를 먼저 의심할 것.** 일반화하면 시나리오 함수는 「자기가 바꾼 전역 상태를
  자기가 되돌린다」를 규약으로 삼아야 한다. (`2026-08.md` "2-D(드래그 프록시)")
- **"오프스크린이니까 모달이 알아서 넘어가겠지"는 틀린 가정이다 — `QMessageBox.warning(...)`
  같은 정적 편의 메서드가 실제로 `.exec()`까지 도는 코드 경로면 `QT_QPA_PLATFORM=offscreen`
  에서도 진짜로 블로킹한다.** §8 항목10 Stage C(닫기확인창)에서 실측: dirty 추적 자체가
  버그(위 "파일 분할" 절)로 무효였던 동안엔 `closeEvent`의 확인 다이얼로그가 한 번도 실제로
  뜬 적이 없어 몰랐는데, 그 버그를 고치자마자 기존 테스트 8곳(`w.close()`, 사전 조건 없이
  그냥 정리용으로 호출하던 것)이 dirty 상태에서 실제 모달을 만나 스위트 전체가 멈췄다(타임아웃
  전엔 "느린 게 아니라 멈춘" 상태로 조용히 걸려 있었다). **교훈**: 새 기능이 모달 경로를 만들면
  ⓐ 먼저 그 모달이 실제로 뜨는 조건(여기선 dirty)을 만드는 기존 테스트가 있는지 전수 확인하고
  ⓑ 그 경로를 타는 테스트는 모두 정적 메서드를 직접 몽키패치(`QMessageBox.warning = staticmethod
  (lambda *a, **kw: 답)`)하거나 사전 조건을 없애야 한다 — "헤드리스라 안전하다"는 검증 없는
  가정으로 넘기지 말 것(이 항목이 위에 이미 있던 "모든 모달 경로는 테스트에서 모킹 필수" 교훈의
  재확인이지만, 그때와 다른 점은 **버그가 먼저 있어서 위험 자체가 가려져 있었다**는 것 —
  버그를 고치는 순간 잠재 위험이 실제 장애로 승격할 수 있다는 사례). (`2026-08.md` §8 항목10
  Stage C, `tests/test_part1_ui_arrows.py`·`test_part6_grid_minimap_layers.py`)
- **`CanvasWindow()`를 `.show()` 없이 offscreen에서 만들면 `viewport().grab()`/`.repaint()`를
  불러도 아이템의 `paint()`가 아예 호출되지 않는다** — 상태 어서션(`bound_shapes()` 등)은
  정상인데 렌더 픽셀만 배경색 그대로라 "로직은 맞는데 안 그려진다"로 착각하기 쉽다. 몽키패치로
  `paint()` 호출 여부를 직접 로그해서야 확인됨. 렌더 픽셀을 검증해야 하는 테스트는 반드시
  `w.show(); _app.processEvents()`를 먼저 하고, 상태만 검증하는 테스트는 `.show()` 없이도
  된다(이 구분을 안 하면 "픽셀이 안 바뀐다"는 거짓 음성이 남는다). 기존 `_draw_port_dots`류
  테스트(`test_part5_precision_edit.py`)가 `MagicMock()` 페인터로 `drawEllipse.called`만
  보는 것도 같은 함정을 피하는 패턴 — 실제 픽셀이 필요 없으면 `.show()`도 필요 없다.
  (`2026-08.md` 화살표 끝점 재부착 예고점, 2026-08-19)
- **`QThread` 워커를 도입하면, 그 워커를 시작하는 테스트는 어서션이 `wait()` 전에 실패해도
  반드시 join되도록 `try/finally`로 감싸야 한다.** `with patch(...)` 블록 안에서
  `worker.start()` 후 곧바로 어서션을 걸었다가 그게 실패하면, 백그라운드 스레드가 아직
  실행 중인 채로 `with`가 예외 전파로 조기 종료돼 패치가 되돌아간다 — 그 순간 스레드가
  patch 이전의 **진짜** 함수(여기선 실제 게이트웨이 HTTP 호출)를 다시 붙잡아, 테스트가 끝난
  뒤에도 실제 네트워크 호출이 프로세스 안에서 계속 살아 있게 된다. 이 세션에서 정확히 이
  경로로 SVG/Mermaid AI 다이얼로그 async화 테스트 스위트가 (겉으론 무관해 보이는) 몇 시간
  뒤 다른 테스트에서 멈춘 것으로 관측됐다 — `pytest -q`가 진행 표시(`.`/`F`) 몇 개를 찍고
  멈췄을 뿐이라 원인이 그 위치의 코드처럼 보였지만, 실제 범인은 그보다 훨씬 앞에서 실패한
  다른 테스트였다. 진단은 실패 지점을 세어(`.`/`F` 개수 → `pytest --collect-only`로 인덱스
  역산) 좁히고, 격리 재실행(`pytest file::test_name`)으로 그 자체는 순식간에 통과함을 확인해
  "단독으론 무해, 순서 의존 오염"임을 확정했다. 교훈: 새 QThread 관련 테스트는 ⓐ 워커를
  시작했으면 성공/실패 어느 쪽이든 `finally`에서 `wait()`(join)하고 ⓑ "생성 중 닫기 무시"류
  검증은 `close()`+`result()` 조합(갓 만든 `QDialog.result()`가 이미 `Rejected`(0)라 "닫기
  전"과 구분 불가) 대신 `closeEvent(QCloseEvent())`를 직접 호출해 `isAccepted()`로 판정한다.
  (`2026-08.md` §8 항목18/20 후속 — SVG/Mermaid 다이얼로그 비동기화, 2026-08-19)
- **pytest 격리(`conftest.py` autouse fixture)는 pytest로 실행할 때만 발동한다 — 같은
  테스트 함수를 다른 진입점으로 부르면 무방비다.** 커밋 9323180이 "테스트가 실사용자
  QSettings(`HKCU\Software\EasyCAD\EasyCAD`)를 지운다"를 `conftest.py`의 autouse
  `monkeypatch`로 고쳤는데, 바로 다음 세션에서 **같은 사고가 재발**했다 — 원인은
  `tests/test_easycad.py`(CLAUDE.md가 "전체 실행" 진입점으로 그대로 추천하는 커스텀
  러너)가 pytest 없이 테스트 함수를 직접 호출해 그 fixture 자체가 안 걸리는 것이었다.
  실측: 실제 레지스트리에 표식 값을 넣고 `python tests/test_easycad.py`를 돌리자
  사라짐(수정 전) / 살아남음(수정 후, `_shared.py`에서 모듈 임포트 시점에 `gw._SETTINGS_
  ORG/_SETTINGS_APP`을 재바인딩 — 모든 test_part*.py가 pytest·커스텀 러너 어느 쪽으로
  실행되든 제일 먼저 `from _shared import *`를 타므로 두 경로 다 커버). **교훈: 격리
  훅을 pytest 픽스처 한 곳에만 걸면 "pytest로 실행한다"는 전제가 깨지는 순간(레거시
  진입점, `python -m module` 직접 호출, CI가 다른 러너를 쓰는 경우 등) 조용히 무력화된다
  — 실사용자 상태를 건드리는 격리는 그 리소스에 접근하는 모든 코드가 반드시 거치는
  가장 이른 지점(여기선 공용 `_shared.py` 임포트)에 걸어야 진짜 안전하다.**
  (`2026-08.md` "pytest 게이트웨이 키 소실 재발 — 두 번째 진입점", 2026-08-20)
- **자체검증용 임시 `python -c`/스크립트에서 `CanvasWindow()`를 만들기 전에 `QApplication`
  인스턴스가 있는지 먼저 확인한다 — 없으면 트레이스백 하나 없이 종료코드 127로 조용히
  죽는다(`except BaseException`으로도 못 잡는 진짜 네이티브 크래시).** `tests/_shared.py`는
  `_app = QApplication.instance() or QApplication([])`을 모듈 최상단에서 보장해 pytest
  경로는 이 문제가 없지만, 검증하려고 즉석에서 짜는 스크립트는 이 줄을 깜빡하기 쉽다(위젯
  개수가 늘어날수록 재현이 더 빨라지는 경향이 있어 "방금 추가한 코드가 범인"으로 오인하기
  쉽다 — 실제로는 pytest로 같은 코드를 돌리면 멀쩡했다). 원인 불명 127을 만나면 먼저 이
  가능성부터 배제할 것. (`2026-08.md` "회전 스핀박스 타이핑 불가 버그 + 화살촉 크기(배율)
  신설", 2026-08-21)
- **속성 편집 undo 경로는 둘이다 — `capture_state`(겉모습: 색·두께·화살촉 등)와
  `capture_geom`(기하: pos/rot/scale). 새 속성패널 컨트롤을 만들 때 어느 쪽을 쓸지는
  "이 값이 스냅샷 dict의 어느 필드에 있는가"로 판정해야지 감으로 고르면 안 된다.** 회전
  각도 숫자입력(2026-08-21)을 처음엔 다른 속성 편집(`_edit_width`/`_edit_style`)과 같은
  관례로 `_edit_items`(=`capture_state`/`apply_state`)에 태웠는데, `capture_state`는
  `rotation()`을 애초에 담지 않는 필드라 **되돌리기가 조용히 무효**였다(적용은 되는데
  undo가 값을 안 바꿈 — 예외도 안 남). offscreen 자체검증으로 "적용→undo→값 비교"를
  직접 찍어봐서(rule 11-d) 잡았다 — 겉으로는 정상 작동해 보여 사용자 재현 없이는 못
  잡았을 것. `_floating_set_radius`가 이미 같은 이유(반경도 기하)로 `capture_geom`/
  `push_undo_geom`을 쓰고 있었던 걸 나중에 발견 — 처음부터 "회전=기하"로 분류했다면
  피할 수 있었던 함정. (`2026-08.md` "속성패널 6항목 재정렬+화살촉 양방향+회전 각도",
  2026-08-21)
- **`_time()`의 `reset` 인자는 "아이템이 움직이는" 시나리오뿐 아니라 "뷰포트가 움직이는"
  시나리오(팬·줌)에도 필요하다.** `scn_pan`/`scn_zoom`은 2026-08-15에 드래그 계열이 이미
  겪은 "reset 누락 → best-of-N이 최소값이 아니라 가장 싼 우연을 고른다" 함정을 그대로
  재발시켰다 — 스크롤 위치가 trial마다 누적 이동해, trial 수가 늘수록 콘텐츠 밖 빈 캔버스
  까지 벗어나 best-of-9가 "OK"(5.8ms)를 찍었다. 실제로는 콘텐츠 안에 머무는 sustained 측정
  으로 60fps 예산의 4배 이상이었다. 뷰포트 상태(스크롤·변환)를 건드리는 시나리오는 아이템
  위치를 건드리는 시나리오와 똑같이 "trial 간 원상복구"가 필요하다고 기본 가정할 것.
  (`2026-08.md` "팬도 드래그 프록시 대상으로 확장 + perf_bench pan/zoom reset 버그 수정",
  2026-08-24)
- **pytest의 합성 `QApplication.processEvents()` 재진입은 진짜 `QApplication.exec()`
  이벤트루프와 다른 크래시 거동을 보일 수 있다 — "이 테스트가 크래시한다"가 "실사용자도
  이 크래시를 겪는다"를 자동으로 의미하지 않는다.** 최종 검수 Phase 1(2026-08-25)에서
  `pytest tests/`가 exit 127(힙 손상 abort 서명 — `os.abort()`가 이 환경에서 정확히
  exit 127을 냄을 별도 확인, 일반 `SIGSEGV`는 exit 3)로 21%만 실행하고 죽는 것을
  진단했다. 이등분 탐색으로 단일 테스트(`test_left_panel_scrolls_...`, 40회 UI 반복이
  좌측 패널 위젯을 최대 120회 재구축)로 좁힌 뒤, 원인 함수(`_refresh_custom_symbol_
  section()`)의 `QApplication.processEvents()` 동기 호출(함수 맨 위 clear 루프가 예약한
  `deleteLater()`가 이 재진입 안에서 처리됨)을 무효화하자 크래시가 사라져 확정했다.
  **그런데 진짜 `QApplication.exec()` 이벤트루프(`QTimer.singleShot`으로 같은 조작을
  예약하는 방식, 실제 앱과 동일 조건)로 재현하면 크래시가 안 났다** — 지금까지 배포된
  코드가 실사용자에게 이 크래시를 낸 적은 없었다는 뜻. 그래도 `QTimer.singleShot(0, ...)`
  (재진입 없이 다음 틱으로 미룸)로 교체해 향후 위험을 없앴다. **교훈**: pytest 안에서만
  나는 크래시를 만나면 "테스트 하네스가 실사용자 조건과 다른 이벤트루프 경로를 만들고
  있을 가능성"을 먼저 의심하고, 가능하면 진짜 `app.exec()`으로 최소 재현해 실사용자
  영향 여부를 분리 판정할 것 — 자동으로 "프로덕션 버그"로 단정하면 원인 없는 곳에서
  가설(창 미종료·QApplication 파괴 순서·QThread 수명 등)만 3개 세우고 전부 빗나갈 수
  있다(이번에도 그랬다). (`2026-08.md` "최종 검수 Phase 1 — pytest 전체 스위트 exit 127
  크래시 근본원인 규명·수정", 2026-08-25)
- **재현 스크립트 자체의 버그(scene 좌표를 shape-local 좌표 자리에 넘김)가 실제 증상을
  10배 이상 과장해, 완전히 틀린 원인론(사다리 채점 로직)으로 사용자를 인터뷰까지 끌고
  갔다.** `_PolyArrowItem.set_bound(idx, shape, local_pt)`는 shape-local 좌표를 받는데
  재현 스크립트가 `_shape_ports()`가 돌려주는 scene 좌표를 그대로 넘겼다 — 도형이
  원점(0,0)에 있으면 local==scene이라 우연히 안 드러나고, 원점에서 떨어진 배치에서만
  `_bound_normal_scene`이 엉뚱한 법선을 돌려준다. 그 결과 "화살표 진입 스텁이 3px까지
  붕괴한다"는 잘못된 수치로 deep-interview를 거쳐 "사다리(clearance ladder) 채점 기준을
  고친다"는 방향까지 사용자 승인을 받았는데, 구현 착수 직후 코드를 다시 읽다가 사다리가
  애초에 이 경로를 안 탄다는 걸 깨닫고 재현을 다시 짜서야 스크립트 버그를 발견했다 —
  고쳐서 다시 재니 최악값이 5~10px(사다리와 무관, `_dedup_pts`의 별개 버그)였다. **교훈:
  헤드리스 재현으로 만든 수치를 "확정된 증거"로 사용자 의사결정에 쓰기 전에, 그 재현이
  쓰는 API 하나하나(특히 좌표계 — local vs scene)가 실제 앱 코드의 계약과 맞는지 먼저
  최소 대조(원점이 아닌 곳에 배치해 정상 케이스와 결과가 일치하는지)로 검증할 것 — 원점
  근처에서만 시험하면 좌표계 버그가 통째로 숨는다.** (`2026-08.md` "화살표 속성 리뷰
  인계 항목2", 2026-08-31)

## 하위호환·직렬화
- `.ecad`에 필드를 추가할 때는 항상 "키 없으면 안전한 기본값"으로 로드하게 만든다(예: 옛
  `routing:"ortho"`는 반경 0으로, `col_widths` 개수 불일치는 균등폭으로). 새 기본값이 옛 파일의
  시각 결과를 바꾸면 안 된다.
- 반환 계약이 이미 25곳 이상에서 소비되는 함수(`load_document`의 `int` 반환)는 타입을 바꾸지
  말고 별도 함수(`load_document_layers()`)로 분리 — 회귀 폭 최소화. (`2026-07.md` 레이어 패널)

- **새 아이템 클래스를 추가할 때 `isinstance` 체인으로 분기하는 병렬 export 파이프라인
  (`dxf_export.py` 등)을 갱신했는지 반드시 확인할 것 — 상속 관계가 아니면 자동으로 안
  걸린다.** `_PolygonItem`(§8 항목21)이 `_RectItem`을 상속하지 않는 독립 클래스로 설계돼
  있어, `dxf_export.py`의 `isinstance(it, _RectItem)`류 dispatch 체인 어디에도 안 걸려
  예외 없이 조용히 드롭됐다 — v1 설계문서가 "DXF는 후속으로 미룸"이라 명시했던 항목인데,
  이후 TRIM 연동까지 다 끝나도록 다시 안 다뤄져 방치됐다. 더 나쁜 건 당시 회귀 테스트가
  "조용히 스킵됨"을 정상 동작으로 그대로 assert해 뒀던 것 — 새 기능이 미완성 상태로
  "완료"처럼 보이는 이 조합이 재발 위험을 특히 키운다. (`2026-08.md` "다각형 DXF 내보내기
  완전 누락 수정")

- **ezdxf는 `doc.header["$EXTMIN"]`/`"$EXTMAX"`/`"$LIMMIN"`/`"$LIMMAX"`에 직접 쓴 값을
  저장(`save()`/`saveas()`) 시 조용히 덮어쓴다** — 내부 `update_all()`이 `update_extents()`/
  `update_limits()`를 호출해 **modelspace 레이아웃 자신의 `dxf.extmin`/`extmax`/`limmin`/
  `limmax` 속성**으로 헤더를 다시 채우기 때문(`ezdxf/document.py`). 저장 직전까지는 헤더에
  내가 쓴 값이 그대로 보여 착각하기 쉽다 — 반드시 **레이아웃 속성**(`doc.modelspace().dxf.
  extmin = ...`)에 써야 저장 후에도 살아남는다. (`2026-08.md` "DXF 내보내기 — 실제 콘텐츠
  범위를 EXTMIN/EXTMAX·초기 뷰포트에 반영")

## 파일 분할(리팩터링)
- 거대 파일을 쪼갤 때 공유 상수만 따로 뽑아 `_constants.py` 같은 별도 파일을 만들면, 그 상수를
  실제로 쓰는 다른 leaf 모듈(위젯·다이얼로그 등)과 원본 파일 양쪽에서 서로 참조하다 순환
  임포트가 되기 쉽다 — 상수는 그걸 실제로 쓰는 다른 콘텐츠와 **같은 leaf 파일**에 두는 편이
  안전하다. (`2026-08.md` 프로젝트 구조 정리, `host_constants.py` 폐기 → `host_widgets.py`로 재배치)
- 한 클래스(믹스인)의 메서드들이 다른 클래스(아이템)의 함수를 부르고, 그 아이템 클래스가 다시
  그 믹스인을 상속하는 식으로 **진짜 순환 의존**인 부분은 억지로 여러 파일로 쪼개지 말 것 —
  함수-지역 임포트를 여러 곳에 흩뿌려야 해서, 이미 버그가 잦았던 영역일수록 위험만 커진다.
  하나의 파일로 유지하는 쪽이 더 안전한 경우가 있다(크기만으로 분할 여부를 정하지 않는다).
  (`2026-08.md` — `annotator_core.py`를 16개 대신 `core_shapes.py` 하나로 유지)
- **"활성 인스턴스로 포워딩하는 프로퍼티" 패턴(`self._foo` → `self._active.foo`)을 도입할 때,
  새 필드 하나만 그 밑줄 접두 관례를 깜빡하면 조용히 무효가 된다.** `CanvasWindow`가 문서별
  상태를 `self._active_doc`으로 포워딩하도록 리팩터(§8 항목10)하면서 `_scene`/`_undo`처럼
  기존 이름 24개는 전부 `_<name>` 프로퍼티를 자동 생성했는데, 새로 추가한 `dirty` 필드만
  `self.dirty = True`(밑줄 없이)로 썼다 — `CanvasWindow` 클래스엔 그런 프로퍼티가 없으므로
  Python이 그냥 평범한 인스턴스 속성을 새로 만들어버렸다. 예외도 안 나고 `self.dirty`를
  읽으면 방금 쓴 값이 그대로 나와(같은 이름이니 당연히) **완전히 정상 동작하는 것처럼
  보이는데, 실제 문서 객체(`self._active_doc.dirty`)는 전혀 안 바뀌어 있었다.** 직접 호출
  테스트(`w._active_doc.dirty`를 프로퍼티가 아니라 문서 객체에서 직접 읽는 assert)로만
  잡혔다 — `w.dirty`만 확인했다면 통과했을 것. **포워딩 프로퍼티 패턴을 쓸 땐 새 필드를
  추가할 때마다 "밑줄 붙은 이름으로 썼는가"를 코드가 아니라 실제 문서 객체를 직접 읽어
  대조하는 테스트로 확인할 것** — 이름이 같은 두 속성(포워딩 프로퍼티 vs 평범한 인스턴스
  속성)은 서로를 완벽하게 가려 대체 증거가 없으면 못 잡는다. (`2026-08.md` §8 항목10
  Stage C, `tests/test_part10_multi_doc.py`)
- **단일 인스턴스를 다중 인스턴스 컬렉션으로 바꿀 때, `__init__` 안에서 "이번 한 번만"
  실행되던 후처리 호출은 새 인스턴스가 생기는 지점마다 다시 걸어야 한다 — 안 그러면 최초
  인스턴스만 정상이고 나머지는 그 후처리를 영원히 못 받는다.** §8 항목10에서 `CanvasWindow`
  가 문서를 하나에서 여럿(탭)으로 바꾸면서, 원래 `__init__` 끝에 있던 `_apply_theme(dark)`
  (씬 배경을 테마색으로 칠하는 후처리)가 그 유일한 씬 하나만 칠하면 충분했던 게, 탭 도입
  뒤엔 "새 탭"으로 만든 문서엔 전혀 안 걸리게 됐다(문서 자체는 테마를 모르는 채 항상
  흰색으로 시작 — 의도된 설계). 증상은 "새 탭만 흰 배경, 다크모드 껐다 켜면 정상화"로
  사용자가 실사용에서 발견 — **자체검증 스크린샷에서 이미 흰 배경을 직접 봤는데 "빈 문서라
  흰 게 맞다"로 잘못 합리화하고 넘어갔다**(다크 테마 기준이면 빈 문서도 어두워야 한다는
  대조를 안 함). 교훈 둘: ① 컬렉션화 리팩터를 할 땐 `__init__`의 후처리 호출들을 전부
  나열해 "새 인스턴스 생성 지점(들)에서도 똑같이 걸리는가"를 확인할 것. ② 자체검증
  스크린샷은 "이 상태가 지금 설정 기준으로 정상인가"까지 대조해야 한다 — 값 하나만 보고
  "그럴 수도 있지"로 넘기면 바로 이 함정에 빠진다. (`2026-08.md` §8 항목10 "실사용 버그 1건
  — 새 탭이 흰 배경으로 뜨는 버그")

## AI 이미지 생성(에셋 디자인)
- 시안을 재생성할 때는 **이전 프롬프트에 있던 필수 요소를 매번 다시 명시**한다 — "일부만
  수정해서 다시 그려줘"식 프롬프트는 이전에 확정된 디테일(예: 십자선)이 조용히 빠질 수 있고,
  생성 모델은 스스로 누락을 알리지 않는다. 재생성 직후 반드시 이전 결과와 나란히 놓고 대조.
  (`2026-08.md` 안테나 심플화 §8 항목13 — MESH 재생성에서 십자선 누락)
- 사용자가 "GPT한테 물어본 거야"처럼 **생성 주체를 명시**했으면 그 도구로 다시 만든다 — 비슷해
  보인다고 다른 방법(SVG 직접 작성 등)으로 대신 만들면 스타일이 미묘하게 달라 사용자가 즉시
  알아챈다. (`2026-08.md` 안테나 심플화 §8 항목13)

## AI 게이트웨이 호출(이미지→도면)
- **`urllib`로 factchat 게이트웨이를 치면 SSL 검증 실패**(self-signed in chain). `openai`
  SDK·`httpx`(certifi 번들)는 같은 URL·같은 키로 정상. 표준 라이브러리로 직접 치지 말 것.
  (2026-08-11 실측, `docs/ai_image_import.md`)
- **`claude-*` 계열은 밀집 도면 전체 이미지에서 504 Gateway Timeout** — 절반으로 축소해도
  동일. `gemini-3.6-flash`는 같은 이미지를 44초에 처리. 모델 능력이 아니라 게이트웨이 시간
  제한이므로 **폴백 체인 필수**. (2026-08-11 실측)
- **작은 요소 누락은 모델 탓이 아니라 입력 픽셀 탓**이다 — 전체 1패스는 작은 서브박스(M/S)를
  통째로 못 봤고 같은 구획을 3배 확대해 넣으니 전부 잡혔다. "인식이 나쁘다" 싶으면 프롬프트를
  고치기 전에 **입력 해상도부터** 본다. (2026-08-11 실측)
- **"구획 크롭이라 타임아웃 안 난다"는 결론은 크롭 크기에 따라 다시 깨진다.** 설계 단계
  실측(위 항목)은 사람이 수동으로 고른 작은 크롭(360×160)이었는데, B단계 자동 타일링
  (`compute_tiles`, 기본 `max_shapes_per_tile=8`)이 실도면(1920×1080, 밀집)에서 만든 타일은
  훨씬 커서(줌 후 최대 변 ~2000px) claude가 크롭에서도 6개 중 4개나 504로 타임아웃했다 —
  "크롭이면 안전하다"가 아니라 "**그 크롭이 원래 측정한 크기와 비슷할 때만** 안전하다"였다.
  폴백 체인 덕에 파이프라인 자체는 죽지 않았지만(gemini로 조용히 넘어감), claude가 주는
  세부 인식 이득은 타일이 클수록 줄어든다 — 자동 타일링 크기를 튜닝할 땐 사람이 측정했던
  기준 크기를 넘지 않는지 확인할 것. (2026-08-11 B단계 실도면 검증, `tools/ai_sketch.py`
  `compute_tiles` 주석)
- **정확도만 보고 기본 모델을 고르면 비용을 놓친다.** P2 타일 기본을 `claude-sonnet-5`로
  정할 때 "크롭에서 세부를 더 잘 잡는다"는 것만 실측했지 크레딧 비용은 안 쟀다 — 실사용
  중 크레딧 소모가 과하다는 지적을 받고서야 같은 크롭으로 4모델(gemini-3.6-flash·
  claude-sonnet-5·claude-haiku-4-5·gpt-5.4-mini)을 다시 재보니, `gpt-5.4-mini`가
  `claude-sonnet-5`와 shapes·edges가 완전히 같으면서 비용은 1/18(3.08 vs 56.92 크레딧)·
  속도는 1/6(4.9s vs 32.3s)이었다. **정확도 비교에 비용 열을 같이 안 넣으면, "더 정확한
  모델"이 실은 "20배 비싼데 똑같은 결과"일 수 있다** — 모델 기본값을 정할 때는 항상
  크레딧 소모까지 같은 표에 넣고 비교할 것(`gw.get_credit_balance()` 전후 델타로 실측
  가능). (2026-08-11, `docs/ai_image_import.md` "모델 선택")
- **타일링(도면을 여러 조각으로 나눠 각각 인식)은 "도형 인식"은 살리지만 "그 사이 연결선"은
  기본적으로 죽인다.** `namespace_tile_result`가 edges를 "두 끝이 같은 타일 안에서 함께
  인식됐을 때만" 살리는데, 도면 전체를 가로지르는 긴 연결선(왼쪽 끝 장비 → 한가운데 허브 →
  오른쪽 끝 안테나 같은)은 두 끝이 서로 다른 타일에 나뉘어 찍히기 쉬워 도형은 둘 다 정확히
  인식돼도 그 사이 선 정보만 통째로 사라진다. 실측(KBS 1RFM 도면): 실제 도형 27개 중
  11개(41%)가 화살표를 하나도 못 얻었고, 그중엔 도면 중앙 핵심 허브 블록도 포함됐다 —
  결과물이 "도형은 맞는데 다 흩어져 보이는" 인상을 주는 주범이 라우팅 품질이 아니라
  **이 연결 정보 손실**이었다. 타일 오버랩 패딩을 늘리는 정도로는 해결 안 된다(패딩은 짧은
  겹침 구간만 커버, 페이지를 가로지르는 긴 선은 여전히 못 잡음). 해법은 별도 패스(P3.5) —
  병합된 도형 목록 + 전체 원본 이미지를 다시 모델에 주고 "이 도형들 사이 실제 선만" 찾게
  하면(스키마를 edges 전용으로 좁혀 새 도형 생성 여지를 차단) 고립 도형 비율이 41%→12.5%로
  줄었다. **타일링을 쓰는 어떤 인식 파이프라인이든 "타일 경계를 넘는 관계"는 별도로 다시
  물어봐야 한다는 게 일반화될 수 있는 교훈** — 타일 안에서 정확한 것과 타일 사이 관계가
  보존되는 것은 별개 문제다. (2026-08-11, `docs/ai_image_import.md` "B 후속 — P3.5")
- **같은 실물이 서로 다른 타일에서 다른 정밀도로 두 번 인식되면, 중복 제거 임계값 하나로는
  못 잡을 수 있다.** 순수 겹침(IoU) 기반 중복 제거는 두 감지가 서로 다른 크롭 컨텍스트에서
  나와 박스 크기·위치 추정이 어긋나면 실제로는 같은 물체인데도 겹침이 낮게(실측 0.12) 나올
  수 있다. 그렇다고 임계값을 무작정 낮추면 "PIC-FM이 그 안의 Audio(A)를 감싸는" 것처럼
  **라벨이 다른 진짜 별개의 중첩 구성요소**(실측 겹침 0.11~0.35, 임계값과 겹치는 범위)까지
  잘못 합쳐진다. 해법은 신호를 하나 더 쓰는 것 — 라벨이 사실상 같은 텍스트일 때만 낮은
  바닥값을 추가로 허용(라벨 정규화는 공백 제거+소문자화만으로 충분했다, 실측: 쉼표 뒤
  공백 유무 차이). **기하(겹침)만으로 안 되면 의미(라벨/텍스트) 신호를 추가하는 쪽이,
  기하 임계값 하나를 계속 조정하는 쪽보다 안전하다** — 임계값 하나짜리 조정은 항상
  "더 잡느냐 vs 오탐 넣느냐"의 트레이드오프뿐이지만, 독립적인 두 번째 신호는 그 트레이드오프
  자체를 없앤다. (2026-08-11, `docs/ai_image_import.md` "B 후속")
- **"원본 이미지를 생성형 AI로 먼저 단순화한 뒤 처리하면 정확도가 오르지 않을까"는
  직관적이지만 위험한 아이디어다.** 이미지 생성 모델(예: gpt-image 계열)은 "그럴듯하게
  다시 그리는" 모델이지 "정확히 보존하며 다시 그리는" 모델이 아니다 — 특히 작은 글씨(한글
  기술 라벨, 포트 번호 같은)는 생성형 모델이 가장 못 보존하는 영역인데, 정작 이 파이프라인이
  이미 잘 해내고 있는 부분이 바로 그 정밀 텍스트 추출이다. 이 프로젝트가 이미 두 번 겪은
  환각 함정(재생성 시 디테일 조용히 소실, 논리적으로 있어야 할 것 같은 연결선을 만들어냄)과
  같은 종류의 위험을 이미지 픽셀 단계로 옮기는 것뿐이라, "실제로 그려진 것만"류 프롬프트
  안전장치가 이미지 생성 단계에는 먹히지 않는다(그 단계 자체가 원본을 이미 한 번 걸러
  버려서, 무엇이 빠졌는지조차 확인할 방법이 없어진다). 결정적(비생성) 전처리(대비 향상·
  업스케일 등, Pillow만으로 가능 — 새 모델 호출 불필요)는 같은 목표를 안전하게 달성할 수
  있는 대안이지만 실험은 아직 안 함. (2026-08-11, 사용자 질문에 대한 답변,
  `docs/ai_image_import.md` "B 후속")
- **부정확한 입력 하나가 서로 무관해 보이는 두 증상(시각적 지저분함 + 심각한 성능 저하)을
  동시에 만들 수 있다 — 사후 보정을 아무리 정교하게 해도 원리적 한계가 있다.** vision 모델의
  절대 픽셀 좌표 추정이 부정확한 채로(같은 실물도 두 번 인식하면 좌표가 몇~수십 px씩 어긋남)
  그 좌표를 그대로 신뢰해 배치하면, ⓐ 도형이 겹쳐 시각적으로 지저분해지고 ⓑ 그 겹친 도형
  더미 사이로 자동 배선(A*)이 지나갈 길을 못 찾아 성능까지 파국적으로 나빠진다(실측: 20개
  그룹 드래그가 60fps 예산의 55배, **도형 수는 1600개짜리 정상 문서보다 훨씬 적은데도** 더
  느렸다 — "도형이 많아서"가 아니라 "겹쳐 있어서"). 중복 제거 임계값을 아무리 정교하게
  다듬어도(2차례 시도) 좌표 추정 오차 자체는 안 없어지므로 다른 형태로 계속 재발했다.
  **근본 해결은 "부정확한 값을 보정"이 아니라 "그 값을 애초에 안 쓰는" 방향**이었다 — 겹침
  없는 배치가 필요하면 좌표를 버리고 관계(무엇이 무엇과 연결되는가)만으로 자동배치 엔진에
  맡긴다(이 프로젝트는 이미 Mermaid 가져오기로 검증된 그래프 레이아웃을 갖고 있어 그대로
  재사용했다). 같은 원리: **입력 정확도에 근본적 한계가 있는 시스템에서 "출력을 다듬는"
  전략은 그 한계 근처에서 계속 다른 증상으로 재발한다 — 다듬기 전에 "이 부정확한 값을
  아예 안 쓰는 대안이 있는가"부터 검토할 것.** (2026-08-11, `docs/ai_image_import.md`
  "P3.75가 왜 필요했나")
- **"성능이 나아졌다"와 "완전히 고쳐졌다"는 다르다 — 부분 개선 후에도 근본 원인이 하나 더
  남아 있을 수 있다.** 좌표 겹침을 없애는 관계 기반 배치로 그룹 드래그 성능이 916ms→675ms로
  개선됐지만(26%), 여전히 60fps 예산의 40배였다. 재프로파일링으로 확인한 두 번째(별개)
  원인: `_route_ortho`의 클리어런스 사다리(2026-07-27 도입, "혹 감소"용)가 **경로가 이미
  결함 없어도(base 완벽) 매번 무조건 4단×최대 2회씩 추가 A* 탐색을 돌려 더 예쁜 우회로가
  있는지 찾는다** — 도입 당시 "사다리는 base보다 엄격히 나은 후보만 채택하는 단조개선이라
  늘 실행해도 무해하다"고 판단했는데, 그 "무해"는 **정확성** 기준이었지 **성능** 기준이
  아니었다. 겹침을 없애도 이 "화장" 탐색의 고정 비용 자체는 그대로 남아, 프레임당 reroute
  호출이 많은 시나리오(그룹 드래그)에선 여전히 예산을 크게 초과했다. **"항상 실행해도
  무해하다"는 주장을 볼 때는 그게 정확성 무해인지 성능 무해인지 구분할 것** — 전자만
  확인하고 후자를 안 재면, 결과가 항상 옳아도 항상 느릴 수 있다. (2026-08-11,
  `docs/history/2026-08.md` "§8 항목18 — 좌표 신뢰 배치를 관계 기반 배치로 완전 교체")
- **AI가 생성한 SVG를 미리보기하려고 `QSvgRenderer`로 원본 텍스트를 그대로 렌더하면
  안 될 수 있다 — 프롬프트가 "색은 지정 안 해도 됨"을 허용했다면 특히.** SVG 스펙 기본값은
  `stroke:none`이라 선-아트(테두리만 있는 도형)는 통째로 안 보이고, `fill:black`이라 닫힌
  도형은 색을 안 정했어도 검은 덩어리로 뜬다 — 이 앱의 SVG 임포터(`svg_import.py`)가
  "원본 색은 애초에 무시하고 항상 앱이 다시 칠한다"는 설계라 프롬프트에서 색 지정을
  일부러 생략하게 하는데, 그 SVG를 파서 없이 `QSvgRenderer`로 곧이곧대로 그리면 그 생략이
  고스란히 빈 화면/검은 덩어리로 나타난다(A단계 진단 스크립트가 겪은 `QBrush(0)` 오해석
  검은 덩어리 함정과 본질이 같음 — 원인은 다르지만 증상이 같은 클래스). 해법: 미리보기도
  실제 삽입과 똑같은 경로(전용 파서로 아이템을 만들고 앱의 중립 펜으로 그리기)를 타게 해,
  "이 앱이 색을 무시하고 다시 칠한다"는 설계 전제를 렌더링 경로 전체에서 한 번만 지키면
  된다 — 원본 텍스트를 그대로 렌더하는 지름길은 그 설계 전제를 몰래 깨는 것과 같다.
  (2026-08-14, `docs/history/2026-08.md` "§8 항목20 AI SVG 에셋 생성 — B단계")
