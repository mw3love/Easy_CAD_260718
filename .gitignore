__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
*.spec.bak
.pytest_cache/
.DS_Store
Thumbs.db

# 참고자료 대용량 PDF (로컬만 유지, 리포 경량화)
docs/reference/*.pdf

# 오프스크린 스크린샷 산출물(tools/screenshot.py)
tools/_shot*.png

# 성능 측정·회귀 기준 지문(tools/perf_bench.py, tools/perf_baseline_check.py).
# 이 PC의 특정 시점 기준값이라 공유 대상이 아니다 — 스크립트만 커밋하고 지문은 각자 뜬다.
tools/_perf_ref/
tools/_perf_*.json

# 작업 도면·입력 사진 (사용자 산출물 — 로컬만 유지, 리포는 코드·문서만).
# 리포 루트의 .ecad만 무시한다 — 의도적으로 넣는 테스트 픽스처(tests/**.ecad)는 살린다.
/*.ecad
/송출 도면/
/실제 도면/

# Claude Code 로컬 세션 상태(머신별 설정·락파일, 리포에 공유할 내용 아님)
# handoff/ 만 예외 — 세션 인계 파일은 git으로 PC 간 동기화한다(2026-08-18, handoff 스킬 참조)
/.claude/*
!/.claude/handoff/

# 작업용 SVG 산출물(용도 미상 — 리포는 코드·문서만, 사용자 산출물은 로컬만 유지)
/broadcast_svg_set/

# superpowers subagent-driven-development 작업 워크스페이스(로컬 워크트리 스크래치) — git 추적 대상 아님
/.worktrees/
