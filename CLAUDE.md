# Easy CAD — 프로젝트 지침 (Claude Code용)

빠르고 쉬운 **순서도/간단도면 작성기**. 최종 목표는 **PDF 인쇄**, 기존 CAD 자산과 **DXF 상호운용**.
"가벼운 CAD 대체 + 쓰면서 나에게 맞추는 맞춤 프로그램". pasteflow 주석 편집기를 독립 무한캔버스
앱으로 승격한 프로젝트.

> 이 파일은 세션 연속성용이다. Claude의 프로젝트 *기억*은 세션 시작 폴더(cwd)로 키가 걸려,
> 초기 세션들(Drive 경로 `G:\내 드라이브\A1. 개인 자료\A1. AI 연습\260718 Easy CAD`)에서 쌓은
> 기억은 이 Dev 경로 세션엔 자동으로 안 딸려온다. 그래서 핵심 맥락을 여기 적어둔다.
>
> **2026-08-02 문서 분할**: 이 파일은 원래 1229줄(진행 로그 전체)이었다. 지침만 남기고
> 상세 진행 기록은 `docs/history/2026-07.md`·`docs/history/2026-08.md`로, 함정(⚠)만
> 추린 요약은 `docs/pitfalls.md`로 이관했다(코드·의미 변경 없음, git log가 나머지를 보존).
> **"무엇을 왜 했는지" 세부 경위가 필요하면 history를, "이거 전에 겪은 함정 있나"는
> pitfalls를, "다음에 뭐 할지"는 `docs/EasyCAD_계획.md` §8을 본다.**

## 확정 결정 (심층 인터뷰, 2026-07-18)
- 플랫폼: **PyQt6 데스크톱**. pasteflow(`C:\Users\7make\Dev\Paste_flow`) 편집기를 verbatim 이식해 승격.
- CAD 상호운용: **DXF 파일 왕복**(`ezdxf`, 베지어→SPLINE). 클립보드 직행은 AutoCAD 독자포맷이라 보류.
- 빌드 vs 바이: **직접 제작**(draw.io 인터페이스 불편·QCAD 과함, 사랑하는 스냅+베지어 UX를 이미 보유).
- 전체 계획서: `docs/EasyCAD_계획.md` (리포 내). 참고 이미지·PDF는 `docs/reference/`
  (구글드라이브 원본에서 이관 — 대용량 PDF는 `.gitignore`로 로컬만 유지).
  같은 이유로 **작업 산출물도 리포에 안 들어간다** — 루트 `*.ecad`와 작업 사진 폴더는 `.gitignore`
  (리포는 코드·문서만). 의도적 테스트 픽스처는 `tests/` 아래면 커밋 가능(루트 한정 패턴).

## 구조
```
easycad/
├── canvas/
│   ├── annotator_core.py   2026-08-02 분할 — 이제 아래 3개 재수출하는 얇은 shim(18줄).
│   │                       실제 코드 수정은 core_constants/core_shapes/core_view에서.
│   ├── core_constants.py   상수·아이콘·커서 팩토리(잎 모듈)
│   ├── core_shapes.py      `_HandleResizeMixin`+전체 아이템 클래스+최근접점/포트/A* 라우팅/
│   │                       그룹변형 — pasteflow verbatim 이식 + 우리 확장(지속연결 등),
│   │                       크게 편집 가능(우리 fork). 셋이 실제 순환의존이라 한 파일 유지(주석 참조).
│   ├── core_view.py        `_AnnotatorView` — 마우스/키 이벤트, 드래그선택, 스냅, 팬/줌
│   ├── host.py             CanvasWindow(창) — __init__만, 나머지는 아래 믹스인 다중상속
│   ├── host_ui.py / host_fileio.py / host_layers.py / host_style.py / host_undo.py /
│   │   host_selection.py / host_context.py / host_canvas.py   CanvasWindow 믹스인(역할별)
│   ├── host_widgets.py     독립 위젯(팔레트버튼·미니맵뷰·플로팅패널·토스트·색상팝업) + 공유 상수
│   ├── host_dialogs.py     입력 다이얼로그(용지·표제란·표·케이블채번·Mermaid·AI 게이트웨이 설정)
│   └── shortcuts.py        단축키 레지스트리(2026-08-21) — QAction·뷰 raw-key 46종 통합,
│                            설정 창에서 재할당 시 QSettings에 저장(기본값은 이 파일)
├── ai/
│   ├── gateway.py          AI 게이트웨이(Mindlogic) 클라이언트 — 키/주소 저장·모델 목록·크레딧
│   │                       조회·텍스트 호출(폴백 포함)
│   └── text_to_mermaid.py  프롬프트 빌더·코드펜스 벗기기·generate_mermaid (§8 항목18)
├── fileio/
│   ├── pdf_export.py       PDF 출력(A4~A1, 전체/선택영역)
│   ├── document.py         .ecad(JSON) 저장/열기 — 문서모델 씨앗(DXF 매핑 기반)
│   ├── dxf_export.py / dxf_import.py   DXF 왕복
│   ├── mermaid_import.py   Mermaid flowchart → .ecad
│   └── sketch_build.py     이미지→도면 빌더(Qt 비의존)
└── main.py · run.py        진입점
tests/
├── test_easycad.py         전체 실행 진입점(하위호환 shim) — python tests/test_easycad.py
├── _shared.py              공용 임포트·헬퍼(QApplication 등)
├── conftest.py             pytest용 env·sys.path 부트스트랩
└── test_part1~10_*.py      테마별 회귀 스모크 641종(개별 pytest 실행 가능)
tools/                     개발용 스크립트(앱 런타임 비의존) — perf_bench.py/profile_*.py
│                          (성능 실측·cProfile), make_perf_doc.py(부하테스트용 .ecad 결정론
│                          생성 --preset 500/1000), perf_baseline_check.py(기하+시각 지문
│                          회귀 게이트), screenshot.py(레이아웃 자체검증),
│                          ai_probe.py(게이트웨이 실측)
docs/
├── EasyCAD_계획.md          로드맵·§8 다음 순서·큰 설계 필요 항목
├── history/2026-07.md 등    월별 상세 진행 기록(완료 사항의 경위·근거)
├── pitfalls.md              ⚠ 함정 요약(재발 방지 체크용)
├── perf_plan_500_1000.md    500~1000개 부하 감소 계획·최종 수치·남은 항목(2-D)
└── reference/                참고 이미지
symbol_library/
└── symbol_library.json     "내 심볼" 팔레트 데이터(폴더·즐겨찾기·썸네일 포함) — 2026-08-20
                             까지 OS AppData에 있던 것을 리포로 이관해 git으로 PC 간 동기화
                             (`easycad/fileio/symbol_library.py._library_path()`, 사용자
                             요청 — 다른 작업산출물과 반대로 이건 의도적으로 커밋 대상).
```
실행: `python run.py` · 테스트: `python tests/test_easycad.py` · PyQt6 전역설치(Python 3.14).

## 현재 상태 (요약 — 상세는 `docs/history/`)
- **Phase 0~5 완료**: 무한캔버스 코어, PDF/.ecad, 지속연결 커넥터(A* 직교 라우팅+경유힌트),
  심볼 라이브러리(10종 — 2026-08-03 도메인 픽토그램 카메라/증폭기/랙/안테나 4종 제거),
  포트/접속점, DXF 왕복(내보내기·가져오기·INSERT/BLOCK·펜두께), Mermaid import, 이미지→도면
  빌더(`sketch_build.py`).
- **Phase 6(편집 경험 현대화 UI/UX) 진행 중** — M1~M5 완료(툴바→QToolBar, 다크모드, 플로팅
  패널, undo 저널, 정렬/분배 등). 이후 신규기능 다수 완료: 클립보드 이미지 붙여넣기·그리드/
  스냅투그리드·미니맵·스타일 복사(format painter)·케이블 자동채번·레이어 패널·캔버스-퍼스트
  레이아웃(도킹→플로팅)·DXF/.ecad 통합·8포트+라우팅 안정성 대수술·도형 채우기·색 선택 팝업·
  커스텀 심볼 팔레트(§8 항목8).
  **M6(디자인 베이크오프) 진행 중** — 버튼 색·상태(코랄 accent, checked 2단계) + 상단바
  아이콘 18종(SVG, 최종은 테마 적응 중립색 — 코랄은 버튼 상태 전용) 코드반영 완료, 새 스킬
  `design-bakeoff`(전역) + 취향 문서 `~/.claude/design-system/` 도입(2026-08-02). 이후
  실사용 피드백으로 undo/redo·snap·line 4종 재설계 + 표제란/표 흰 배경 채움 제거(테마 적응
  선·잉크색으로 전환) 완료(2026-08-03, `docs/history/2026-08.md` 참조).
- **실제 도면 기반 신규 안건(§8 항목12~15, 2026-08-03 deep-interview)** — KBS 송신소 실제
  도면 검토 후 4건 확정: ~~항목12 포트-테두리 trim 워크플로우~~ **완료**(삼각형 기본도형·
  포트 팔레트/자동스냅·테두리 trim·8포트 커넥터 연동·DXF 진짜 분절·`.ecad` 직렬화, 스모크
  365종, `docs/history/2026-08.md` 참조). ~~항목13 안테나 심플화~~ **완료**(실물 사진 5장
  대조 + artifact 시안 비교로 MW·CP다이폴·CP RING·DTV·MESH 7종 + 범용 번개 표식 1종(8종),
  2026-08-04, 스모크 378종, `docs/history/2026-08.md` 참조). 14(PDF 메뉴 통합)·15(AI
  이미지→도면)는 세부 스코프 미정 — 착수 시 deep-interview.
- **Lucid 대조 캔버스 UX 개선 시리즈(§8 항목16)** — 호버 예고점 잔상/불규칙, 미선택 도형 내부
  클릭·이동커서, 테두리 변 전체 리사이즈, 핸들 테두리 밖 오프셋, 선택 표시 2갈래(단일·화살표=
  얇은 중심선 / 다중=바깥 밴드), 직각 커넥터 세그먼트 알약 분할-드래그, 화살촉 어깨 모따기
  수정(2026-08-03) + 미선택 도형 테두리 연속 호버(2026-08-04, `_hover_port_at` 2단 이산/연속
  폴백 통일, DXF `_PathItem` 포함, 스모크 377종) + **포트 상호작용 재설계**(2026-08-04, 4라운드
  끝에 "포트=평범한 도형" + 큐닷 규칙 통일(클릭=복제/드래그=화살표만, 전 도형 공통)로 수렴,
  드래그 프리뷰 실선화+출구축 스냅, 스모크 381종)까지 **전부 완료**.
- **포트 트림 구간 스냅·호버 차단**(2026-08-09, 스모크 388종) — 포트 몸통 안쪽인데 뒤쪽 호스트
  테두리의 예고점·커넥터 커서가 뜨고 화살표까지 붙던 버그. 원인은 포트 트림이 **진짜 기하
  분절이 아니라 배경색 덮어그리기**(2026-08-03 Qt 우회)라 히트/스냅 기하만 옛 사각형 그대로
  남아 있던 것 — "화면에 그려지지도 않은 테두리"에 스냅되고 있었다(기능 버그). `_nearest_
  border`(포트 부착·슬라이드가 공유하므로 불변) 대신 호버·스냅 호출부만 `_nearest_border_
  visible`을 opt-in하고, 트림 판정은 화면 렌더와 같은 함수로 통일. **진단이 3라운드 걸린
  이유와 결정적 방법(커서 지도 격자 스캔)은 `docs/pitfalls.md` "히트테스트·법선"·"검증
  방법론" 참조** — 상세 경위는 `docs/history/2026-08.md` 같은 제목 항목.
- **포트 테두리 리사이즈 밴드 줌 비례 확대 수정**(2026-08-09, 같은 날 후속, 스모크 389종) —
  위 수정을 사용자가 확인하던 중 별도 증상 발견: 선택한 포트를 고배율로 확대하면 테두리에
  닿지도 않은 안/밖 넓은 영역에서 리사이즈 커서가 떴다. 원인은 `_box_edge_side`의 tol이
  `_EDGE_HIT_MIN`(씬단위 8.0)을 `_scale_or_1()`로 나누지 않아(다른 세 호출부는 나눔) 고배율
  (2164%)에서 밴드가 화면 86px까지 벌어진 것 — `_scale_or_1` 나눗셈 누락 계열 함정의 네
  번째 재발(`docs/pitfalls.md` "좌표계·변환" 참조). 한 줄 나눗셈 추가로 수정.
- **심볼 전략 SVG 전환 완료**(2026-08-04) — SVG 가져오기(다중 파일·개별 선택 유지·S/T 경로
  커맨드 지원)를 커스텀 심볼 팔레트(§8-8 "내 심볼")의 정식 입력 경로로 확정, 좌측 팔레트의
  "순서도" 섹션(순서도 10종+안테나 8종)을 제거해 그 자리를 대체(백엔드 `_SYMBOL_KINDS`/DXF/
  .ecad/Mermaid는 호환 유지 — 팔레트 UI만 없앰).
- **PDF 내보내기 메뉴 통합 완료**(§8 항목14, 2026-08-07) — "전체"/"선택영역" 별도 메뉴 2개를
  `_PdfExportDialog` 1개로 통합(전체/선택 라디오+용지크기·방향 콤보+라이브 미리보기), 표제란/
  용지틀이 있으면 그 용지 설정을 잠금·반영. 스모크 387종 전체 통과. 상세:
  `docs/history/2026-08.md` "PDF 내보내기 메뉴 통합 완료 §8 항목14" 참조.
- **AI 이미지→도면 — 밀집 실도면 확장 검증 완료**(§8 항목15, 2026-08-07, 코드 변경 없음) —
  deep-interview 중 "외부 API vs 게이트웨이 확장" 갈림길이 2026-07-21에 이미 답난 질문임을
  발견(`docs/image_to_ecad.md` 기존 설계·검증 존재), 진짜 남은 질문(밀집 실도면 확장성)을
  KBS 송신소 사진 구획 1건으로 실제 빌더 실행해 검증(구조 작동 확인, 자유형 배선/회로 심볼은
  어휘 밖으로 확정). 상세: `docs/history/2026-08.md` "§8 항목15 밀집 실도면 확장 검증" 참조.
- **밀집 도면 성능 버그 3건 + 텍스트 라벨 클릭 UX 완료**(§8 항목0, 2026-08-07~08) — 사용자가
  KBS 실도면(`kbs_1tv_test.ecad`)에서 "가장 중요한 문제"로 지목한 성능 버그를 3라운드에 걸쳐
  해결: ① 도형 선택(이동 없음)만으로 A* 전체 재라우팅이 폭주하던 것(`_on_scene_changed`가
  선택강조 리페인트를 실제 변경으로 오판) — 지오메트리 스냅샷 diff로 교체, 120배 개선.
  ② 화살표 세그먼트(직각 알약 핸들) 드래그 지연 — 화살표-화살표는 서로 라우팅에 무관함을
  코드로 확인 후 트리거에서 제외 + `_astar_ortho` 사전필터, 100배+ 개선. ③ 도형 자체 드래그
  지연 — `_astar_ortho`가 시작~도착 주변 회랑(코리도) 안 장애물만 먼저 시도(완전성은 별도
  대조 스크립트로 검증), 2.4~3.7배 개선(밀집 클러스터 근처는 완전 해결 아님). 별개로 사용자가
  스크린샷으로 지적한 UX도 함께 수정: 도형 중앙 텍스트 라벨을 클릭하면 라벨만 따로 선택되던
  것을 도형이 선택되도록(더블클릭해야 텍스트 편집 진입, Lucid 관례). 스모크 387종 매 단계
  재통과. 상세: `docs/history/2026-08.md` "선택 시 A* 재라우팅 폭주 성능 버그 수정"·"세그먼트
  드래그 잔여 지연 2건 추가 수정"·"도형 드래그 잔여 지연"·"도형 중앙 라벨 클릭 통과" 참조,
  함정 기록 `docs/pitfalls.md` "라우팅(A*/직교 엘보)".
- **밀집 도면(1600개) 성능 최적화 3~4단계**(§8 항목0 후속, 2026-08-08~09) — "버벅거림 최소화"
  요청으로 측정 주도 최적화 계속. 신규 도구 `tools/perf_bench.py`(시나리오별 ms)·
  `tools/perf_baseline_check.py`(기하+시각 지문 회귀 게이트) 도입. **3단계**(커밋 `a772012`):
  미니맵을 150ms 디바운스 스냅샷 캐시로 전환(레터박스 회귀 발견·수정 포함) + `_PolyArrowItem.
  boundingRect`/`_content_rect` 메모이즈(`_geom_version` 키 — 15개 기존 변경 지점이 이미
  호출하던 `prepareGeometryChange()`를 무효화 신호로 재사용) + reroute 후보 스캔 BSP
  공간쿼리화(다건 변경 시 전체스캔 폴백 안전밸브 포함) — 도형 드래그 139.6→5.1ms(-96%).
  **4단계**(2026-08-09): 유일하게 남은 60fps 초과 시나리오(뷰 렌더·전체축소, 5.2배)를
  `tools/profile_render_fit.py`(신규)로 프로파일 — `_PolyArrowItem.paint`가 전체 비용의
  46.5%, 그중 `_head_points()`/`_trimmed_body_pts()`가 매 렌더마다 처음부터 재계산되고
  있어(이미 있던 `_geom_version` 계약을 안 쓰고 있었음) 같은 캐시를 확장 적용, render_fit
  86.3→~80ms(-6%). `QGraphicsItem.setCacheMode`(Device/ItemCoordinateCache)는 "작은
  아이템 다수" 도면에서 오히려 2~9배 악화됨을 실측으로 확인(반영 안 함, 함정 기록만).
  실제 창(오프스크린 아님)에서 repaint 83ms/frame·스크린샷 육안 확인으로 자체 검증 완료.
  안티에일리어싱 끄기도 스크린샷 대조로 시험했으나 이득 6% 미만인데 테두리·연결선에 뚜렷한
  계단현상이 보여 **폐기 확정**(render_fit은 "전체 축소 보기" 코너케이스라 실사용 빈도도
  낮음 — 남은 4.8배 격차는 LOD 같은 더 큰 설계가 필요해 이번 세션은 여기서 마무리). 상세:
  `docs/history/2026-08.md` "밀집 도면 성능 4단계" 참조, 함정 `docs/pitfalls.md`
  "렌더링(QPainter/QIcon/오프스크린)".
- **포트/삼각형 기본 크기·방향 조정 — 실도면 대조**(2026-08-09, 스모크 389종) — 사용자가
  첨부한 실제 방송장비 도면 2장을 픽셀 실측해 포트 기본 18×18(폭 대비 15%, 텍스트보다 큼)이
  참고 이미지(호스트 크기 무관 절대 고정, 폭 대비 4~6%, 텍스트보다 작음)보다 3배 이상 크다는
  것과 삼각형이 꼭짓점-위 기본이던 것을 확인 — 포트 기본 12×12로 축소, 삼각형 기본 방향을
  꼭짓점-오른쪽(신호흐름 방향)으로 교체(회전 핸들은 기존 자유회전 유지). **2차 실사용
  피드백**(같은 날)으로 4건 추가 반영: 삼각형을 정삼각형화(`_tri_rect` 헬퍼, 전용 기본박스
  `_PALETTE_TRIANGLE_WH` 90×90 신설, 라벨 앵커/inset 재계산), 포트 Alt+드래그 복제(부착된
  포트가 Qt 자식이라 기존 클론 필터에서 빠지던 문제 수정), 포트 도구로 호스트 테두리를
  정확히 클릭하면 오히려 이동커서가 뜨던 역설 수정(유효 호스트 근접 시 클릭 위임 예외),
  드래그 미리보기 최소크기 클램프는 의도된 동작 확인(코드 변경 없음). 부수로 사각형 기본을
  120×72→120×120(정사각, 포트 변×10) 변경. **3차**(같은 날, 실사용 스크린샷 재현) — 큐닷
  자체는 회귀 아님(2026-08-04 4차 결정대로 포트도 예외 없이 유지, 사용자 기억이 반대였음)
  확인 후, 진짜 버그 2건 발견·수정: `_qc_create`(큐닷 클릭=복제)가 부착 포트의 호스트-로컬
  좌표를 씬좌표로 오인해 복제가 화면 밖으로 튀던 버그, `_hover_port_at` 조기반환이 Alt
  모디파이어를 확인 안 해 미선택 포트 Alt+드래그가 항상 화살표-뽑기로 새던 버그 — 둘 다
  포트 특례가 아닌 전 도형 공통 규칙으로 수정(회귀 테스트 3종 추가, 스모크 392종). 함정
  2건 위키 기록. **4차**(같은 날) — 사용자가 3차 수정을 실제 화면에서도 여전히 정렬 안
  됨으로 재확인 — `_maybe_alt_drag_copy`가 Alt+press 그 자리에서 포트 클론을 재부모화한 뒤
  곧바로 Qt 드래그-그랩에 넘기는 순서 자체가 위험하다고 판단해, 재부착을 release로 이연(
  press엔 평범한 최상위 클론만 두고 `_find_port_host_near`로 호스트탐색 로직 통합, `_commit_
  move` 이후에만 재부모화). **다만 이 하네스는 일반 도형의 라이브 드래그조차 합성 이벤트로
  재현 못 함을 재확인**(150px 이동 지시에 1px만 반영) — 구조 개선 논리는 탄탄하나 실제
  해결 여부는 사용자 재확인 대기 중(medium confidence). 코너 정렬 스냅(포트 변을 호스트
  변에 맞추는 기능)과 큐닷 전면 재검토는 당시 다음 순서로 미뤄뒀으나, 2026-08-11 재확인
  결과 실사용상 불편이 없다는 사용자 판단으로 목록에서 제외(코너 정렬 스냅은 포트가
  모서리 근처에 붙을 때 호스트 바깥으로 살짝 튀어나갈 수 있는 미세한 시각적 차이일 뿐—
  코드로 재현 가능함은 확인해뒀으나 착수 안 함). 상세:
  `docs/history/2026-08.md` "포트/삼각형 기본 크기·방향 조정"·"포트/삼각형 후속 피드백
  4건"·"포트 Alt+드래그·큐닷 실사용 스크린샷 재현"·"Alt+드래그 포트 복제 — 재부착을
  press에서 release로 이연" 참조.
- **큐닷 클릭=복제 폐지 + 호버 고스트 미리보기 제거**(2026-08-09, 스모크 392종) — "호버하면
  화살표+도형이 나와 다른 작업을 방해한다"는 피드백으로 시작. 1차로 큐닷 시스템 전체(마커·
  클릭·드래그·호버 전부) 삭제까지 갔으나 사용자가 "점(마커)까지 지우면 안 됐다, 호버 기능만
  빼 달라는 뜻이었다"고 정정해 **`git restore`로 통째로 되돌리고** 범위를 다시 좁혀 재작업.
  최종: 4변 점 마커·드래그=화살표 생성은 그대로, **클릭(드래그 없이 누르고 뗌)은 더 이상
  도형을 복제하지 않고 선택만 하며**(선택·미선택 도형 공통 규칙), **누르기 전 호버만으로
  뜨던 점선 고스트 사각형+화살표 미리보기는 완전히 제거**됐다. 미선택 도형 근접 시 뜨는 작은
  예고점(점만, 도형/화살표 없음)과 그 점 드래그로 커넥터를 뽑는 기능은 무변경. 상세:
  `docs/history/2026-08.md` "큐닷 클릭=복제 폐지 + 호버 고스트 미리보기 제거" 참조.
- **화살표 hover·화살촉 캐싱 버그 2건**(2026-08-09 6~7차, 스모크 392종) — ⓐ 선택된 화살표의
  끝점 핸들 위 hover가 "새 커넥터 생성"(Cross 커서 + 호스트 4접속점 전부)으로 잘못 신호하던
  것을 재부착 신호(PointingHand + 대상 점 1개)로 정리. ⓑ 화살표를 재라우팅할 때마다 화살촉이
  몸통과 어긋나던 버그 — 원인은 2026-08-09 4단계 성능 최적화가 심은 지뢰로,
  `prepareGeometryChange()`가 버전 증가를 `super()`보다 **먼저** 해서 Qt가 `super()` 안에서
  동기 호출하는 `boundingRect()`가 "옛 기하 + 새 버전번호" 창에 걸려 옛 화살촉을 새 버전
  캐시에 영구히 박아버린 것. 순서를 뒤집어 수정. ⚠ 교훈(**버전키 메모이즈는 `super()` 먼저,
  버전 증가는 그다음**)은 `docs/pitfalls.md` "Qt 시그널·이벤트 발화 조건"에 기록.
- **§8 항목17(범용 CAD TRIM/EXTEND) 1단계(렌더 게이트+기하 커널) 완료**(2026-08-10,
  스모크 406종) — **2026-08-09 deep-interview 스코프 확정** 후 착수. 착수 전 열린 질문 ⓐ
  (회전 도형 처리)를 선택창으로 확정: 기하 커널은 좌표계 무관 순수함수, 호출부가 host 로컬
  좌표로 변환해 넘김(Qt mapToScene/mapFromScene이 회전 자동 반영, 특례 코드 불필요). (1) 렌더
  게이트 — 2026-08-09 오프스크린 스파이크(8/8 통과)에 이어 이번엔 실제 창(`QT_QPA_PLATFORM`
  없음, 플랫폼 `windows`)+`export_pdf()`가 만든 진짜 PDF 파일(PyMuPDF 래스터화)에서도 진짜
  분절 렌더 간격을 육안·픽셀로 확인. (2) 기하 커널 — `_seg_seg_intersection`(교차점, 끝점
  접촉도 유효)·`_seg_circle_intersections`(접선 dedupe)·`_seg_ellipse_intersections`(단위원
  환원) 3종을 `core_shapes.py`에 추가(`_seg_cross_seg`는 A* 핫패스 성능 유지 위해 분리),
  `_host_outline_local_polygon` 재사용. 신규 pytest 13종(`tests/test_part8_trim_kernel.py`).
  요지: 포트 도형을 일반 도형+TRIM으로 대체, 닫힌 도형은 비파괴 cut 구간·열린 도형만
  진짜 분절, 문지르기 조작, 원/타원 포함·베지어 제외, EXTEND는 선 끝점만.
- **§8 항목17 2단계(cut 구간 모델 + 렌더 통합) 완료**(2026-08-10, 스모크 413종) — 착수 전
  열린 질문 ⓑ(타원 cut 파라미터)를 선택창으로 확정: 폴리곤 근사(정확도보다 기존 변+t 포맷·
  코드 단일화 우선, 사용자 선택) — `_host_outline_local_polygon`에 타원 분기 추가(`QPainterPath.
  addEllipse().toSubpathPolygons()`, 심볼과 `_flatten_closed_path_to_polygon` 헬퍼로 코드
  공유). `build_trimmed_border_path`를 포트 전용에서 `host._cuts`(변 인덱스+t0+t1,
  `_add_border_cut`) 일반형으로 확장 — 포트와 같은 gap 병합 로직을 타서 포트 없이 cut만으로도
  gap이 생긴다. `_RectItem`/`_EllipseItem`/`_SymbolItem.paint()`가 `_cuts` 있을 때
  `_paint_filled_trimmed_border`(채움은 `_fill_path()`로 닫힌 영역 그대로, 테두리만 진짜
  분절)로 갈아탐 — 1단계가 테스트 안 monkeypatch로만 확인했던 렌더 게이트를 실제 프로덕션
  코드로 승격. 부수 발견: 이전엔 타원이 이 폴리곤 함수에서 사각형 네 꼭짓점으로 잘못 취급되던
  잠재 버그(원형 포트 trim이 애초에 부정확했을 원인)도 같이 드러나 고쳐짐. 신규 pytest 7종
  (`tests/test_part8_trim_kernel.py`, resize 후 자국 추종 수치 검증 + 회전(40°) 호스트에서도
  cut gap이 제 위치에 뜨는지 픽셀 검증 포함), 자체 스크린샷으로 사각·타원·삼각형 3종 모두
  채움 비파괴+테두리 실분절 렌더 육안 확인.
- **§8 항목17 3단계(히트·스냅 반영) 완료**(2026-08-10, 스모크 414종) — `_nearest_border_
  visible`/`_border_pt_in_gap`(옛 `_port_covers_border_pt` 개명·일반화)이 연속 폴백에서
  cut 구간을 인식하게 확장. 착수하며 열린 질문 ⓒ를 실제 조사한 결과 **이산 4점(N/E/S/W)
  경로에도 같은 결함**이 있었다(사각형 위쪽 변 정중앙 = discrete N과 정확히 겹치는 경우로
  확인) — 신설 `_shape_ports_visible`을 `_border_snap_at`(화살표 도구 이산 스냅)·
  `_qc_snap_target`(도형 내부 드롭 흡수)·`_shape_ports_for_preview`(호버 예고점) 3곳에
  적용해 이산·연속 스냅/호버를 전부 통일. 선택된 도형 자신의 qc-dot 드래그 핸들
  (`_qc_dot_rects`)은 클릭=선택/드래그=화살표 규칙이 얽힌 별도 민감 영역이라 의도적으로
  범위 밖에 남김(4단계 재검토 예정). 신규 pytest 1종 + `docs/pitfalls.md` "검증 방법론"의
  커서 지도 격자 스캔을 실제 창(오프스크린 아님)에서 재현해 cut 경계와 호버 null구간이
  정확히 일치함을 확인, 스모크 413→414종.
- **§8 항목17 4단계(TRIM 도구 UI) 완료**(2026-08-10, 스모크 422종) — 툴바에 「자르기」 도구
  추가(단축키 `T` — 숫자 1~9는 이미 다 참, AutoCAD TR 관례). 호버하면 잘릴 구간이 빨간 점선
  으로 예고되고, 클릭 1회로 확정하거나 드래그하면 지나가는 교차 구간을 계속 커밋(문지르기,
  AutoCAD Quick Trim 관례). 핵심 계산 `_trim_candidate_segment(host, scene_pt, other_shapes)`
  는 2단계 결정("타원도 폴리곤 근사로 통일")을 그대로 재사용해 host·cutter 둘 다
  `_host_outline_local_polygon`으로 폴리곤화한 뒤 `_seg_seg_intersection` 선분-선분 교차
  하나로 처리 — 원·타원 cutter도 특례 코드가 필요 없다(1단계에서 만든 선분-원/타원 전용
  커널은 이 경로엔 안 쓰임, 5단계 EXTEND처럼 진짜 곡선 정밀도가 필요한 용도용으로 남겨둠).
  후보 스캔은 계획대로 기존 BSP 공간쿼리(`_conn_shapes_near`) 재사용(2026-08-08 전체스캔
  성능 함정 회피), 마우스 press 처리는 도형 선택/이동 분기보다 먼저 가로채야 함(2026-08-09
  포트 배치 때 겪은 "테두리를 정확히 누를수록 오히려 막히는" 함정과 같은 종류라 그 패턴을
  그대로 따름). 신규 pytest 8종(순수 계산 5종 + 실제 `_AnnotatorView`에 QMouseEvent를 흘려
  호버 예고→클릭 확정→드래그 문지르기 전체를 검증하는 종단 시나리오 3종), 기존 툴바 버튼
  개수를 하드코딩했던 회귀 테스트 2건(6→7) 수정, 스모크 414→422종. 자체 스크린샷(실제 창,
  오프스크린 아님)으로 빨간 점선 예고와 확정 후 진짜 분절 렌더를 육안 확인.
- **§8 항목17 5단계(열린 도형 분절 + EXTEND) 완료**(2026-08-10, 스모크 438종) — 대상은
  `_LineItem`/`_PolyArrowItem`(sarrow)뿐(곡선 `_ArrowItem`·`_PathItem` 제외, 계획서 확정
  범위). 닫힌 도형의 (변 인덱스, t) cut 포맷을 (seg,t) 마크 2개로 확장한 것뿐 —
  `_trim_candidate_open_segment`가 host의 여러 세그먼트 사슬을 전역순서 키(seg+t)로 훑어
  꼭짓점째로 걸친 구간도 한 번에 찾는다. 커밋(`apply_open_item_trim`)은 자국이 경로 끝에
  닿으면 host가 짧아지기만 하고, 중간이면 host는 앞쪽 조각으로 줄고 뒤쪽 조각은 `clone()`
  복제로 씬에 추가(계획서 원문 "조각 분리 시 아이템 복제") — 바인딩은 잘린 쪽만 해제,
  라벨은 host(앞쪽)만 유지하고 복제본은 라벨 없이 시작(같은 텍스트 중복 표시 회피,
  `clone()`이 라벨을 원래 안 복사하는 관례 재사용). EXTEND(`_extend_candidate`/
  `apply_extend`, 신규 커널 `_ray_seg_intersection`)는 더 가까운 끝점에서 반직선을 쏴 씬
  전체(TRIM의 국소 반경 대신 — 연장 목표가 margin 밖일 수 있음)에서 첫 교차까지 늘인다.
  TRIM 도구(4단계)를 그대로 확장해 Shift로 EXTEND 전환하되 press가 마지막 hover 값을 안
  믿고 **press 시점 modifiers로 다시 계산**(Shift 누른 채 안 움직이고 바로 클릭하는 stale
  hover 함정을 설계 단계에서 선제 차단 — 실제 창 재현에서 hover가 못 잡은 위치도 press가
  정확히 커밋함을 확인). 부수 일반화: `_trim_candidate_segment`(4단계)의 커터 순회를
  `_host_outline_local_polygon` 전용에서 `_item_local_edges`(닫힌=폐곡선/열린=비폐곡선 통일)
  로 바꿔 **닫힌 도형이 열린 도형(선)을 커터로 받는 것도 공짜로 가능**해짐(계획서 원문
  "임의 두 도형/선의 교차"). 신규 pytest 16종, 스모크 422→438종. 자체 스크린샷(오프스크린+
  실제 창 둘 다)으로 TRIM 빨간 점선 예고·3조각 연쇄 분리·EXTEND 예고·연장 후 렌더 육안 확인.
- **§8 항목17 6단계(직렬화·DXF·undo) 완료**(2026-08-10, 스모크 447종) — `.ecad`는
  `item_to_dict`/`dict_to_item`에 `cuts` 필드 추가(포트와 같은 관례, 없으면 키 생략 —
  하위호환). DXF는 `_export_trimmed_border` 게이트를 `_ports` 단독 판정에서 `_ports or
  _cuts`로 확장(`_export_rect`/`_export_symbol`) + `_export_ellipse`에 그 분기가 아예
  없던 잠재 버그(원형 포트/cut이 DXF에서 이제껏 하나도 안 잘려 나갔음)를 같은 김에 수정.
  undo는 `group`(`_group_id`)과 같은 전용 `mut` sub(`"cuts"`, `push_undo_cut`)를 닫힌
  도형용으로 신설했지만, **열린 도형 TRIM/EXTEND는 새 sub가 필요 없었다** — 기존
  `capture_geom()`/`apply_geom()`이 이미 pts·라우팅·바인딩까지 통째로 포괄해 host 쪽은
  그대로 재사용, 분리로 새로 생기는 clone은 `push_undo_add_many`와 같은 "여러 op 한
  엔트리" 패턴(`push_undo_open_trim`)만 얹었다. 문지르기 드래그 전체가 undo 1스텝으로
  뭉치는 것도 `push_undo_move`가 이미 쓰던 `coalesce_key`(`object()` 세션 키) 패턴 재사용
  뿐 — 서로 다른 host의 mut을 한 엔트리에 순서대로 누적하는 기존 `_coalesce_into` 동작이
  다중 조각 연쇄 분리에도 그대로 맞아떨어짐(pytest로 실제 undo/redo 왕복 수치 확인). 신규
  pytest 9종, 스모크 438→447종.
- **§8 항목17 7단계(포트 흡수) 완료 — 항목17 전체 종료**(2026-08-10, 스모크 449종) —
  팔레트에서 「포트□」/「포트○」만 제거(백엔드는 그대로라 기존 `.ecad`가 그대로 열림).
  `_RectItem`/`_EllipseItem`/`_SymbolItem.paint()` 게이트를 `_cuts` 단독에서 `_ports or
  _cuts`로 넓히고 옛 `_paint_port_cover_if_needed`(배경색 덮어그리기, 2026-08-03 Qt 버그
  우회) 호출을 삭제 — 포트도 `build_trimmed_border_path`(2단계부터 `_ports`·`_cuts` 둘
  다 이미 병합)로 진짜 분절 렌더를 탄다. 착수 전 조사에서 뜻밖의 발견: **`_SymbolItem.
  paint()`는 이 호출 자체가 없어서 삼각형에 포트를 붙여도 지금까지 시각적으로 전혀 안
  잘렸다**(항목12부터 있던 별개의 잠재 버그, 같은 게이트 확장으로 함께 수정). 신규
  pytest 2종, 스모크 447→449종. **§8 항목17(범용 CAD TRIM/EXTEND) 1~7단계 전부 완료**
  (2026-08-04 제안 → 2026-08-10 종료, 스모크 393→449종) — 옛 포트-테두리 trim(항목12)
  전용 특수 케이스를 흡수해 범용 TRIM/EXTEND 시스템 하나로 통일. **다음 순서**: 밀집
  도면 성능은 이전 세션에서 폐기 확정한 AA/LOD 축을 빼면 마무리(LOD 재착수는 별도
  deep-interview 필요) + 기존 "다음 순서"·"큰 설계 필요" 목록(메뉴 전용 아이콘 8종
  SVG화 등) — `docs/EasyCAD_계획.md` §8 참조.
- **§8 항목17 후속 — 실사용 버그 4종 + EXTEND 짝 기능 신설**(2026-08-10, 같은 날 후속,
  스모크 451종) — 항목17 "완료" 직후 실사용 재현(스크린샷·동영상 여러 라운드로 원인을
  못 잡아 코드에 임시 재현 로그를 심어 사용자가 재현→Claude가 로그 직접 확인하는 방식으로
  전환, 상세 `docs/pitfalls.md` "검증 방법론")으로 5건 발견·수정:
  ① **포트 정렬-스냅 간극** — 스마트 정렬(`_apply_smart_snap`)이 `_content_rect()`(선/
  타원/화살표/DXF패스는 재귀방지·hit-test 여유로 펜폭만큼 부풀린 값)를 "보이는 외형"으로
  오용해, 정렬 스냅이 발동해도 실제 선과 간극이 남았다 — 해당 클래스는 패딩 없는 실제
  기하(끝점/정점/rect/path bbox)를 쓰도록 분기.
  ② **TRIM 드래그를 진짜 Fence로 재설계** — 기존 "문지르기"(커서가 그 순간 테두리 위에
  있어야만 잡힘)는 빠른 드래그에서 중간 프레임을 놓쳤다. AskUserQuestion으로 확정한 대로
  press~현재 구간을 seg-seg 교차(`_fence_crossings`)로 훑는 고전 AutoCAD Fence 방식으로
  전환(커서가 테두리 위에 있을 필요 없음), 대상 테두리를 따라 그대로 겹쳐 드래그하는
  평행선 케이스는 기존 판정을 보완으로 병행. 드래그 궤적을 빨간 점선으로 렌더.
  ③ **정렬 스냅 "중심-변" 교차 조합 재추가** — "중간점끼리는 붙는데 하나만 중간점일 땐
  안 붙는다"는 실사용 보고로 확정: 이 조합은 2026-08-01에 "과발화 원인"으로 한 번
  제거됐던 것인데, 지금 방식(동률 후보 전부를 동시에 보여주는 방식)이 그 문제를 이미
  흡수하므로 재추가해도 재발하지 않음을 실사용 로그로 재확인.
  ④ **TRIM 도구 클릭을 가로채던 "도구 무관 조기반환" 3종** — qc-dot 접속점(`_connect_
  port_at`)·더블클릭 라벨편집(같은 자리 빠른 재클릭이 Qt에서 `mouseDoubleClickEvent`로
  새는 경우)·선택된 끝점 드래그 핸들이 전부 `current_tool` 확인 없이 TRIM 클릭을
  가로챘다(과거 Alt+드래그 버그와 같은 클래스, `docs/pitfalls.md` "상호작용 설계" 참조 —
  하나를 찾은 뒤 나머지 둘을 감사(audit)했어야 했는데 사용자가 새 증상을 보고할 때마다
  순차 발견했다는 것도 함께 기록).
  ⑤ **EXTEND의 짝 기능 신설: 닫힌 도형 TRIM 자국 복구** — 사용자가 실제로 하던 작업은
  "선을 늘이는" 게 아니라 "사각형 테두리 자국을 되돌리는" 것이었음이 밝혀져(EXTEND가
  선 끝점만 다루던 기존 스코프엔 대응 기능이 없었음), Shift+EXTEND가 열린 도형에 늘일
  끝점이 없으면 닫힌 도형의 `_cuts` 자국 복구로 폴백하는 대칭 기능을 신설
  (`_restore_cut_candidate`, 예고는 초록 점선 — restore·extend 둘 다 "생기는" 예고라
  통일, TRIM의 "지워지는" 빨강과 구분). 신규 pytest 2종, 스모크 449→451종. 진단에
  쓴 임시 로그(`_dbg()`)는 **사용자 요청으로 코드에 그대로 유지 중**(다음 버그도 같은
  방식으로 잡을 예정) — 삭제 시점은 사용자 승인 후. 상세 경위 `docs/history/2026-08.md`
  같은 제목, 재사용 가능한 디버깅 기법은 개인 위키
  (`~/.claude/wiki/easycad-실시간재현로그-스크린샷추측대체.md`)에도 기록.
- **스마트 정렬 스냅·TRIM 자국 UX 개선 시리즈**(2026-08-10, 같은 날 후속, 스모크
  393→477종) — "삼각형 두 변이 사각형 잘린 자국에 snap으로 붙을 수 있나"는 질문에서
  출발해 실사용 재현(고배율 줌·`.ecad` 파일 대조)으로 근본 원인 여러 건을 순차 발견·수정.
  스냅 계열: 정점 스냅 도입(`_real_snap_vertices_local`) → 회귀 발견 → `srect()`를
  `_tight_scene_bbox`(패딩 없는 실제 외곽선)로 통일하는 근본 수정(사각형·다중선택 그룹
  박스 둘 다 해당) + 원래 질문에 답하는 "자국 복구 스냅"(`cut_restore_snap_delta`, TRIM
  cut의 두 경계점에 도형 변 두 개가 정확히 지나도록 강체이동 역산) 신설. 자국(cut) 계열:
  선택 강조선이 잘린 모양을 무시하던 버그 수정, 자국 경계를 마름모 선택점으로 표시+드래그
  조절 신설(`_cut_handle_rects`), 같은 변 반복 TRIM 시 자국 리스트 자동 병합
  (`_merge_cuts_list`). 선 도구: 화살표 전용이던 테두리 스냅(그리기·유휴 예고 둘 다)을
  Line에도 확장. **삼각형 근본 재설계**(여러 라운드 시행착오 끝에 Lucid 대조로 확정) —
  정삼각형 내접(`_tri_rect`)이 리사이즈 핸들·qc-dot·자국 핸들이 실제 꼭짓점과 어긋나는
  근본 원인이었음을 파악, `_sym_triangle`이 이제 bbox를 그대로 채우고(비정삼각형 이등변,
  기본 생성 크기 90×77.94로 정삼각형 유지) 관련 특례 코드를 대부분 삭제(`_tri_rect` 자체도
  죽은 코드가 돼 삭제). 상세 경위 `docs/history/2026-08.md` 같은 제목, 함정 기록
  `docs/pitfalls.md` "좌표계·변환"·"히트테스트·법선" 참조.
- **도형 안쪽 호버 마커 미표시 버그 수정 + 격자 기본값 off 전환**(2026-08-11, 스모크
  477종) — 도형 몸통 안쪽 호버 시 변 중심점 예고 마커가 안 뜨던 버그(2026-08-09에 겹친
  도형 억제 로직을 추가하며 "자기 자신"까지 억제 대상에 걸린 부작용) 수정 — `_port_dot_
  target`의 occluder 판정을 자기 자신 제외로 좁혀, 이동 커서 유지 + 마커 표시를 동시에
  만족(Lucid 관례). 겹친 도형 억제(원래 목적)는 재확인해 보존. 부수로 격자 기본을 켜짐→
  꺼짐으로 전환(`host.py`/`host_ui.py` 두 곳). 상세 `docs/history/2026-08.md` 같은 제목.
- **§8 항목18 AI 이미지→도면(앱 내장) — 설계 확정, 구현 대기**(2026-08-11, 코드 변경 없음)
  — 항목15가 "Claude Code 세션 안에서 사람이 돌리는 워크플로"(`docs/image_to_ecad.md`)로
  닫혔던 것을 **앱 런타임**으로 승격하기로 확정(사용자 소유 Mindlogic/factchat 게이트웨이).
  앞단만 게이트웨이 호출로 바뀌고 뒷단 `sketch_build.py` 빌더는 그대로 재사용, 중간 표현은
  빌더 API를 미러링한 좁은 JSON(Mermaid 배제 — 좌표가 없어 원본 배치를 버림). 같은 날 실제
  키로 게이트웨이를 찔러 ⓐ `json_schema` 구조화 출력 작동 ⓑ `claude-*` 계열은 밀집 도면
  **전체 이미지에서 504 타임아웃**(gemini 계열은 44초에 처리) ⓒ 구획 크롭 3배 확대가 작은
  서브박스 인식을 실제로 살린다(전체 패스는 통째로 누락)는 것까지 측정 — 그래서 진짜 구조는
  "개괄→에셋 2단계"가 아니라 **전체 훑기→구획 타일링**이고, 사용자에겐 입력창 하나로만
  보인다. **설계·실측·A~D단계 검증 기준 전문: `docs/ai_image_import.md`**, 실측 재현 도구는
  `tools/ai_probe.py`(환경변수 `EASYCAD_GW_KEY`), 함정은 `docs/pitfalls.md` "AI 게이트웨이
  호출(이미지→도면)".
- **§8 항목18 AI 이미지→도면 — A+B단계(게이트웨이 클라이언트 + 헤드리스 파이프라인) 구현·
  실도면 검증 완료**(2026-08-11, 다른 PC에서 인계한 세션, 스모크 501종) — 위 설계 그대로
  구현. A단계 `easycad/ai/gateway.py`(모델 폴백 트리거를 `model_not_found`뿐 아니라
  `timeout`까지 확장 — 이 게이트웨이의 실패 모드가 밀집 도면 504이므로), B단계
  `tools/ai_sketch.py`(P1 개괄→타일링 판단→P2 타일→좌표 복원→P3 병합(IoU 중복제거가
  타일경계 연결 잇기의 실제 구현)→`Sketch`→`.ecad`). 신규 pytest 21종(mock) + 실키로
  A단계 3항목(모델목록 79종·크레딧 잔액·이미지 왕복) 전부 확인 + `docs/reference/image
  (1).png`(KBS 1TV 실도면) 실행해 P1 29개→P3 최종 128개로 baseline(39, "타일링하면 뚜렷이
  많아져야 성공")을 3배 이상 충족, 렌더 육안 확인(다중 TX 체인 토폴로지·"모악산 On-Air
  Receiver" 등 도메인 라벨 정확 추출)까지 마침. 부수 발견 둘: 자동 타일링이 만든 타일이
  수동 실측 크기보다 커 claude가 6개 중 4개는 여전히 타임아웃(폴백은 정상 동작, 다음 튜닝
  지점), Windows 한국어 로캘(cp949)에서 콘솔 한글 출력이 깨져(파일 자체는 UTF-8로 정상)
  `_fix_console_encoding()` 추가. D단계(에셋 패스)는 스코프 밖. 상세:
  `docs/history/2026-08.md` "§8 항목18 AI 이미지→도면 — A+B단계", 함정 2건은
  `docs/pitfalls.md` "AI 게이트웨이 호출"·"검증 방법론".
- **§8 항목18 C단계(앱 통합) 구현·실제 창 검증 완료**(2026-08-11, 같은 날 후속, 스모크
  393→510종) — 사용자가 "빠른 테스트"를 위해 입력창까지 요청, 취소·원본 언더레이 대조는
  스코프 축소로 제외. 파이프라인을 `tools/ai_sketch.py`에서 `easycad/ai/sketch_pipeline.py`로
  이동(앱이 `tools/`에 의존하면 방향이 꼬임), `document.insert_items()` 신설(`load_document`의
  3-pass 로직을 씬을 안 지우는 버전으로 공유), 이 코드베이스 첫 `QThread`
  (`host_ai._AISketchWorker` — 게이트웨이 왕복이 몇 분이라 GUI 스레드 블로킹 방지, 씬 조작은
  전부 완료 콜백에서 메인 스레드가 함), 입력·진행 다이얼로그(`host_dialogs.py`), 메뉴
  "AI 이미지→도면…"(Ctrl+Shift+A). 삽입 후 뷰 중심으로 오프셋 + `build_elbow()` 명시
  재라우팅(Mermaid 가져오기와 동일 관례), `push_undo_add_many`로 undo 1스텝. 신규 pytest
  8종(mock) + **실제 창(오프스크린 아님) + 실제 게이트웨이 호출** 종단 검증(파일 선택
  다이얼로그만 monkeypatch) — shapes 13·edges 10 정상 삽입(기존 도형 보존), undo 1회로
  정상 복원, 스크린샷 육안 확인. 부수로 게이트웨이 키를 secrets 파일(`~/.claude/.secrets/
  easycad-gateway.key`, `jbnu-gateway` 스킬과 동일 관례이나 다른 계정)에서도 읽게 확장 —
  `!` 프리픽스 셸 문법 실수로 키가 대화에 노출된 사고의 재발 방지. 상세:
  `docs/history/2026-08.md` "§8 항목18 C단계".
- **§8 항목18 C단계 후속 — 빠른 개선 3종**(2026-08-11, 같은 날 후속, 스모크 510→515종) —
  실사용 중 나온 요청 4가지 중 3가지 구현: 다이얼로그 전용 드래그드롭+Ctrl+V(캔버스 자체
  붙여넣기/드롭은 무변경, `_AIImageImportDialog`만 확장), 진행률+경과시간(정확한 ETA는
  호출 편차 15~56초로 포기, `[P2 타일 i/N]` 로그를 정규식으로 파싱한 결정형 막대+경과
  타이머), 모델 선택 드롭다운(`gw.list_models()` 실호출로 전체 노출 + 실측 기본값
  "(추천)" 표시, `list_models`에 짧은 timeout 인자 신설 — 다이얼로그 생성이 동기 호출이라
  네트워크 죽으면 기본 600초를 물려받던 문제 방지). **툴바 버튼 노출은 보류** — 착수 전
  조사에서 과거에 정반대 방향(이미지·표제란·표·Mermaid·SVG 5종을 툴바에서 빼서 메뉴
  전용으로 바꾼 이력, "아이콘만으론 구분 어렵다"는 사용자 피드백)이 있었고 memory에도
  "상단 툴바에 종류를 늘리지 말고" 원칙이 남아 있어 사용자에게 충돌을 알리고 확인 대기
  중. 신규 pytest 9종 + 실제 창 검증(모델 79종 실채움, Ctrl+V 실제 임시파일 생성, 드롭
  합성이벤트, 전체 파이프라인 재실행). 상세: `docs/history/2026-08.md` "§8 항목18 C단계
  후속 — 빠른 개선 3종". **툴바 버튼은 사용자가 "메뉴+단축키만 유지"로 확정**(같은 날
  후속) — 코드 변경 없음.
- **§8 항목18 C단계 후속 — Ctrl+V 실버그 수정 + 썸네일 + 크레딧 표시 + 모델 비용
  재검토**(2026-08-11, 같은 날 후속, 스모크 515→519종) — 방금 만든 드래그드롭+Ctrl+V를
  실사용하자마자 Ctrl+V만 안 됨을 발견: `QDialog.keyPressEvent` 오버라이드는 포커스
  위젯(`QLineEdit`/`QPlainTextEdit`)이 표준 붙여넣기 키를 자기 keyPressEvent에서 먼저
  처리·accept()해버려 부모까지 이벤트가 안 올라오는 흔한 Qt 함정 — 포커스 위젯 자신에게
  `installEventFilter`를 거는 방식으로 교체(`docs/history/2026-08.md` 같은 제목).
  `QTest.keyClick`으로 진짜 이벤트 디스패치까지 재현해 확인. 같이 요청받은 썸네일
  미리보기(`QPixmap.scaled`)·상태바 크레딧 잔액 표시(워커 스레드에서 `get_credit_balance`
  한 번 더 호출, 실패해도 본 결과엔 무영향)도 구현. 별도로 사용자가 KAIROS 대시보드에서
  `claude-sonnet-5` 호출 2건이 각각 125~164 크레딧(같은 시간대 gemini는 5~18)임을
  보여주며 비용 문제 제기 — 같은 크롭으로 4모델(gemini-3.6-flash·claude-sonnet-5·
  claude-haiku-4-5·gpt-5.4-mini) 재비교 결과 **gpt-5.4-mini가 claude-sonnet-5와
  shapes·edges 완전히 같으면서 비용 1/18·속도 1/6**이라 P2 기본을 교체(정확도만 보고
  기본값을 정하면 비용을 놓친다는 교훈, `docs/pitfalls.md` "AI 게이트웨이 호출" 참조).
- **§8 항목18 C단계 후속 — 수동 붙여넣기 모드 신설(설계 원안엔 없던 신규 기능)**
  (2026-08-11, 같은 날 후속, 스모크 519→526종) — 모델 비용 재검토 직후 사용자가 더
  근본적인 구조 제안: "크레딧 바닥나면 못 쓰는 구조인데, 프롬프트 안내하고 답변 붙여넣으면
  도면으로 작동하는 방식도 좋을듯". KAIROS Chat이 같은 kairos 계정 크레딧을 쓸 가능성이
  높다고 사용자가 확인해줘 릴레이 대상에서 제외하고, 완전히 별도 정액제 AI(Claude Code·
  claude.ai·chatgpt.com 등)를 겨냥해 범위 확정 후 착수. `manual_prompt()`(JSON 출력
  형식을 예시까지 명시 — API의 구조화 출력 강제가 없으니 프롬프트 텍스트가 유일한 형식
  강제 수단)·`build_from_manual_json()`(게이트웨이 호출 0회로 바로 `Sketch` 변환, P1
  단일 패스 취급이라 `axis_snap`/`dedupe_shapes`는 생략) 신설. 다이얼로그에 "수동 모드"
  체크박스로 자동/수동 그룹 전환, OK 핸들러가 수동일 때만 JSON을 미리 검증(실패해도
  다이얼로그를 안 닫음 — 재입력이 바로 되게). `host_ai.py`가 수동이면 `QThread`/진행
  다이얼로그 자체를 안 띄움. auto/manual 공통 삽입 로직은 `_finish_ai_insert`로 추출.
  신규 pytest 8종 + 실제 창 검증(수동 JSON 렌더 결과 육안 확인, 워커 미기동을 mock으로
  확인 — 게이트웨이 호출 0회가 코드로도 보장됨). 상세: `docs/history/2026-08.md`
  "§8 항목18 C단계 후속 — 수동 붙여넣기 모드 신설".
- **§8 항목18 — 실사용 품질 피드백 2건 수정: 중복박스 병합 + 타일 경계 연결선 누락
  (P3.5 신설)**(2026-08-11, 같은 날 후속, 스모크 526→536종) — 사용자가 실제 KBS
  "1RFM(5kW)" 도면 사진과 변환 결과를 나란히 보여주며 "생각보다 실망스럽다"고 피드백,
  코드로 직접 원인 분석. ① **중복박스** — 실측: 같은 실물이 서로 다른 타일에서 두 번
  인식(IoU 0.48 쉼표공백차, IoU 0.12 정밀도차). 순수 IoU만 낮추면 "PIC-FM"이 그 안의
  "Audio(A)"를 감싸는 것 같은 라벨이 다른 진짜 별개 구성요소(IoU 0.11~0.35)까지 잘못
  합쳐질 위험이 있어, `dedupe_shapes`를 "순수 IoU≥0.4 OR (라벨 정규화 일치 AND
  IoU≥0.05)" 이중 신호로 재설계. ② **타일 경계를 넘는 연결선 통째 누락** — 같은 파일
  실측: 실제 도형 27개 중 11개가 화살표 0개, 도면 중앙 핵심 블록 "PIC-FM" 포함(원본은
  여러 선이 드나드는 중심축). 원인은 edges가 "양 끝이 같은 타일 안에서 함께 인식됐을
  때만" 살아남는 구조라, 페이지를 가로지르는 긴 선은 타일 오버랩만으론 못 잡음(설계
  문서에 이미 문서화된 한계 — 실제 영향 크기는 이번에 처음 확인). AskUserQuestion으로
  사용자가 "새 단계 추가"를 선택해 **P3.5 연결선 보완**(`complete_edges`) 신설 — P3
  병합 직후 전체 원본 이미지 + 병합된 shapes 목록을 다시 모델에 보내 "이 도형들 사이
  실제로 그려진 선만" 찾게 함(스키마를 edges 전용으로 좁혀 새 도형 생성 여지 차단 +
  "실제로 그려진 선만" 환각방지 프롬프트 유지, 타일링 트리거 시에만 실행, 실패해도
  파이프라인은 P3까지 결과로 계속 진행). 신규 pytest 10종 + **실측 검증**(사용자가 준
  원본 사진 24MP 그대로 재실행) — 고립 도형 비율 41%→12.5% 감소, "PIC-FM" 연결 복원,
  중복박스 0건, 렌더 스크린샷으로 흩어진 배치가 응집된 신호흐름 형태로 바뀜을 육안
  확인. 별도로 사용자가 물어본 "생성형 이미지로 먼저 단순화 후 처리" 아이디어는
  비추천 결론(이미지 생성 모델은 정확한 텍스트·구조 보존을 보장 못 해 오히려 정확도를
  해칠 위험 — 이 프로젝트가 이미 두 번 겪은 환각 함정과 같은 종류, 결정적 Pillow
  전처리가 더 안전한 대안으로 남겨둠). 상세: `docs/history/2026-08.md` "§8 항목18 —
  실사용 품질 피드백 2건 수정", 함정 3건은 `docs/pitfalls.md` "AI 게이트웨이 호출".
- **§8 항목18 — 좌표 신뢰 배치를 관계 기반 배치로 완전 교체**(2026-08-11, 같은 날 후속,
  스모크 536→542종) — 위 수정에도 사용자가 실제 도면 재비교로 "여전히 못 쓰겠다" 재보고
  (스턱루프 트립와이어, 규칙 11-b — 3번째 임계값 패치 대신 재진단). 근본 원인은 vision
  모델의 절대 픽셀 좌표 추정 자체가 부정확하다는 것(실측 3회 확인)이었고, 이게 지저분함
  **뿐 아니라 성능까지** 파국적으로 망가뜨리고 있었다 — 사용자가 지적한 "그룹 드래그
  버벅임"을 `perf_bench.py`로 실측: 20개 그룹 드래그 916ms(60fps 예산 55배, 1600개짜리
  기존 스트레스 문서보다 도형 140개인 이 문서가 44배 더 느림). cProfile 확인: 시간의
  96%가 A* 자동배선 — 겹친 도형 더미 사이 경로를 못 찾아 클리어런스 사다리를 8~10회씩
  재시도. 지저분함·버벅임이 같은 뿌리(좌표 신뢰 배치)의 두 증상이었던 것. 사용자가
  "완전 교체"(옵션 안 남김)와 "방향성만 약하게 반영"을 확정 후 신규 `layout_graph()` —
  P1~P3.5가 그대로 뽑는 관계 정보(shapes+edges)만으로 Mermaid 가져오기가 이미 검증해
  쓰는 `layout_positions()`(그래프 BFS 레벨 배치)를 재사용해 새로 배치(규칙 2 손안의
  카드 — 새 레이아웃 알고리즘 안 만듦). 방향은 rough 좌표 bbox 비율로 추정, 셀 크기는
  최대 도형 크기+여백이라 겹침이 원리적으로 불가능. `axis_snap()`(격자 배치라 스냅할
  미세오차가 안 생겨 완전히 무의미해짐)은 함수·전용테스트 통째로 삭제(규칙 8 고아 정리).
  신규 pytest 7종 + **실측**(사용자 실제 laggy 파일을 그대로 재구성) — 겹침 0건, 그룹
  드래그 916ms→675ms(26% 개선), 렌더 스크린샷으로 흩어진 배치가 뚜렷한 좌우 컬럼
  구조로 바뀜을 확인. **다만 완전한 해결은 아니다** — 재프로파일링으로 별개의 두 번째
  원인 발견: `_route_ortho`의 클리어런스 사다리가 경로가 이미 완벽해도 매번 무조건 추가
  A* 탐색을 도는 설계(2026-07-27, "무해"는 정확성 기준이지 성능 기준이 아니었음)라
  겹침을 없애도 이 고정비용은 그대로 남음. 이 코드는 프로젝트에서 가장 스턱루프가 잦았던
  민감 영역(768케이스 회귀 스윕 이력)이라 성급한 패치 없이 사용자 확인 대기 중. 상세:
  `docs/history/2026-08.md` "§8 항목18 — 좌표 신뢰 배치를 관계 기반 배치로 완전 교체",
  함정 2건은 `docs/pitfalls.md` "AI 게이트웨이 호출" 절 끝부분.
- **§8 항목18 — 클리어런스 사다리 조건부 건너뛰기(그룹 드래그 916ms→156ms, 5.9배)**
  (2026-08-11~12, 같은 세션 후속, 스모크 542→544종) — 남은 성능 문제를 이어서 수정. 원인은
  `_route_ortho`의 클리어런스 사다리가 base가 이미 결함 없어도 매번 무조건 추가 A* 탐색
  (최대 4단×2회)을 도는 것 — `fast` 파라미터를 `_route_ortho`→`build_elbow`/
  `_route_with_hints`→`_apply_routing`→`reroute`→`host_canvas._on_scene_changed`까지
  체인 전체에 추가(기본값 False로 기존 동작 완전 무변경), `_on_scene_changed`가 이미
  쓰던 "다건 변경 판정"(`_last_geom_change_count > _FEW_CHANGED`)을 그대로 재사용해
  `fast=many_changed`로 넘김. **정확성 검증 과정에서 자기 진단을 스스로 뒤집은 사례** —
  처음엔 무조건 건너뛰다 자체 검증에서 "화살표 5개가 도형 관통"을 발견해 회귀로 확신하고
  `base_score`(이미 계산된 값, 비용 0) 체크로 고쳤는데, 같은 수정에 같은 스크립트를 다시
  돌려도 여전히 5건이 나와 재확인하니 **위반 판정 자체가 검증 스크립트의 버그**였다 —
  도형 경계를 `_obstacle_rects()`가 쓰는 원본 `.rect()`가 아니라 펜폭 패딩 포함
  `.boundingRect()`로 잰 것(이 문서 "좌표계·변환" 절에 이미 기록된 함정을 검증 스크립트
  작성 중 그대로 재현). 바로잡아 재니 수정 전/후 코드 모두 위반 0건 — 애초에 실제 회귀는
  없었다. `base_score` 체크는 실증된 회귀 수정이 아니라 무비용 방어적 안전장치로 코드·
  pytest 주석 모두 정정(전역 규칙 11-c를 자기 진단 서사에도 적용). 신규 pytest 2종 +
  `perf_baseline_check.py`(정적 렌더 무변경 확인) + 1600개 문서 무회귀 확인. 상세:
  `docs/history/2026-08.md` "§8 항목18 — 클리어런스 사다리 조건부 건너뛰기", 함정은
  `docs/pitfalls.md` "검증 방법론" 절 끝부분.
- **웹 트랙 병행개발(2026-08-05 시작 → 2026-08-06 일단 중단, Python 집중으로 전환)** —
  `web_prototype/`(이 리포 하위, PyQt 본체와 별개 코드, `easycad/`는 안 건드림)는 Python(PyQt)
  앱과 **둘 다 끝까지 만드는** 목표 자체는 유지하되(§8 항목10), 사용자가 2026-08-06 하루
  집중개발 끝에 "캔버스 인터랙션 코어는 예상보다 빠르게 따라잡았지만 생각한 만큼의 개발
  속도는 아니었다"고 판단해 **당분간(재개 시점 미정) 웹 개발을 멈추고 Python(easycad/)에
  집중하기로 결정**(폐기 아님 — 다시 시작할 근거·코드는 그대로 남겨둠). 아래는 그날까지의
  진행 기록. 시작은 "playwright
  자동검증만으로 상호작용 버그를 화면 없이 잡을 수 있는가" 가설 검증이었다: 1차 자동검증만으로
  실버그 1건(A* 그리드반올림 대각선), 2차 지속연결(도형 이동→화살표 재라우팅) 1차 구현에서 버그
  없이 통과, 사용자가 실제 브라우저로 손맛 확인 완료("나쁘지 않다") → 병행개발 결정으로 이어짐.
  3차(인터랙션
  스트레스테스트): 다중선택(고무줄 밴드)+그룹드래그+Delete/Escape 추가, 8도형·6화살표 교차
  자동검증 통과, 17도형·22화살표 규모에서 A* 폴백 대각선 버그 1건 발견+수정. 성능도 같은
  라운드에 해결 — 매 pointermove 전체 화살표 재계산이 60fps 예산을 초과함을 실측(21.8ms/frame)
  → 연결+근접 화살표만 재계산하고 드롭 시 1회 전체 재계산하는 하이브리드로 교체, 20도형·31
  화살표 규모에서 2.15ms/frame(10배)로 재측정 확인. 추천 방향은 전면 재작성이 아니라 하이브리드(DXF/PDF는 기존
  Python 유지, 캔버스/인터랙션만 웹). 4차(2026-08-06): 저장/불러오기(파일 다운로드/업로드, JSON —
  도형+화살표 포트참조 직렬화, 깨진 파일 업로드해도 기존 문서 보존) 실클릭+실파일 왕복으로
  검증 완료. 5차(같은 날): undo/redo — 저널 엔트리 4종(이동/도형생성/도형삭제(연결화살표
  cascade 복원)/화살표생성), Python `host_undo.py`와 다른 데이터모델에 맞게 새로 설계, 툴바
  버튼+`Ctrl+Z`/`Ctrl+Shift+Z` 단축키까지 실이벤트로 검증 완료. 6차(같은 날, 사용자가 "묻지
  말고 자체판단해서 쭉 진행"으로 위임): 팬/줌(무한캔버스) — 고정 800×500 뷰포트였던 상태에서
  SVG `viewBox` 직접 조작으로 휠=커서고정 줌/휠버튼드래그=팬 구현(Python `core_view.py` 관례
  그대로: 배율 1.15, 화면px 히트반경을 줌으로 나눠 무관하게 유지), A* 라우팅 탐색범위가
  800×500으로 하드코딩돼 원점에서 먼 도형끼리는 못 잇던 버그를 도형 bounding box 기반
  동적범위로 함께 수정. 7차(같은 날): 도형 라벨(텍스트) — 더블클릭 시 도형 위에 정확히
  겹치는 플로팅 `<input>`(HTML)으로 인라인 편집(SVG `<text>` contenteditable은 IME 호환성이
  나빠 회피), Enter=커밋/Escape=취소/blur=커밋, 편집 중 Delete/Backspace가 캔버스 단축키로
  새는 것을 사전에 막는 가드 추가, undo/redo(`label` 엔트리)·복제·저장불러오기 전부 라벨 관통
  확인. 8차(같은 날): 그리드/스냅투그리드 — SVG `<pattern>` 점격자를 캔버스 전체를 덮는
  `<rect>`에 채워 표시(팬/줌 중 `viewBox` 변화를 그대로 따라가 별도 갱신 불필요), `G`키+버튼
  토글, 단일 도형 드래그·더블클릭 신규생성 좌표에만 20단위 스냅(Python처럼 다중선택 그룹드래그
  는 제외). 구현 중 자체 발견: 격자 `<rect>`가 기본 pointer-events로 클릭을 가로채 빈캔버스
  더블클릭·마퀴선택을 깰 뻔한 문제를 검증 전 `pointer-events:none`으로 선제 수정. 9차(같은 날,
  사용자 스크린샷으로 정정받은 뒤): 도형 리사이즈 + 포트 8→4 축소 — 사용자가 실제 Python UI
  스크린샷으로 "포트와 리사이즈 핸들이 충돌한다"는 내 오판을 정정(포트는 테두리에서 gap만큼
  띄워 뜨고, 리사이즈는 그 안쪽 테두리 선·모서리 핸들이 담당해 원래 안 겹침) — 이 과정에서
  Python이 2026-07-30에 상시포트를 8개(N/E/S/W+대각)에서 4개(N/E/S/W)로 이미 줄인 사실도
  발견, AskUserQuestion으로 "4포트로 축소 + 리사이즈까지 계속" 확인받아 진행. 포트 원을
  테두리에서 8px 띄워 표시(연결 앵커는 표시 위치와 분리해 여전히 정확한 테두리 점), 단일
  선택시에만 나타나는 변(단축 리사이즈)+모서리(자유 리사이즈) 핸들 추가(그리드 스냅·40×30
  최소크기 클램프). 구현 중 자체 발견: 포트 축소로 옛 8포트 `.json` 문서를 로드하면
  `resolveEndpoint`가 예외를 던져 `applyDocument` 도중 크래시할 위험 — `isResolvableEndpoint`
  가드로 검증 전에 선제 수정(해당 화살표만 스킵, 나머지 문서는 정상 로드). 10차(같은 날):
  도형 종류(타원·마름모) — 히트/이동/리사이즈 앵커는 계속 기존 `rect.body`(bbox)로 유지하고
  시각만 `<ellipse>`/`<polygon>`으로 그 위에 얹는 surgical 설계라 기존 이동·리사이즈·포트·
  라우팅 코드를 하나도 안 건드림(포트 N/E/S/W 위치가 타원·마름모 실제 외곽선과 수학적으로
  정확히 일치해 재계산도 불필요). 툴바 라디오 버튼 3개로 다음 생성 종류 선택, `kind` 필드를
  undo·복제·저장불러오기 전체에 관통(옛 문서는 `kind` 없으면 사각형 기본값). 11차(같은 날):
  도형 채움색 — Python `_COLOR_PRESETS` 6색 스와치+리셋 버튼, 선택 도형(다중 허용)의 채움에
  적용. Python은 이 팔레트를 선(stroke) 색에 쓰지만, 웹은 선택 강조가 테두리색 교체 방식이라
  선 색에 커스텀 색을 얹으면 "선택됨"과 "이 색"이 같은 채널(테두리)에서 충돌 — 채움색으로
  분리해 회피(선택 코랄 테두리+커스텀 채움색이 항상 공존 가능함을 실측 확인). 12차(같은 날):
  화살표 개별 선택+삭제 — 화살표를 `<path>` 단일 엘리먼트에서 `<g>`(보이는 선 `arrow-line` +
  겹치는 넓은 투명 히트영역 `arrow-hit`)로 구조 변경, 고유 id로 포트쌍이 같은 화살표가
  여럿이어도 정확히 하나만 지목, 도형선택(`selectedIds`)과 화살표선택(`selectedArrowId`)을
  상호배타로 분리(`setSelection`이 항상 먼저 화살표선택을 지움), `deleteArrow` undo 엔트리
  추가. Python `_ArrowItem`이 `ItemIsSelectable`로 이미 되던 기본 조작(화살표만 골라 지우기)이
  웹엔 없던 격차를 메움. 13차(같은 날): 화살표 라벨(더블클릭 인라인 편집) — 도형 라벨의
  `beginLabelEdit`/`commitLabelEdit`을 `kind: 'shape'|'arrow'`로 일반화해 재사용, 라벨
  위치는 경로 호길이 중점에 수평/수직 구간별 9px 오프셋(직교 라우팅 전제, Python의 드래그
  재투영 오프셋은 범위 밖), `setArrowPoints`가 재라우팅마다 라벨 위치도 함께 갱신해 도형이
  움직여도 라벨이 따라감. `label` 필드를 직렬화·cascade복원(`deleteShapes`)·개별삭제복원
  (`deleteArrow`) 전부에 관통, 새 undo 엔트리 `arrowLabel`. 14차(같은 날): 커스텀 색
  다이얼로그("다른 색…") — Python `QColorDialog`를 HTML 네이티브 `<input type="color">`로
  대응, 드래그 중 `input` 이벤트는 undo 없는 실시간 프리뷰만 하고 피커가 닫히는 `change`
  때 시작색 대비 최종값을 1회만 커밋(기존 `fill` undo 엔트리 재사용, 스와치 클릭과 같은
  단위 유지). 네이티브 다이얼로그 자체가 열리고 닫히는 상호작용은 헤드리스 자동화 불가
  영역이라 그 앞뒤 로직(캡처→프리뷰→커밋)만 실이벤트로 검증. 15차(같은 날): Ctrl+A
  전체선택 + 키보드 줌(+/-/0) — 다중선택·마우스휠줌 인프라를 그대로 재사용하는 단축키
  추가뿐이라 설계 결정 없이 착수(Explore 서브에이전트로 Python 대비 "작은 격차" 후보를
  자체조사해 선정). 16차(같은 날): 방향키 도형 미세이동(nudge) — 기본 10px, Shift/Ctrl 시
  1px, 기존 `move` undo 구조 재사용(Python의 연속입력 undo 병합은 이 프로토타입 undo 설계
  전제와 안 맞아 의도적으로 생략 — 여기 undo는 애초에 "시작~끝 1건"이지 "연속값 억제"가
  아님). 17차(같은 날): Shift+H/V 도형 반전(mirror) — 공통 bbox 중심 기준 좌우/상하 반사를
  `move`와 같은 undo 엔트리로 표현(도형이 전부 자기bbox 대칭이라 위치만 옮기면 충분, Python의
  `_mirror_fn`+리베이크 구조는 불필요). 구현 중 자체 발견: 단일 선택 반전은 자기 중심 기준이라
  좌표가 그대로인 진짜 no-op인데, 이 경우 빈 `move` 엔트리를 쌓지 않도록 가드 추가(기존
  드래그 커밋과 같은 패턴). 18차(같은 날): Shift+드래그 이동 축 고정 — 착수 전 Explore
  조사가 "리사이즈 비율고정"으로 잘못 짚었던 걸 실제 `core_view.py`(`_axis_lock`/
  `_apply_axis_lock`)로 재확인해 진짜 의미(도형 이동 중 첫 편차 방향으로 수평/수직 고정,
  일러스트레이터·Figma 관행)를 구현. 구현 중 자체 발견: 그리드 스냅이 고정된 축을 반올림해
  축 고정을 무력화할 뻔한 상호작용 버그를 검증 전 선제 수정(고정된 축은 스냅 대상에서
  제외). 19차(2026-08-06, "작은 격차" 후보 소진 후 deep-interview로 다음 항목 선정): 속성
  패널(선 스타일) + 도형 회전 — PDF/DXF 웹 구현 여부는 이번엔 결정 보류, 사용자가 §5
  "심볼 라이브러리=최고가치" 대신 "속성 패널+회전"을 우선 선택. 회전은 Python 수준 자유회전
  핸들(드래그)로 확정하되, 프레임 챌린지로 "회전된 도형의 포트·화살표가 정확한 회전각을
  따라가야 하는가"를 물어 **v1은 근사로 충분**(추천안)으로 확정 — 시각(테두리·채움·라벨)만
  `<g class="visual-rotate">`에 `rotate()` transform으로 돌고, 포트·리사이즈·회전핸들은
  회전 전 bbox 기준 위치 그대로 유지(화살표가 그 근사 포트에 이미 붙어 있어 회전 자체가
  재라우팅을 요구하지 않는 부수 이득). 속성 패널은 기존 툴바 스와치 확장이 아니라 새
  사이드바로(사용자 선택), 선 스타일은 기존 채움색과 같은 관례로 도형만 대상(화살표 제외).
  구현 중 자체 발견: 새 회전 하위그룹이 끼어들며 `evt.target.parentElement`로 도형 id를
  읽던 클릭·라벨편집 두 곳이 깨질 뻔해 `closest('.shape')`로 선제 수정. **19차를 끝으로 웹
  트랙 중단, Python 집중으로 전환**(위 표제 참조) — 재개 시 여기부터 이어감.
  로컬로 보려면
  `web_prototype/`에서 `python -m http.server 8791` 후 `localhost:8791/index.html`.
  경위·근거는 `docs/history/2026-08.md` "웹 전환 실험" 항목들(2026-08-05 두 건 + 2026-08-06
  열여섯 건), pitfalls는 `docs/pitfalls.md` "라우팅(A*/직교 엘보)" 참조.
- **왼쪽 패널 탭→아코디언 개편 + Mermaid 다이얼로그 재작업(2026-08-12)** — 좌측 팔레트를
  옛 탭 2개(도형/레이어)에서 아코디언 4섹션(기본도형/순서도/내 심볼/레이어)으로 개편,
  "내 심볼"에 폴더(생성·삭제·드래그이동) 신설. 착수 직후 전체 스모크에서 재현된 Windows
  세그폴트(`app.setStyle("Fusion")` 중복 호출 — 창당 위젯 수 증가가 기존 잠재 재현조건을
  앞당김, `faulthandler`로 원인 특정)를 고친 뒤, 실사용 스크린샷 피드백을 5차례 받아 팔레트
  버튼 크기·열 수·폰트·패널 폭을 반복 조정(최종: 4열·48px 버튼·패널 폭 속성/미니맵 패널과
  통일 — QListWidget의 숨은 고정 sizeHint가 진짜 병목이었음, `CanvasWindow.showEvent` 신설
  로 해결). 파일 메뉴를 파일 I/O와 "삽입(&I)"(표제란·표·Mermaid)로 분리 + 이미지/SVG 삽입
  파일선택창 통합. Mermaid 다이얼로그는 프롬프트/코드 1칸 통합 → 재피드백으로 다시 2칸
  분리(참고 이미지의 "Enter=실행/Shift+Enter=줄바꿈" 관례로 이전 2칸 시절의 "AI가 편집
  중인 내용을 덮어쓸 위험"이 구조적으로 재발 안 함), 모델 선택을 2열 병렬 라디오 패널에서
  평범한 드롭다운(gemini/gpt 그룹 헤더)으로, AI 게이트웨이 설정 진입점을 상단 메뉴/툴바
  ↔ Mermaid 창 내부 사이에서 재피드백 따라 되돌림(최종: Mermaid 창 우상단 버튼, 새로고침·
  크레딧 확인은 게이트웨이 설정 창 쪽으로). 스모크 534→542종. 경위·근거는
  `docs/history/2026-08.md` 이 날짜의 연속 항목들(총 8차), 함정은 `docs/pitfalls.md`
  "Qt 시그널·이벤트 발화 조건"·"렌더링" 절 참조.
- **Mermaid 다이얼로그 4차 — 디자인 시안(Artifact) 왕복 후 실코드 반영(2026-08-12,
  같은 날 9차 후속, 스모크 539종)** — 외부 디자인 AI용 프롬프트 요청이 Claude가 직접
  Artifact에 HTML 목업을 만들어 여러 라운드 피드백을 주고받는 방식으로 전환, 확정안을
  `host_dialogs.py`에 반영: 입력칸+첨부·생성 툴바를 카드로 묶어 다크 테마에서도 입력칸만
  흰 배경 고정(코드 편집기와 대비), 이미지 첨부를 툴바 안 컴팩트 칩으로, 입력→코드 사이
  커넥터(원+↓) 추가, 모델 드롭다운 옆 새로고침 버튼 신설, Mermaid 창의 크레딧 표시는
  제거하고 `_AIGatewaySettingsDialog`의 "모델 새로고침"+"크레딧 확인" 두 버튼을 "연결
  테스트" 하나로 통합(크레딧은 "잔여" 표기로 통일). 실제 창 라이트·다크 스크린샷 육안
  확인 완료. 상세: `docs/history/2026-08.md` 같은 제목.
- **§8 항목0 후속 — 200개+ 그룹드래그 성능 조사·구현 완료(2026-08-13)** — 사용자 보고
  ("200개 이상 동시이동 시 심한 버벅임")를 실측 문서(`200.ecad`, 도형 200·화살표 0)로
  확인, 같은 날 앞선 커밋(`43b2a3c`, 화살표 강체-평행이동 최적화)과는 별개로 60fps 예산
  x7.3 초과였다. cProfile로 병목을 `_HandleResizeMixin.boundingRect()`→`_qc_dot_rects()`
  →`_shape_ports()`→`_nearest_border()` 체인(전체비용 86%)까지 좁히고, 같은 날 이미
  시도·실패했던 `_geom_version`+`prepareGeometryChange` 캐시(non-virtual이라 `setFont()`
  같은 네이티브 호출이 무효화를 놓치는 함정)를 피하는 대안 — 이벤트 무효화 없이
  `content_rect()`/`scale`/`handle_px` 값을 매 호출 직접 비교하는 캐시 — 를 구현·반영.
  스모크 543종 전원 통과(이전 시도가 걸렸던 `test_sketch_build_roundtrip` 포함, 신규
  회귀 없음), 실제 창 재실측으로 `drag_all`(200개) 121.81ms(x7.3 초과)→34.84ms(x2.1
  초과, 3.5배), `drag_multi`(20개) 18.61ms(x1.1 초과)→9.97ms(예산 내) 검증 완료. 신규
  벤치마크(`perf_bench.py --only drag_all`)·프로파일러(`profile_group_drag.py`)도 신설.
  남은 x2.1 초과분은 `_sync_geom_snapshot()` 씬 전체 순회(다음 후보, 씬≠선택 규모
  별도 문서로 재확인 필요)와 실제 `paint()` 비용 쪽으로 넘어감. 상세:
  `docs/perf_group_drag_200.md`(원안+구현결과), `docs/history/2026-08.md` 같은 제목,
  함정(실패→성공 대안) `docs/pitfalls.md` "Qt 시그널·이벤트 발화 조건".
- **같은 날 후속 재확인** — `_sync_geom_snapshot()`은 실측으로 전체비용 4%뿐임을
  확인해 후보에서 **제외 확정**. 진짜 남은 비용(~70%)인 `_highlight_band()`(다중선택
  강조 밴드, 스트로크+`simplified()`) 매 paint() 재계산에 같은 값비교 캐시를 시도했으나
  **전체 pytest 스위트에서 비결정적 네이티브 크래시**(exit 127, faulthandler로도
  트레이스백 없음)를 유발 — 대조실험으로 원인이 이 변경임은 확인했지만 근본원인은
  미상이라 **되돌림(커밋 안 함)**. 다음 시도 참고사항은 `docs/perf_group_drag_200.md`
  "⚠ 후속 시도" 절 참조.
- **§8 항목19 화살표 라우팅 전수 점검 완료(2026-08-14)** — 사용자가 "화살표 최적경로 찾는
  알고리즘"을 최다 사용 기능으로 지목, 완성 단계에서 전수감사 요청. 대상: 경로탐색 커널
  (`_route_ortho`/`_astar_ortho*`)+히트테스트/스냅+TRIM/EXTEND 접점+재라우팅 트리거,
  정적 리뷰+실측(경로품질·성능) 병행. **발견 7건 중 우선순위 상 2건 수정**: F1(제3자
  장애물이 연결 도형에 밀착하면 A*가 관통 경로를 조용히 채택 — 클리어런스 사다리 최후수단
  으로 원본 경계 재시도, 관통 0건 전환), F2(부분선택 그룹드래그가 화살표 있는 문서에서
  8배+ 느림 — `_route_ortho` 반복 사전체크에 회랑 사전필터 적용, 최악 x5.0→x2.8). F3
  (사다리 `_seg_hits_rect` 반복호출 — 세그먼트당 루프불변 계산 1회로, cProfile 34%
  개선), F5~F7(죽은 분기 게이트·중복 데코레이터 제거·계수 주석)도 정리. F4(BSP화)는
  실측 결과 역효과라 **되돌림**(교훈만 남김). 스모크 543→544종. 상세: `docs/
  route_review_2026-08.md`(9단계 전문), `docs/history/2026-08.md` "§8 항목19", 함정
  `docs/pitfalls.md` "라우팅(A*/직교 엘보)".
- **DWG 자동변환 열기·저장 완료(2026-08-14)** — 손안의 카드 재확인으로 이미 쓰던
  `ezdxf`(1.4.4)가 ODA File Converter 연동 애드온(`ezdxf.addons.odafc`)을 내장한 걸 확인,
  새 의존성 없이 열기(`_load_ezdxf_doc`)·저장(`export_dwg`, `dxf_export._build_dxf_doc`을
  export_dxf와 공유) 양쪽 구현. ODA 미설치 시 `_prompt_odafc_missing`(다운로드 안내+
  「찾아보기」로 exe 직접 지정 후 재시도)를 열기·저장이 공유. 신규 pytest 16종, 스모크
  544→560종, 실제 미설치 상태에서 안내 분기 실측 확인(실 .dwg 왕복은 사용자 확인 대기).
- **다중 페이지 도면 지원 v1 완료(2026-08-14)** — deep-interview로 v1 스코프 확정(여러
  용지틀 중 하나 골라 그 경계·크기·방향으로 PDF 출력, 일괄 출력은 다음 라운드). `pdf_
  export.py`에 `_list_title_frames`+`frame=` 선택 인자(없으면 기존 자동탐지로 무회귀
  폴백), `_PdfExportDialog`는 프레임 2개 이상일 때만 드롭다운 노출. 신규 pytest 6종,
  스모크 560→566종. 실조건검증: A3/A4 실제 PDF 저장 후 PyMuPDF로 페이지 크기 정확히
  일치 + 픽셀 샘플링으로 프레임 간 내용 격리 확인.
- **§8 항목20 AI SVG 에셋 생성 — A+B단계 완료(2026-08-14)** — "텍스트→AI→SVG 코드→
  에셋" 기능. A단계(게이트웨이 실측, 같은 날 오전): gpt-5.4-mini·gemini-3.6-flash
  둘 다 도메인 아이콘 8/8 성공, 실제 씬 렌더로 시각 품질도 확인. B단계(같은 날 후속,
  스모크 537→566종): deep-interview로 진행표시(동기+WaitCursor, 옛 QThread 워커는
  이미 폐기된 상태였음)·미리보기 레이아웃(가로 카드)·대체 리스케일(대체 도형 bbox
  긴 변 기준) 확정 후 `easycad/ai/text_to_svg.py`(신규)·`svg_import.parse_svg_string`
  (신규, 문자열 입력)·`host_dialogs._SvgAssetDialog`(신규)로 구현, 진입점 2곳
  (삽입(&I) 메뉴 Ctrl+Shift+A · 우클릭 「SVG로 생성…」, 화살표는 재바인딩 안 함 —
  `delete_selection()`의 기존 동작 재사용) 공유. 후보 미리보기는 원본 SVG를
  QSvgRenderer로 그대로 안 그리고 실제 삽입과 같은 파서+펜으로 렌더(SVG 기본 색상
  값이 만드는 "검은 덩어리"류 위험 회피, A단계가 겪은 함정과 같은 종류). 실제 창
  스크린샷으로 다이얼로그·삽입·대체 3경로 검증 후, 사용자가 secrets 파일에 게이트웨이
  키를 저장해준 덕에 **몽키패치 없이 실제 게이트웨이 호출까지 종단 검증**(gpt·gemini
  둘 다 "BNC 커넥터 아이콘" 실생성 성공, 캔버스 삽입까지 스크린샷 확인) — A+B단계
  전체 실키 검증 완료. 상세: `docs/history/2026-08.md` "§8 항목20 AI SVG 에셋 생성"
  A/B단계 + 실키 종단검증 항목.
- **다중 객체 500~1000개 부하 감소 — 1단계 + 2단계 대부분 완료(2026-08-15, 커밋 17건,
  스모크 592→622종)** — 사용자 보고("화살표를 섞으면 훨씬 무겁다, 500개면 다운된 건지
  헷갈릴 정도")를 deep-interview로 목표화(500 ≤16.7ms / 1000 ≤50ms / 놓는 순간 ≤0.5초)
  하고 계획서 `docs/perf_plan_500_1000.md`를 신설해 항목 단위로 진행. **핵심 결정 ⓐ:
  드래그 중에는 화면 품질을 과감히 낮추고 놓는 순간 정확히 복원**(Figma가 1000개에서도
  끌리는 이유) — 이 결정으로 과거 "품질 때문에" 폐기했던 카드들의 전제가 바뀌었다.
  **1-0 측정 기반 고정**이 선행 필수였고 실제로 하네스 자체에서 결함 4종이 나와 그 전
  절대 ms는 전부 무효가 됐다(줌 미통제·`processEvents` 페인트 누락·`repaint` 과대측정·
  시나리오 간 배치 오염 — `docs/pitfalls.md` "검증 방법론"). 기준선이 **"병목은 렌더가
  아니라 A\*"**를 곧바로 확정(1000개에서 도형 1개 드래그 916.6ms인데 순수 렌더 바닥값은
  142.3ms뿐, 전체선택이 오히려 싼 509.9ms인 것도 그때만 강체 평행이동이라 A\*가 안 돌기
  때문)해 2-B를 최우선으로 착수. **최종 실측(1000개, 같은 하네스 재실행)**: 도형 1개
  드래그 916.6→**39.2ms(23배)**, 전체선택 509.9→**187.5ms**, 큰 씬/30개만 825.2→88.3ms,
  놓는 순간 454.4→**374.6ms(상한 500ms 안)**, 호버는 2-H에서 새로 측정축을 만들어
  43.3→21.1ms(선택 없음은 **13.4ms로 60fps 예산 진입**). 완료 항목은 2-A(드래그 세션
  단일 스위치)·2-B(드래그 중 A\* 정지)·2-C(안 그리는 핸들 자리를 boundingRect에서
  예약하지 않음 + 선택밴드 억제)·2-E(Qt 설정 4종 탐색 — 3장은 꽝, `NoIndex`는 같은 날
  재평가로 **확정 폐기**: 인덱스를 바꾸면 화살표 500개 경로가 통째로 달라지고(최대 4620px)
  원표의 이득 수치 자체가 측정 오류였다)·2-F(release 비용)·2-H(호버 경로).
  ⚠ **목표는 미달로 닫혔다** —
  최대 미달은 1000개 전체선택 187.5ms(목표 50ms의 3.7배)이고, 원인은 **같은 문서의 순수
  렌더 바닥값이 97.7ms**라 파이썬 쪽을 더 깎아도 원리적으로 도달 불가라는 것. 남은 유일한
  축은 **2-D(드래그 중 즉시모드 오버레이 — 아이템별 `paint()`/`boundingRect()` 디스패치
  자체를 우회)**이고 그 착수 게이트가 이로써 통과됐다(**→ 바로 아래 항목에서 완료**, 이
  문단의 수치는 그 시점의 기록이다). 과정에서 성능 작업이 만든 실사용
  버그 4건도 함께 잡았다(다중선택 시 이동 자체가 불가 — 화살표 세그먼트가 그룹 조작을
  가로챔 / 드래그 중 화살표가 직각을 잃음 / Ctrl+A 릴리스 2.2초 정지 / `mouseReleaseEvent`
  조기 return 13곳으로 뒷정리 유실). 사용자의 근원적 지적("Lucid는 붙어있는 화살표만
  재계산한다 — 낙장불입")이 release 785ms 벽을 깬 결정타였다(실측: 도형 1개 이동 시
  재계산 13개 중 연결된 건 2개뿐 = 85%가 무관). 상세: `docs/perf_plan_500_1000.md`
  (§5 최종 수치·§4 항목별 결과·§7 열린 질문), `docs/history/2026-08.md` 같은 제목,
  함정 3계열은 `docs/pitfalls.md` "검증 방법론"·"Qt 시그널·이벤트 발화 조건".
- **500~1000개 부하 감소 — 2-D(드래그 프록시) 완료로 계획 종료**(2026-08-15, 스모크
  625→629종) — 드래그 중에는 아이템이 스스로 그리지 않게 하고(`ItemHasNoContents`) 뷰가
  `drawForeground`에서 단순 윤곽을 일괄 렌더한다. 착수 스파이크에서 **계획서 원안의 두 축이
  모두 뒤집혔다**: ⓐ `setVisible(False)`는 Qt가 **선택까지 해제**해(실측 5→1) 멈춰 있던
  A\*가 다시 돌아 오히려 나빠졌다(148ms) — 그래서 `ItemHasNoContents`. ⓑ "선택된 것만"
  감추면 더티영역에 걸린 정지 도형 수백 개가 그대로 다시 그려져 이득이 -5%뿐이라(씬 전체는
  -53~-61%) 대상을 **씬 전체**로 넓히고 아이템 200개 미만 문서는 미발동(사용자 선택).
  진입은 `mouseMoveEvent` 한 곳, 이탈은 기존 `try/finally → _end_drag_session()`(조기
  return 13곳 자동 커버). 오버레이는 기존 `_item_center_path`를 재사용해 `QPainterPath`
  하나로 일괄 스트로크하고 컬링은 씬 공간쿼리로 한다. 라벨 글자는 2-C 결정대로 유지(단
  화면 6px 미만이면 생략). 미니맵은 프록시 중 빈 그림이 나오므로 rebuild를 릴리스까지 미룸.
  **실측(2-D만 켜고 끔)**: 1000개 전체선택 187.8→**123.8ms(-34%)**, 30개만 95.9→67.5,
  20개 93.5→66.2, 도형 1개 39.6→28.6, 500개도 -28~-33%. 놓는 순간 385.8→433.8(상한 500
  안), 드래그 아닌 시나리오는 무변화. **⚠ 목표는 여전히 미달로 닫는다**(1000개 전체선택
  목표 50ms의 2.5배) — 새 바닥은 Qt 더티 계산이 부르는 파이썬 `boundingRect()` 77.9ms이고
  **다음 후보는 계획서 1-1**(캐시 히트율 계측, 미실행). 부수로 하네스 결함 5번째 발견·수정
  (`scn_release`가 드래그 세션을 켠 채 끝나 뒤따르는 zoom/pan/render를 오염시키고 있었음).
  검증: 기하 지문 가드(2-D 끔/켬 화살표 500개 경로 해시 일치 + 플래그 잔류 0) ·
  `perf_baseline_check` 5뷰 일치 · 신규 pytest 4종(수정 전 소스에서 전부 실패 확인, 결과가
  아니라 「일한 양」=드래그 중 paint 0회를 검사) · 실제 창 스크린샷 3장. 상세:
  `docs/perf_plan_500_1000.md` §4 2-D·§5, `docs/history/2026-08.md` 같은 제목, 함정 2건은
  `docs/pitfalls.md` "Qt 시그널·이벤트 발화 조건"·"검증 방법론".
- **실사용 피드백 4건 완료(2026-08-18)** — 기본 도형색을 표제란/표와 같은 "테마 적응
  잉크색"(다크=`#cdd8e3`·라이트=`#111111`, `core_constants._DEFAULT_INK_DARK/LIGHT`)으로
  전환(순수 흰색은 라이트 캔버스에서 안 보임), 미니맵·커스텀 심볼 썸네일이 축소 렌더에서
  1px 선을 못 보여주던 문제를 공유 헬퍼(`core_shapes._min_stroke_render`, 렌더 순간만
  최소 3px로 임시 상향)로 해결, 커스텀 심볼 팔레트 우클릭을 삭제 확인창 대신 메뉴(이름변경/
  삭제)로 교체(`symbol_library.rename_symbol` 신설). 기존 등록된 심볼의 썸네일은 재등록
  전까진 그대로(정적 PNG 저장 방식). 신규 pytest 11종, 직접실행 625→636종, 실제 창 스크린샷
  으로 다크/라이트 기본색·미니맵·썸네일 육안 확인. 상세: `docs/history/2026-08.md` 같은 제목.
- **Shift+휠 다중선택 일괄 적용 + 텍스트/라벨 폰트크기 확장 완료(2026-08-18, 같은 날 후속)**
  — 다중선택 상태에서 shift+휠이 커서 아래 하나만이 아니라 선택된 것 전부에 일괄 적용되도록
  `host_canvas.adjust_selected_properties`(신규) 추가, 독립 텍스트·도형/화살표 라벨(둘 다
  같은 `_TextItem`)은 두께 대신 폰트크기(`_base_pt` 기준)가 조절되도록 확장 — 커서가 라벨
  위에 있으면 라벨 폰트가, 도형 몸통 위에 있으면 도형 두께가 바뀌도록 우선순위 분기.
  속성패널도 단일종류 다중선택 시 개수를 표시(`"사각형 3개"`, 이전엔 혼합 선택일 때만
  개수가 붙었음). 신규 pytest 5종, 직접실행 636→641종, 실제 창에서 QWheelEvent 직접
  발화로 검증. 상세: `docs/history/2026-08.md` 같은 제목.
- **다각형/폴리라인 도구 — 설계 확정 → v1 구현 + 실사용 피드백 2건 완료(2026-08-18, 다른
  PC에서 이어서 구현, 스모크 654→659종)** — deep-interview로 확정한 설계(§8 항목21) 그대로
  `_PolygonItem`(신규 클래스, `_SymbolItem`과 같은 정규화좌표+rect 패턴 재사용) v1 구현 —
  단축키 `9`, 클릭배치(드래그 단계 없음)로 정점 추가, 시작점 재클릭=닫기·더블클릭/Enter=열린
  종료. 이어진 실사용 피드백 2건 반영: ① 정점 배치·시작점에 도형 테두리 스냅(그리드 스냅만
  있던 것 확장) ② 닫기 예고 마커 + 다각형 자체를 다른 도구의 스냅 대상으로 편입(부수 발견:
  `_nearest_border`가 다각형을 외접 사각형으로 잘못 취급하던 잠재 버그도 같이 수정). 전문:
  `docs/polygon_tool_design.md`, `docs/EasyCAD_계획.md` §8 항목21, `docs/history/2026-08.md`
  같은 제목(3개 항목).
- **펜 궤적 실사용 피드백 4건 + SVG 다각형 매핑 수정(2026-08-18, 같은 날 후속)** — SVG
  `<polygon>`/`<polyline>`을 `_PathItem`이 아닌 위 `_PolygonItem`으로 매핑(box 리사이즈·이산
  포트·TRIM 획득), 펜 궤적 계단현상 근본 원인 2건(서브픽셀 정밀도 손실·미스무딩 저장) 수정,
  펜 궤적에도 이산 접속점(양 끝 2점) 추가, 펜 선택 시 양 끝 드래그 핸들 추가. 이 커밋이 함께
  넣은 "select 도구 유휴 호버 억제"가 도형까지 같이 꺼뜨린 부작용은 다음 항목에서 재조정.
  상세: `docs/history/2026-08.md` 같은 제목.
- **화살표 끝점 재부착 예고점 + 유휴 호버 포트점 범위 재조정(2026-08-19, 스모크 704종)** —
  "화살표 머리를 뗐다 다시 붙이면 예고점이 안 보인다"는 보고 → 원인은 이미 그린(선택된)
  화살표의 끝점을 옮기는 경로가 새로 그리기와 달리 예고점 필드를 전혀 안 건드리던 것. 1차
  수정(마커 재사용)은 실사용 재확인에서 "여전히 안 보인다"로 막혀(스턱루프) 임시 실시간
  로그로 실제 창을 추적한 결과, 그 마커가 끝점 핸들 사각형과 완전히 겹쳐(크기·색 동일하게
  설계돼 있었음) 시각적으로 무의미했던 게 원인 — 최종적으로 마커 대신 **끝점 핸들 자체의
  색**(부착 시 초록·미부착 시 파랑)으로 교체해 해결(사용자 확인 완료). 겸사겸사 위 펜 커밋이
  깨뜨린 "도형 유휴 호버 포트점"도 펜(`_PathItem`)에만 좁혀 원상복구. 상세:
  `docs/history/2026-08.md` "화살표 끝점 재부착 예고점 + 유휴 호버 포트점 범위 재조정",
  함정 2건은 `docs/pitfalls.md` "렌더링"·"검증 방법론" 절 끝부분.
- **레이어 패널 좌하단 분리 + 패널 표시/숨김(2026-08-19)** — 레이어를 도형 팔레트
  아코디언에서 좌하단 독립 플로팅 패널로 분리, 모든 플로팅 패널(도형·레이어·속성·미니맵)에
  헤더 우클릭 「닫기」 + 보기(V) 메뉴로 재오픈 추가. 구현 중 만난 비결정적 네이티브 크래시는
  QAction↔panel 연결을 람다 클로저에서 기존 `_make_action` 관례의 `sender()` 기반 바운드
  메서드 연결로 교체해 해결. 스모크 707종. 상세: `docs/history/2026-08.md` "레이어 패널
  좌하단 분리 + 패널 표시/숨김".
- **정렬 가이드선 개선 — 실선/점선 규칙 + 팔레트 드래그 실물 도형화(2026-08-19)** —
  실사용 피드백 2건: ① 팔레트에서 도형을 처음 캔버스로 드래그해 꺼낼 땐 정렬 가이드선이
  안 뜸(한 번 놓았다 다시 드래그해야 뜸) ② 변 3개가 동시에 맞으면 전부 마젠타 실선이라
  산만함. ②는 가이드선에 역할(role)을 실어 `role == "center"`(중심-중심 정렬)만 실선,
  나머지는 점선으로 정리(색은 파랑=선택·초록=연장·빨강=TRIM과 안 겹치는 값이라 유지).
  ①은 1차로 "아직 씬에 없는 사각형" 전용 스냅 계산 경로를 만들어 네이티브 `QDrag`의
  `dragMoveEvent`에 태웠으나, 사용자가 "기존 도형 이동은 드래그 도중 실시간 SNAP되는데
  팔레트 드래그는 드롭 순간에만 확정돼 다르다"고 지적 — 원인이 네이티브 `QDrag`는 OS가
  드래그 중 고스트를 직접 그려 앱이 위치를 못 바꾸는 플랫폼 공통 제약임을 확인하고, 팔레트
  버튼이 드래그 임계를 넘으면 씬에 반투명 진짜 임시 도형을 만들어 `grabMouse()`로 커서를
  따라가며 기존 도형 이동과 **완전히 같은** `_apply_smart_snap()`을 그대로 태우는 방식으로
  재설계(별도 계산 경로는 삭제 — 코드가 오히려 줄었다). 포트·커스텀 심볼은 호스트 부착 등
  별도 배치 로직이라 기존 네이티브 `QDrag`를 그대로 유지(폴백). 신규 pytest 3종, 스모크
  708→711종. 상세: `docs/history/2026-08.md` "정렬 가이드선 개선", 함정은
  `docs/pitfalls.md` "상호작용 설계" 절 끝부분.
- **'내 심볼' 팔레트 아이콘 확대 + 호버 미리보기 + 폴더 이름변경(2026-08-19)** — "다중 도형
  심볼(사각형2+화살표2) 아이콘이 뭔지 안 보인다" + "새 폴더 이름수정이 안 된다" 실사용 피드백.
  내 심볼 전용 아이콘 크기 `_PALETTE_SYM_ICON_PX`(28, 기본도형 18보다 큼) 신설, 호버 시에만
  계산되는 확대 미리보기(160px, `host_selection._symbol_preview_html`, "심볼이 많아지면?"
  확장성 우려에 대한 답으로 지연 계산 설계 — 새로고침 시점엔 안 그림), 폴더 헤더 "✎" 버튼 +
  `symbol_library.rename_folder`(폴더 이름변경은 애초에 미구현이었음, 버그 아님). 신규
  pytest 12종, 전체 pytest 732종 회귀 통과(사전 존재하던 무관 AI 게이트웨이 키 테스트 3건
  제외). 상세: `docs/history/2026-08.md` "'내 심볼' 팔레트 아이콘 확대 + 호버 미리보기 +
  폴더 이름변경".
- **§8 항목20 후속 — Mermaid·SVG AI 다이얼로그 재정리 Stage 1(비동기화+진행표시)
  완료(2026-08-19)** — SVG 생성 시 창이 멈추는 문제를 사용자가 보고, deep-interview로
  6단계 계획 확정 후 1단계만 착수. 원인은 `_SvgAssetDialog`가 gpt+gemini를 `WaitCursor`+
  메인 스레드 순차 동기 호출로 처리하던 것(최악 gemini 20~28초+gpt 합산 블로킹) — 과거
  이미지→도면 기능이 쓰던 `QThread` 워커는 그 기능 폐기로 코드에서 완전히 삭제된 상태라
  새로 설계. `_MermaidGenWorker`/`_SvgGenWorker`(QThread, `host_dialogs.py` 안 — 회귀
  테스트가 `host_ai.py` 존재를 금지) + 공유 `_GenProgressRow`(marquee 진행바+경과시간)로
  교체, SVG는 이번 단계에서 호출 의미(순차·후보 1개씩)는 유지하고 스레드만 옮김(다중후보·
  병렬화는 Stage 2). 기존 pytest 9종 비동기 대응 갱신 + 신규 4종, 전체 pytest 737종
  통과. 부수로 테스트 자체 버그(어서션이 워커 join 전에 실패 → 살아있는 스레드가 patch
  해제 후 진짜 네트워크 함수를 다시 잡음)가 스위트를 몇 시간 멈추게 한 사고를 진단·수정
  (`try/finally`로 항상 join). 실제 창+실제 게이트웨이 호출로 종단 검증(이벤트루프 반응
  틱 수 확인, 마퀴 진행바·완료 결과 스크린샷). 상세: `docs/history/2026-08.md` "§8 항목20
  후속 — Mermaid·SVG AI 다이얼로그 재정리 Stage 1", 함정: `docs/pitfalls.md` "검증 방법론".
- **§8 항목20 후속 — Stage 2(SVG 다중후보+완전병렬) 완료(2026-08-19, 같은 세션 후속)** —
  Stage 1의 Not-tested 항목(게이트웨이가 10개 동시 요청을 견디는지)을 먼저 실측(10/10
  성공)한 뒤 착수. gpt/gemini 체크박스(후보 1개 고정)를 독립 드롭다운(0~5개)으로 교체,
  `_SvgGenWorker`를 "호출 1건=워커 1개"로 재설계해 요청 전부를 동시 시작(모델 간·모델
  내부 구분 없는 완전 병렬), 최대 10장을 담는 가로 `QScrollArea` 추가. 신규 pytest 3종,
  전체 pytest 739종 통과, 실제 창+실제 게이트웨이(4개 동시 요청)로 종단 검증(4개 모두
  다른 모양의 아이콘으로 성공, 첫 도착 후보가 나머지가 도는 중에도 화면에 뜸). 상세:
  `docs/history/2026-08.md` "§8 항목20 후속 — Mermaid·SVG AI 다이얼로그 재정리 Stage 2".
- **§8 항목20 후속 — Stage 3(SVG 이미지 입력) 완료, Stage 1~3 종료(2026-08-19, 같은 세션
  후속)** — `_MermaidDialog`의 이미지 첨부 로직(찾아보기·드래그드롭·Ctrl+V)을
  `_ImageAttachMixin`으로 추출해 `_SvgAssetDialog`와 공유(문자 그대로 복붙 대신 재사용).
  `text_to_svg.py`에 `generate_mermaid`와 동일 패턴으로 이미지 분기 추가(이미지 있으면
  대상 설명 선택사항, timeout 120s). 신규 pytest 12종, 전체 pytest 749종 통과, 실제
  창+실제 게이트웨이로 종단 검증(`docs/reference/`의 실제 참고 이미지 1장, 텍스트 설명
  없이 gpt·gemini 둘 다 서로 다른 아이콘 생성 성공). 상세: `docs/history/2026-08.md`
  "§8 항목20 후속 — Mermaid·SVG AI 다이얼로그 재정리 Stage 3".
- **§8 항목20 후속 — Stage 4(카드 다중선택+내 심볼 저장) 완료(2026-08-19, 같은 세션
  후속)** — 후보 카드에 체크박스("심볼로 저장") 추가(클릭 단일선택과 완전 독립), "내
  심볼로 저장" 버튼→폴더 선택 다이얼로그(기존 폴더/새 폴더)→`CanvasWindow._save_svg_
  candidates_to_symbols`(host_fileio.py, `register_selection_as_symbol`과 같은 위치
  정규화·썸네일 관례)로 한 번에 등록. `host_dialogs.py`는 순환 임포트를 피하는 잎 모듈
  이라 `host_selection.py`를 직접 import 못 해 `getattr(parent, ...)` 위임으로 호출
  (`_MermaidDialog`가 이미 쓰던 관례 재사용). 신규 pytest 20종, 전체 pytest 762종 통과,
  실제 창 종단 검증(격리된 심볼 라이브러리 파일에 실제 기록, 좌측 팔레트 반영까지
  스크린샷 3장). §8 항목20 후속 Stage 1~4 전부 이 세션에서 완료. 상세:
  `docs/history/2026-08.md` "§8 항목20 후속 — Mermaid·SVG AI 다이얼로그 재정리 Stage 4".
- **§8 항목20 후속 — Stage 5(Mermaid 실시간 렌더 미리보기) 완료(2026-08-19, 다른 PC에서
  handoff로 이어받은 세션)** — 코드칸 옆(가로 분할)에 미리보기 패널 추가.
  `_render_mermaid_preview_pixmap`(신규, `host_dialogs.py`)이 실제 삽입 경로
  (`host_fileio._make_mermaid_node`/`_make_mermaid_edge`)와 같은 파서·배치·도형매핑
  (`host_widgets._MERMAID_SHAPE_ITEM`)·직교라우팅(`_PolyArrowItem.build_elbow`)을
  그대로 재사용해 임시 씬에 렌더(신규 mermaid.js 의존성 없음), `_edit.textChanged` →
  350ms 디바운스 `QTimer`로 갱신(AI 생성 결과도 같은 훅으로 자동 반영). 신규 pytest
  9종, 전체 pytest 771종 통과, 실제 창(오프스크린 아님) 스크린샷으로 한글 라벨·엣지
  라벨까지 렌더 확인. 상세: `docs/history/2026-08.md` "§8 항목20 후속 — Mermaid·SVG AI
  다이얼로그 재정리 Stage 5".
- **§8 항목20 후속 — Stage 6(레이아웃 최종 통일) 완료, Stage 1~6 전부 종료(2026-08-19,
  같은 세션 후속)** — 원래 목업("Mermaid 가져오기 Studio v2.0")을 다시 확인해보니
  2칸(설명/코드) 분리 이전 초기안이라 지금 구조와 충돌 — "시각 언어만 차용"으로 범위를
  좁혔다(선택창 확정). ① `_MermaidDialog`/`_SvgAssetDialog` OK 버튼을 "확인 (캔버스
  삽입)"/"확인 (도형 삽입)"·"확인 (도형 대체)"(신규 `confirm_label` 인자, 진입점별로
  다르게) ② Mermaid 코드칸 복사 버튼 ③ SVG 생성 창을 Mermaid와 같은 카드+툴바 스타일로
  통일. 신규 pytest 5종, 전체 pytest 776종 통과, 실제 창(오프스크린 아님) 다크·라이트
  스크린샷 확인. §8 항목20 후속 Stage 1~6 전부 이 세션(다른 PC handoff로 이어받음)에서
  완료. 상세: `docs/history/2026-08.md` "§8 항목20 후속 — Mermaid·SVG AI 다이얼로그
  재정리 Stage 6".
- **'내 심볼' 상단 접기 부풀림 버그 수정 + 폴더별 접기 재개편(2026-08-19, 같은 세션 후속)**
  — "내 심볼" 아코디언을 접으면 패널이 원래 크기로 안 줄고 부풀어 보이던 실사용 버그를
  headless 재현으로 좁혀보니(두 번째 접기부터 재현), 원인은 `QWidgetItem`이 부모 레이아웃에
  캐시한 위젯 sizeHint가 `updateGeometry()` 없이는 위젯 경계를 넘어 전파 안 되는 Qt 함정
  (`docs/pitfalls.md` "Qt 시그널·이벤트 발화 조건" 참조). 사용자가 겸사겸사 제안한 재설계
  (기본도형·내 심볼 최상단 접기 제거 + 내 심볼은 폴더별 개별 접기, 이름변경·삭제는 폴더
  이름 우클릭 메뉴로 통합)를 그대로 구현 — `_AccordionSection`(접기 있음)을 `_StaticSection`
  (접기 없음)으로 교체하고 폴더별 접기를 신설했는데, 이번엔 위젯 경계가 3개(그리드→폴더존→
  섹션)라 `updateGeometry()`를 세 곳(zone·`_custom_sym_body`·`_custom_sym_section`) 모두에
  걸어야 왕복이 완전히 안정됐다(실측 확인). 신규 pytest 4종(옛 최상단 접기 테스트는 폐기),
  전체 pytest 779종 통과, 실제 창 스크린샷으로 4회 왕복 확인. 상세:
  `docs/history/2026-08.md` "'내 심볼' 상단 접기 부풀림 버그 수정 + 폴더별 접기 재개편".
- **'내 심볼' 재개편 후속 실사용 피드백 4건(2026-08-19, 같은 세션 후속)** — ① "+" 폴더
  추가 버튼 코랄색을 테마 중립색으로(코랄은 이 앱에서 "선택 상태" 전용 accent) ② 폴더
  접었다 펼 때 패널 폭이 흔들리던 버그를 실측으로 추적(230px↔208px) — `QGridLayout(widget)`
  생성자 직결이 Qt 기본 여백(9px×4방향)을 물려받는 것 + `_SymbolFolderDropZone`의 자체
  여백이 부모와 이중으로 겹치는 것, 2단계로 완전히 제거(0px 흔들림 확인) ③ 폴더 삭제 시
  소속 심볼을 미분류로 보존하던 정책을 **완전 삭제**로 전환(AskUserQuestion으로 사용자
  명시 승인 — SVG 에셋은 게이트웨이 비용 들여 생성한 자산이라 확인 필수 판단), 확인창에
  삭제될 개수 명시 ④ "미분류→즐겨찾기" 제안은 데이터 모델 충돌(즐겨찾기가 미분류를
  대체하면 폴더 없는 심볼이 갈 곳이 없어짐)을 지적하고 "대체 대신 추가"를 대안으로 제시,
  착수는 다음 세션 deep-interview로 보류(코드 변경 없음). 신규/수정 pytest 4종, 전체
  pytest 781종 통과, 실제 창(사용자의 실제 심볼 라이브러리로) 색상·폭 안정성 확인. 상세:
  `docs/history/2026-08.md` "'내 심볼' 재개편 후속 실사용 피드백 4건", 함정
  `docs/pitfalls.md` "Qt 시그널·이벤트 발화 조건" 절 끝부분.
- **'내 심볼' 즐겨찾기 기능 구현 완료(2026-08-20, 다른 세션 handoff로 이어받음)** — 위 ④
  검토를 deep-interview로 스코프 확정 후 구현. `symbol_library.json`에 `"favorite":
  true/false` 필드(folder와 같은 패턴, 원본 삭제·폴더 완전삭제 시 자동 정리), '내 심볼'
  최상단(미분류 위) 이중표시 섹션(원본 폴더+즐겨찾기 둘 다 노출), 우클릭 토글 1개, 드롭
  대상 아님(우클릭 전용). 이중표시로 sid당 버튼이 2개 될 수 있어 `_custom_sym_buttons`를
  `dict[str, QToolButton]`→`dict[str, list[QToolButton]]`로 확장(소비처 2곳도 함께 수정).
  검색창·리스트뷰·최근사용 3개는 여전히 착수 보류(§8 항목22). 신규 pytest 12종, 전체
  pytest 791종 통과(로컬 secrets발 무관 실패 3건 제외), 실제 창(격리된 임시 라이브러리)
  으로 등록→즐겨찾기→이중표시→해제→폴더삭제 자동정리 전 과정 스크린샷 확인. 상세:
  `docs/history/2026-08.md` "'내 심볼' 즐겨찾기 기능 구현 완료".
- **§8 항목20 후속 — Mermaid/SVG 다이얼로그 UX 개선 8건 + 첫 오픈 지연 수정
  완료(2026-08-20, `/clear` 후 새 세션)** — 실사용 피드백 8건 일괄 반영: 레이아웃
  2분할(왼쪽=입력카드+모델행+코드칸, 오른쪽=미리보기 세로 전체), 미리보기 클릭→확대
  보기 다이얼로그, 모델 드롭박스 폭은 구조상 자동으로 축소, 새로고침 버튼·복사 버튼
  제거, 삽입(&I) 메뉴에 「AI 게이트웨이 설정…」 독립 항목 신설(2026-08-12 "메뉴/툴바
  상시노출 되돌림" 결정을 메뉴 텍스트 항목으로 한정해 재도입, SVG 창엔 애초에 설정
  진입점이 없던 것도 같이 신설), 이미지 첨부 드래그오버 시 카드 테두리 코랄 점선 강조
  (Mermaid·SVG 공통). "Mermaid 창 첫 오픈만 느리다" 검토 결과 `_populate_models()`가
  다이얼로그 생성자 안에서 `list_text_models`를 동기 호출하고 있었던 것이 원인(연결
  재사용으로 두 번째부터 빨라지는 체감과 일치) — `_ModelListWorker`(QThread)로
  비동기화, 3초 지연 시뮬레이션에서 다이얼로그 생성 0.079초로 확인. 자체검증 중
  QThread 필드 하나로만 워커를 추적하면 재빠른 재호출 시 이전 워커가 고아가 돼
  `closeEvent` 가드를 피해갈 수 있는 잠재 크래시를 발견해 선제 가드 추가(이 프로젝트가
  반복 겪은 QThread-vs-destroyed-widget 크래시와 같은 종류). 기존 pytest 3종 비동기
  대응 갱신 + 죽은 테스트 1건 삭제 + 메뉴 회귀 가드 반전, 전체 789종 통과(무관한 사전
  존재 실패 4건 제외 — 로컬 secrets 파일 우선순위 3건 + 전체 스위트 한정 순서의존
  플레이크 1건, 둘 다 이번 변경과 무관 확인). 실제 창(오프스크린 아님) 스크린샷 5장 +
  타이밍 실측으로 자체검증 완료. 상세: `docs/history/2026-08.md` "§8 항목20 후속 —
  Mermaid/SVG 다이얼로그 UX 개선 8건 + 첫 오픈 지연 수정", 함정
  `docs/pitfalls.md` "Qt 시그널·이벤트 발화 조건" 절 끝부분.
- **심볼 라이브러리 저장 위치를 OS AppData → 리포 안(`symbol_library/symbol_library.json`)
  으로 이관(2026-08-20, 같은 세션 후속)** — 사용자가 여러 PC를 git으로 동기화하는 습관이
  있어 "내 심볼"도 그 흐름을 타길 원함(`.claude/handoff/`가 2026-08-18에 같은 이유로
  git 추적 대상이 된 것과 동일 판단). `symbol_library.py._library_path()`가 이제
  `QStandardPaths.AppDataLocation` 대신 리포 루트 기준 `symbol_library/`를 반환(경로
  계산은 파일 위치에서 2단계 상위로 고정, `QStandardPaths` 의존성 제거). 기존 AppData에
  있던 실제 데이터(폴더 1개·심볼 5개, 테스트용 이름들 — 사용자 확인 후 "그대로 이관"으로
  진행)를 새 위치로 복사해 유실 없이 이어짐. 테스트는 `_library_path`를 이미 격리 경로로
  `patch.object` 하고 있어(`tests/_shared.py._isolated_symbol_library`) 구현 변경에
  영향받지 않음. 전체 pytest 790종 통과(무관한 사전 존재 실패 3건 제외, 위 항목과 동일
  원인). `.gitignore`는 새 경로를 특별히 배제하지 않음(다른 작업산출물과 반대로 이건
  의도적으로 커밋 대상).
- **§8 항목20 후속 — SVG 에셋 생성창 UX 개선 6건 완료(2026-08-20, 같은 세션 후속)** —
  모델 GPT/Gemini 고정 2종 → 슬롯 A/B 각각 실제 모델(Mermaid와 같은 그룹 드롭다운, 채우기
  로직 `_fill_model_combo_grouped`로 모듈함수 추출해 공유)+개수 선택으로 확장, 미리보기
  패널 제거 후 후보 갤러리를 3열 그리드로 확장(더블클릭=확대로 대체), 프롬프트 복사
  버튼 신설(`text_to_svg.build_prompt`, 외부 AI에 같은 조건으로 요청하는 워크플로 지원),
  "생성할 대상" 제목을 카드 밖으로(Mermaid와 동일 위치), 첨부 버튼을 클립 아이콘 →
  "+" 정사각형(내 심볼 폴더추가 버튼과 같은 스타일 재사용, `_ImageAttachMixin._build_
  attach_button` 공용화) — `attach.svg`/`_SVG_ACT_ICON_NAMES` 항목은 고아라 함께 삭제.
  먼저 6건 각각 의견(추천안 명시)만 낸 뒤 사용자가 모델 슬롯 범위("2개 유지, 각각
  모델·개수 선택")를 확정해 구현. 테스트는 `_gpt_count`/`_gemini_count`→`_count_a`/
  `_count_b` 이름변경 + `_populate_models()`가 생성자에서 실행돼 실키가 있는 이 PC에선
  구성만으로 네트워크 호출이 뜨므로 Mermaid와 동일하게 `_populate_models`를 no-op으로
  스텁(28개 생성 지점). 전체 pytest 732종 통과, 실제 창 스크린샷 3장(빈 상태·실제
  게이트웨이로 모델 채운 상태+가짜후보 3열 그리드·더블클릭 확대창)으로 자체 검증.
  **같은 세션 재피드백** — 후보 갤러리에 Mermaid 미리보기와 같은 사각 테두리(`QScrollArea`
  가 `QFrame`이라 별도 래퍼 없이 동일 QSS 적용)를 추가하고, 프롬프트 복사 버튼을 상단
  툴바에서 "SVG 코드" 제목 옆으로 옮겨 아이콘 전용(+호버 툴팁)으로 축소(`_act_icon`에
  "copy"/"check" 글리프 신설 — 아이콘 전용이라 복사 완료 피드백은 텍스트 대신 체크
  아이콘 1.2초 스왑). 후속 재확인에서 후보 갤러리 테두리 꼭짓점이 각져 보인다는 지적 —
  원인은 `QScrollArea`(기본 `frameShape=StyledPanel`, 일반 `QFrame`과 달리 `NoFrame`이
  아님)에 `setFrameShape(NoFrame)` 없이 QSS `border`만 줘서 네이티브 사각 프레임이 겹쳐
  그려진 것, `setFrameShape(NoFrame)` 추가로 해결(다크 팔레트로 사용자 조건 재현해 확인).
  **같은 세션 마지막 피드백** — 설정 버튼을 모델 행 끝(줄맞춤 깨짐)에서 우하단 확인/취소
  버튼 옆으로(Mermaid도 동일), "내 심볼로 저장" 버튼 텍스트 제거해 아이콘 전용으로.
  전체 pytest 732종 재통과, 실제 창 스크린샷으로 확인. 상세:
  `docs/history/2026-08.md` "§8 항목20 후속 — SVG 에셋 생성창 UX 개선 6건" + 같은 날
  "SVG/Mermaid 다이얼로그 설정 버튼 위치 + '내 심볼로 저장' 아이콘화", 별도 사고 기록은
  "pytest 게이트웨이 키 소실 재발 — 두 번째 진입점" 참조.
- **pytest 게이트웨이 키 소실 사고 재발 — 두 번째 진입점 발견·수정(2026-08-20, 같은 세션)**
  — 사용자가 "API 키가 또 사라졌다"고 보고. 리포 최상단 커밋(9323180)이 이미 같은 증상을
  `tests/conftest.py`의 pytest autouse fixture로 고친 상태였으나, 이번 세션에서 회귀
  검증에 쓴 `python tests/test_easycad.py`(CLAUDE.md가 그대로 추천하는 "전체 실행"
  명령)는 **pytest가 아니라 자체 러너**라 그 fixture가 안 걸려 같은 경로로 재발했음을
  코드로 확인. `tests/_shared.py`(모든 `test_part*.py`가 pytest·자체 러너 어느 쪽으로
  실행되든 공통으로 거치는 임포트 지점)에서 `gw._SETTINGS_ORG/_SETTINGS_APP`을 격리값으로
  재바인딩해 두 경로 모두 근본 차단 — 실제 레지스트리에 표식 값을 넣고 재실행해 생존을
  확인. **사용자의 실제 키 값 자체는 복구 불가 — AI 게이트웨이 설정 창에서 재입력
  필요**(안내함). 상세: `docs/history/2026-08.md` "pytest 게이트웨이 키 소실 재발 —
  두 번째 진입점", 함정: `docs/pitfalls.md` "검증 방법론" 절 끝부분.
- **SVG 에셋 생성창 5건 완료(2026-08-20, 같은 세션 후속)** — 후보 미리보기 선을 다크
  테마에서만 밝은 전용 색으로(`_render_svg_candidate_pixmap`에 `pen_color` 인자 추가,
  실삽입 색은 무변경), 확대 보기를 하단 Close 버튼 없는 비모달 팝업으로(`_QuickLookDialog`
  — `Qt.WindowType.Popup`은 실측 결과 네이티브 타이틀바까지 없애버려 대신 `WindowDeactivate`
  감지로 자동 닫힘 구현), 후보 카드 열 개수를 창 폭에 반응하게(`_update_candidate_columns`,
  카드 픽셀 크기는 고정 — 재래스터화 비용 회피), "생성할 대상" 입력창을 `QLineEdit`→
  `QPlainTextEdit`(3줄, Enter=생성/Shift+Enter=줄바꿈, Mermaid와 동일 패턴)으로 확장.
  신규 pytest 9종 + 기존 테스트 대량 갱신(`.setText`→`.setPlainText` 17곳 등), 전체
  732종 통과, 실제 창 스크린샷 4장(3열→5열→3열 반응형, 흰 선 대비, 팝업)으로 확인. 상세:
  `docs/history/2026-08.md` "SVG 에셋 생성창 5건 — 미리보기 선 밝기·확대창 팝업화·후보
  열 반응형·입력창 3줄".
- **확대창 갤러리 뷰어화 + 바깥클릭 닫힘 버그 수정(2026-08-20, 같은 세션 후속)** — 위
  항목의 "바깥클릭으로 닫힘"이 실사용에선 안 됐다(Esc만 동작) — 원인은 `WindowDeactivate`
  가 `changeEvent()`가 아니라 `event()`로 전달된다는 것(실측으로 확인, 지난 검증은
  `changeEvent()` 직접호출이라 이 경로 자체를 못 봤음 — 프록시검증 사례). `event()`로
  교체하고 회귀 테스트도 진짜 창 두 개+`activateWindow()`로 재작성. 같은 라운드에서
  확대창을 인스턴스 하나로 재사용하는 갤러리 뷰어로 확장: `←`/`→`(+‹›버튼)로 후보 탐색+
  "n/N·모델명" 표시, 메인 그리드 클릭↔확대창 탐색 양방향 동기화, `Space`로 "심볼로 저장"
  체크 토글. 신규 pytest 9종(실제 창 활성화 전환 포함) + 전체 732종 통과, 실제 창
  스크린샷 5장으로 확인. 상세: `docs/history/2026-08.md` "확대창 갤러리 뷰어화 + 바깥클릭
  닫힘 버그 수정", 함정: `docs/pitfalls.md` "Qt 시그널·이벤트 발화 조건" 절 끝부분.
- **SVG 창 좌우 열 폭 균일화(2026-08-20, 같은 세션 후속)** — "오른쪽 생성후보 테두리
  대비 왼쪽 입력창이 좁다, Mermaid는 균일함" 지적 — Mermaid는 양쪽에 같은 최소폭 상수
  (420)를 쓰는데 SVG 창은 왼쪽(`_code_edit` 300)·오른쪽(`_candidates_scroll` 392)이
  서로 달랐다. `self._lr_min_width`(오른쪽 계산식 재사용, 392) 하나로 통일해 기본
  크기·최소크기 둘 다에서 좌우 폭이 정확히 같아짐(코드 값 비교 + 스크린샷 2장 확인).
  전체 pytest 732종 재통과. 상세: `docs/history/2026-08.md` "SVG 창 좌우 열 폭 균일화".
- **실사용 피드백 4건 — 무콘솔 런처·기본 도형색 순백 환원·속성패널 재구성(2026-08-20,
  같은 세션 후속)** — ① 더블클릭 시 터미널이 함께 뜨는 문제로 `run.pyw`(콘솔 없는
  `pythonw.exe` 연결) 신설, 개발용 `python run.py`는 그대로. ② 기본 도형색을 2026-08-18의
  `#cdd8e3`에서 순수 흰색으로 환원 — `pdf_export`의 "화면 흰색→종이 검정" 인쇄 치환
  (2026-07-29 확정)이 `#cdd8e3`는 임계값 미달로 못 잡아 A4 인쇄 시 흐릿하게 나오던 것을
  사용자가 지적, 순백으로 되돌리면 화면·인쇄 둘 다 대비가 좋아짐(표제란/표 잉크색은 별도
  상수라 무변경). ③ 속성패널 '종류'+'도형 바꾸기'를 한 행(`QStackedWidget`)으로 통합,
  '화살표'+'방향' 인접 배치, '선'/'화살표 종류' 드롭박스를 아이콘 방식(`_pen_style_icon`/
  `_arrow_kind_icon`, `core_constants.py` 신설)으로 통일 — 화살표 종류 선택은 QToolButton+
  QMenu에서 QComboBox로 위젯을 바꿔 '선' 드롭박스와 같은 방식이 됨(옛 `_build_routing_menu`
  삭제). 신규 pytest 2종, 전체 808종 통과, 실제 창 스크린샷으로 확인. 상세:
  `docs/history/2026-08.md` 같은 제목.
- **실사용 재확인 3건 — 속성패널 폭 요동/미니맵 여백·아이콘 크기·전역 흰색화(2026-08-20,
  같은 세션 후속)** — ① 선택 여부에 따라 속성패널 폭이 190↔170으로 흔들리고 미니맵도 따라
  흔들리던 문제(오늘 변경 이전부터 있던 버그, `git worktree`로 직전 커밋과 대조해 확인) —
  안내문(`_pf_hint`) sizeHint에 상한이 없던 게 원인, `setMaximumWidth()`로 190 고정(좁은
  쪽 170에 맞추면 미니맵 줌 라벨이 오프스크린 폰트에서 말줄임되는 회귀가 생겨 넓은 쪽으로
  통일). ② '선'/'화살표' 아이콘 콤보가 `QComboBox` 기본 iconSize(16×16)에 짜부라져 안
  보이던 것 — 텍스트를 없애고 아이콘을 키움(72×18/72×24, 클래스 상수로 생성·테마 재도색
  양쪽 공유), 이름은 툴팁으로 유지. ③ 도형 기본색(흰색)은 이미 반영 중이었으나 그 색을
  고르는 UI 아이콘·글자가 여전히 `#cdd8e3`라 흐릿해 보이던 것 — `_ICON_COLOR_THEME["dark"]`
  와 `_dark_palette()`의 WindowText/Text/ButtonText/ToolTipText를 전부 흰색으로 통일(표제란/
  표 잉크색은 별도 상수라 무변경). 신규/갱신 pytest, 전체 808종 중 805종 통과(로컬 secrets
  무관 사전 존재 실패 3건 제외). 상세: `docs/history/2026-08.md` "실사용 재확인 3건" 참조.
- **실사용 피드백 9건 — 레이어 코랄선·정렬아이콘·PDF 선택영역/여백 버그·내보내기(PDF/PNG/
  SVG) 통합·도구 메뉴 + 후속 3건(2026-08-20, 다른 세션)** — "일단 진행하지 말고 의견줘"로
  시작한 9건 검토 후 승인받아 구현. **확정 버그 4건**: 레이어 패널만 제목줄 코랄 밑줄이
  빠졌던 것(`_apply_theme`의 `head_qss` 목록 누락) 추가, 정렬가이드선 툴바 아이콘이
  완전히 투명하게 그려지던 것(`_act_icon("align")`이 SVG 목록·글리프 어디에도 없었음)
  `align.svg` 신설로 수정, "격자 (스냅투그리드)"만 영어를 한글 음역해 다른 토글과 패턴이
  깨졌던 걸 "격자 (Grid Snap)"로 통일, "새 창"을 "새로 만들기" 바로 아래로 재배치.
  **PDF 버그 2건**: "선택 영역" 내보내기가 실제로는 선택 항목 bbox와 겹치는 비선택 도형도
  함께 냈던 것(`scene.render`가 source 사각형과 겹치는 아이템을 전부 그리는 Qt 기본 동작)
  — `_paint_scene`에 `isolate_selection` 추가해 비선택 아이템을 렌더 동안 숨김. "여백이
  이상하다"는 실측으로 새로 발견한 원인 — `QGraphicsScene.render(target, source,
  KeepAspectRatio)`가 종횡비 안 맞을 때 남는 여백을 target 좌상단으로 몰아주는(가운데
  정렬 아닌) Qt 기본 동작이었음, `_centered_target_rect()` 신설로 해결. **내보내기 통합**:
  `pdf_export.py`에 `export_image`(PNG)·`export_svg`(`QtSvg.QSvgGenerator`, 새 의존성
  없음) 신설, `_PdfExportDialog`에 형식 콤보 추가해 File 메뉴 "내보내기" 하위메뉴·우클릭
  메뉴가 진입점 기본값만 다르게 하나를 공유. **도구(&T) 메뉴 신설** — 상단 툴바 8개 도구를
  미러링, 체크상태·아이콘을 `set_tool()`/테마전환과 동기화. **후속 3건**: 화살표 콤보가
  선 콤보보다 커 보이던 것(`_PROPS_ARROW_ICON_H` 통일)·곡선 아이콘이 직선과 구분 안 되던
  것(수직 이등분선 방향 bulge로 재설계)·"다크 흰 아이콘이 안 보인다"는 오해 정정 후 실제
  비대칭이던 라이트 테마 아이콘색(`#39434f`)을 순검정으로 대칭 적용. 세션 도중 다른 창
  (다른 세션, Opus 4.8)이 겹치는 파일 3개를 커밋(`38a5ff8`)한 것을 뒤늦게 발견 — 커밋 전
  `git diff`로 hunk 단위 전부 대조해 겹치는 로직 없음을 확인 후 진행. 검증용 임시 스크립트가
  실수로 앱의 영구 테마 설정(QSettings)을 라이트로 바꿔놓은 것도 발견해 즉시 원복. 신규
  pytest 15종, 전체 813종 통과(로컬 secrets 무관 사전 존재 실패 3건 제외). 상세:
  `docs/history/2026-08.md` 같은 제목, 함정 2건은 `docs/pitfalls.md` "렌더링"·"검증 방법론"
  절 끝부분. **미해결**: PDF 용지크기/방향 "안 먹힘" 재현(표제란 락일 가능성, 사용자 재확인
  대기), "내 심볼" 개별 썸네일이 옅게 보이는 문제(팔레트 아이콘과 다른 메커니즘 — 다음
  라운드 후보).
- **라벨 3건 — 재편집 하이라이트 잔류 수정·다중 라벨 지원·선-갭 세로 여백 비대칭
  수정(2026-08-21)** — ① 더블클릭 재편집 후 아무것도 안 고치고 나가도 Qt 선택
  하이라이트(주황)가 영구히 남던 버그(원인: select-all 해제 누락) + 텍스트류 "채움"
  속성패널 미연결 수정. ② 화살표 하나에 라벨 여러 개(`_LabelMixin`이 `_labels` 리스트로
  소유하도록 재설계, `t`/`off`는 라벨 인스턴스 자신이 소유) — 빈 자리 더블클릭=새 라벨,
  기존 라벨 더블클릭=재편집, Delete=그 하나만 삭제. 옛 단수 `label`/`.ecad` 필드는 하위호환
  유지. ③ 세로선 라벨의 위쪽 여백이 아래보다 2배 넓던 버그 — 선-갭 사각형이 잉크가 아니라
  Qt 문서박스(줄간격 포함) 기준이던 것을 `_ink_vertical_span`(실측 잉크 구간)으로 교체.
  상세: `docs/history/2026-08.md` 이 세 제목(같은 날 연속 커밋).
- **종류 콤보 실제 도형명 표시 + 내 심볼 그룹삽입 지원 + 정리 2건(2026-08-21, 같은 날
  후속)** — '종류' 콤보가 모든 커스텀 심볼을 "심볼" 한 단어로 뭉개던 버그 수정(실제 `_kind`
  표시), 도형 바꾸기 드롭다운에 "내 심볼"을 폴더별 구분선과 함께 노출(단일/다중 아이템
  심볼 모두 그룹으로 삽입). 부수로 '종류' 콤보의 기본도형 사이 불필요 구분선 제거 + 진단
  로그(`_dbg2`)를 `core_view.py`의 기존 `_dbg`와 분리(같은 파일 공유로 pytest 반복 실행이
  사용자 재현 로그를 오염시키던 사고 재발 방지). 상세: `docs/history/2026-08.md` "종류 콤보
  실제 도형명 표시 + 내 심볼 그룹삽입 지원"·"기본도형 사이 불필요 구분선 제거 + 진단로그
  파일 분리".
- **단축키 설정 창 신설 + 실사용 검토 6건(2026-08-21)** — 메뉴/툴바 QAction 단축키(~20종)와
  `core_view.py`가 raw key 비교로 처리하던 뷰 단축키(도구전환·복사/붙여넣기/undo 등,
  ~26종) 46개를 `easycad/canvas/shortcuts.py`(신규) 레지스트리 하나로 통합, 편집(&E) →
  "단축키 설정…"에서 카테고리별 재할당(중복 감지 포함) 가능. 함께 처리한 5건: 우클릭 메뉴
  구분선 시인성(QMenu 인스턴스별 QSS), 내 심볼 우클릭 "SVG로 내보내기…", 도형 '종류' 콤보를
  팔레트 노출 8종으로, 도움말(&H) 독립 메뉴 신설, `__version__` "0.0.1"→"0.1.0"(버전관리
  시작, 이후 git 태그+`__version__` 동시 갱신 관례). 신규 pytest 22종, pytest 826→846종.
  상세: `docs/history/2026-08.md` "실사용 검토 요청 6건 구현 + 버전관리 시작".
- **화살표 스왑 머리쪽 재바인딩 버그 수정(2026-08-21, 같은 날 후속)** — 종류 콤보로 도형을
  바꾸면 꺾인(4점 이상) 화살표의 머리쪽만 새 도형을 안 따라오던 버그. `_arrows_bound_to`가
  `_PolyArrowItem` 끝점 인덱스를 하드코딩된 `1`로 돌려줘 직선 2점 화살표만 우연히 맞고
  A* 꺾인 화살표는 재바인딩이 조용히 무효였던 것 — 실제 끝 인덱스(`len(pts)-1`)로 수정.
  신규 pytest 1종, pytest 852종. 상세: `docs/history/2026-08.md` "화살표 스왑 머리쪽
  재바인딩 버그".
- **단축키 도움말 창 잘림 수정 + 상단 툴바 도구 순서 재배치(2026-08-21, 같은 날 후속)** —
  사용자 스크린샷 제보로 확인: 도움말 창이 `QMessageBox`라 목록이 화면 높이를 넘으면
  스크롤 없이 잘렸다 — 스크롤 가능한 `QDialog`(`QPlainTextEdit`)로 교체. 상단 툴바 도구
  순서를 `선택→화살표→텍스트→직선→다각선→펜→번호→트림`(곧은 도구끼리 인접)으로 재배치
  (`core_constants._TOOLS` 순서만 변경). 실제 창 스크린샷으로 스크롤·순서 둘 다 확인,
  pytest 852종 재통과. 상세: `docs/history/2026-08.md` 같은 제목.
- **도형 단축키(사각형2·원5) 폐지 + 상단 툴바 도구 1~7 재번호(2026-08-21, 같은 날 후속)** —
  좌측 팔레트 기본도형 8종 중 사각형·원만 옛 상단바 시절 숫자 단축키가 유령처럼 남아있고
  (팔레트 툴팁도 언급 안 함) 나머지 6종은 애초에 없던 것을 사용자가 지적해 확인, 8종 전부
  클릭/드래그 전용으로 통일(`shortcuts.py`에서 `tool_rect`/`tool_ellipse` id 완전 삭제 —
  재사용 금지 목록에 등재). 비워진 2·5 포함 나머지 도구 7개를 툴바 시각 순서 그대로 1~7로
  재번호(T=자르기·S=스트레치는 문자 니모닉이 나아 유지). pytest 852종 재통과. 상세:
  `docs/history/2026-08.md` 같은 제목.
- **Mermaid 가져오기 창 실사용 피드백 3라운드(2026-08-21, 같은 날 후속, pytest
  852→861종)** — ① 방향 콤보(가로/세로 4종, 코드 1행↔콤보 정규식 양방향 동기화) +
  다크 대비·중앙정렬 버그 2건(`_ICON_COLOR` import-시점 캡처 함정 재발 — 코드 주석에
  이미 문서화된 패턴 재사용, `pdf_export._centered_target_rect` 재사용). ② 추천 gpt
  모델이 게이트웨이에서 은퇴돼(404) 기본값이 항상 실패하던 버그 — 실측으로
  `gpt-5.6-luna` 교체 + `_fill_model_combo_grouped`의 강제 유니온을 계열 내 자동
  폴백으로 근본 수정, 미리보기를 클릭-확대 창에서 패널 내 휠줌/드래그팬
  (`_MermaidPreviewView`, 캔버스 본체 줌 관례 재사용)으로 교체. ③ gpt-5.6 계열
  (luna/sol/terra) 전부가 확장 요청에 노드 1개만 돌려주는 경우가 잦고 비용도
  불안정함을 재실측해 Mermaid 전용 `TEXT_RECOMMEND_MERMAID`(gemini-3.5-flash-lite)로
  분리(SVG 에셋 창의 GPT/Gemini 비교 슬롯은 설계 의도상 무변경) + 폴백 휴리스틱을
  "lite 붙은 이름 중 최고 버전 우선"으로 개선 + 이미지·TTS 전용 모델이 텍스트
  목록에 새던 필터링 버그 수정. 상세: `docs/history/2026-08.md` "Mermaid 가져오기
  창 실사용 피드백 3라운드"(다른 세션, Opus 4.8, 3개 커밋 통합 기록). **Not-tested**
  (원 커밋 명시): gpt-5.6-luna 품질 정식 벤치마크, 드래그 패닝 실이벤트 재현,
  SVG 에셋 창 슬롯 기본값의 같은 불안정성 여부.
- **속성패널 6항목 재정렬 + 화살표 종류-화살촉 통합 + 양방향 화살표 + 회전 각도(2026-08-21,
  같은 날 후속)** — 실사용 피드백 4건 일괄 반영: 항목 순서를 `종류→채움→색→선→두께→폰트`로
  (색↔채움, 두께↔선 교체), 화살표 종류 콤보를 별도 행에서 '종류' 스택 3번째 페이지로 이동해
  도형 바꾸기와 자리 통일, 신규 `_head_at_start`(끝쪽 `_head_at_end`와 독립)로 화살촉
  없음/끝만/양쪽/시작만 4상태 지원(`_ArrowItem`/`_PolyArrowItem` 둘 다 — `_head_points()`
  시그니처를 "삼각형 1개"에서 "삼각형 0~2개 리스트"로 확장, DXF 내보내기·`.ecad` 직렬화
  `head_start` 필드까지 반영, 가져오기는 스코프 밖), 회전 안 되는 도형(화살표 등,
  `_uses_endpoints()`)을 뺀 회전 각도 숫자입력 신설. 신규 pytest 6종, 전체 868종 통과.
  상세: `docs/history/2026-08.md` 같은 제목, 함정(capture_state vs capture_geom undo
  경로 오선택)은 `docs/pitfalls.md` "검증 방법론" 절 끝부분. **Not-tested**: DXF 왕복 시
  화살촉 개수 보존 여부(가져오기 쪽은 기존 휴리스틱이라 단일머리만 복원할 가능성).
- **회전 스핀박스 타이핑 불가 버그 + 화살촉 크기(배율) 신설(2026-08-21, 같은 날 후속)** —
  커밋 전 사용자 재현 2건: 회전 필드가 `FocusPolicy.NoFocus`라 클릭해도 타이핑이 아예
  안 됨(반경 스핀박스도 같은 결함 보유 — 둘 다 `ClickFocus`로 교체, 오프스크린 실측으로
  "휠은 포커스 안 뺏음·클릭은 타이핑 됨" 확인), Lucid 대비 화살촉이 작다는 지적으로
  기본 공식 상향(`max(width*2.5,7.0)`→`max(width*3.2,11.0)`) + 사용자 배율 `_head_scale`
  신설(속성패널 '머리크기' 0.5~3.0×, DXF·`.ecad`·스타일복사까지 관통). 신규 pytest 3종,
  전체 871종 통과. 상세: `docs/history/2026-08.md` 같은 제목, 함정 2건(NoFocus vs
  ClickFocus, QApplication 없이 위젯 생성 시 무말없는 크래시)은 `docs/pitfalls.md`
  "Qt 시그널·이벤트 발화 조건"·"검증 방법론" 절 끝부분. **Not-tested**: 밀집 도면에서
  커진 화살촉이 다른 요소와 안 겹치는지 실사용 확인.

## 작업 규칙
- GUI라 **offscreen 스모크로 프록시검증** 후, **실조건은 먼저 직접 재현 시도**(전역 CLAUDE.md
  규칙 11-d, 2026-07-31 개정) — 이 환경은 Bash가 사용자와 같은 대화형 데스크톱 세션이라
  `QT_QPA_PLATFORM=offscreen` 없이 같은 재현 스크립트를 그대로 돌리면 실제 창·콘솔 로그를
  직접 확인할 수 있다(마우스 클릭 자체가 아니라 결과 상태가 필요하면 API 호출로 대체 가능).
  정말 대리 불가능한 것(마우스 드래그의 손맛·실제 물리 기기·다중 모니터 조합)만 사용자에게
  `python run.py` 확인을 요청한다.
  ⚠ 전례: 지속연결 초안이 offscreen을 통과했으나 GUI에서 버그 발견(플로팅→고정 부착점으로 수정).
  즉 **offscreen 통과 ≠ 해결**. GUI 확인(자체 실행 또는 사용자 확인) 전 "해결" 단정 금지.
  같은 부류 함정을 다시 만나면 먼저 `docs/pitfalls.md`를 훑는다.
- **레이아웃·렌더링 시각 변경은 `python tools/screenshot.py`로 자체 검증**(PNG 렌더 → 직접 확인).
  툴바·팔레트 배치·도형·아이콘·색·위치는 이걸로 잡는다. 단 ⓐ 한글 텍스트는 헤드리스 폰트 없어 □로
  뜨고 ⓑ 상호작용 '느낌'(hover·드래그·스냅)은 못 잡으므로, 그 둘은 여전히 실조건(사용자 화면) 몫.
- 각 기능은 검증가능 목표로 닫고, 새 스모크는 `tests/`에 추가(임시폴더 금지).
- 비자명 커밋엔 트레일러(Rejected/Constraint/Confidence/Not-tested) + `Co-Authored-By: Claude Opus 4.8`.
- 계획/검토 요청이면 코드 손대지 말 것(승인 게이트). "고쳐줘/만들어줘"면 실행.
- 코어는 pasteflow에서 복사해 분기한 것 — `core_shapes.py`/`core_view.py` 편집 허용(단
  surgical하게, 주석으로 우리 확장 표시). `annotator_core.py`는 재수출 shim이라 정의가 없다.
- **완료한 비자명 작업의 경위·근거는 `docs/history/<년-월>.md`에 그 달 파일 맨 끝에 이어 쓴다**
  (CLAUDE.md 본문에 다시 쌓지 않음 — 2026-08-02 분할의 취지 유지). 새로 겪은 함정은
  `docs/pitfalls.md`에도 한 줄 추가.
